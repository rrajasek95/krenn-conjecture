[project]
name = "krenn-conjecture-research"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "networkx>=3.6",
    "numpy>=2.0",
    "scipy>=1.14",
    "sympy>=1.13",
    "python-sat>=1.8.dev1",
    "python-flint>=0.9",
]

[tool.uv]
package = false
