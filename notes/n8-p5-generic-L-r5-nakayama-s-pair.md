# P5 generic-L shifted Nakayama S-pair

The first missing principal S-pair now satisfies a genuine shifted-ideal
relation at the next dual order.  This is stronger than a remainder lying in
`tau*R`, but it is still only a `mod tau^2` prefix of the complete-local
principality lemma.

## Exact quotient

Work after the 207-row Schur graph on the localized generic-L centre

```text
L = F1 = F2 = G = 0,
b,z11,z16,z41,u != 0,
u = z26+b-z44,
v = z26-z44.
```

For each of the 26 nonzero mixed rows, retain the shifted dual germ

```text
M_i = Q7_i + tau*Q8_i  (mod tau^2).
```

Thus `tau*I` in this quotient is generated by the source-faithful rows
`tau*Q7_i`.  The checker uses exactly the one-based rows

```text
1,3,4,6,7,9,10,11,14,15,16,18,19,21,22,25,26,
27,28,30,31,33,34,36,37,38.
```

It adjoins these 26 generators, `tau^2`, the four centre equations, and the
five localization equations, and performs an exact characteristic-zero
Singular reduction.  The resulting ideal is not the unit ideal.

## S-pair result

The all-order newest-bend audit gives leading responses

```text
U30 = C*u,
U33 = C*v,
C = 1/2*z11*z16^2*z41.
```

The corresponding lead-cancelling S-pair satisfies the exact relation

```text
v*M30 - u*M33 in tau*I  (mod tau^2).
```

Its 74,350-term expanded numerator has zero normal form against the explicit
shifted-ideal generators.  The opposite-sign guard
`v*M30 + u*M33` has 103,514 terms and an exact nonzero 80-term normal form.
This both fixes the signs and rules out accidental annihilation by a unit
localization.

The standard-basis normal form is an exact ideal-membership certificate; a
fully expanded coefficient lift to the original 36 generators was separately
capped after 300 seconds and is not claimed.  Source provenance is frozen by
the explicit 26-row generator list above.

## Consequence and remaining gap

This supplies one nontrivial row of the desired Nakayama recurrence one layer
beyond the previously committed dual prefix.  It is consistent with the
proposal that `I=(M30)` in the complete local quotient.  It does **not** prove
that proposal: one still needs identities

```text
M_i = a_i*M30 + tau*sum_j B_ij*M_j
```

for every mixed row, uniformly in all filtered orders (or one finite
source-level relation-module identity implying them).  Only then does
Nakayama yield complete-local principality and permit the Ward transgression
to close the pure H0/H1 membership statement.

The exact checker is
`computations/analyze_n8_p5_generic_L_r5_nakayama_s_pair.py`.  Its frozen
ledger has SHA-256
`dc44f0cacf44bcb191a47d243ec2137cf1efe6409a1839c2dcf381d63b52e8a4`.
