# The centered shear reduces to one augmented cross-word bridge

## Result

The centered-response attack has an exact trichotomy.

1. The ordinary Segre--Tate resolution preserves the physical degree-zero
   algebra, but a non-tangent occurrence shear cannot lift to it.
2. Adjoining `d epsilon=L` makes the lift recursive, but replaces the
   physical algebra `A` by `A/(L)`.
3. The `H0`-preserving cylinder exports the failure as `t*k`, where `k`
   spans the endpoint-odd by matching-standard representation.

The third arm does not create a new target theorem.  Once a target-normal
source generator is physically placed, homogeneous polynomial/Macaulay
closure supplies all `k`-multiples automatically.  A pure target generator
cancels `t*k` completely.  The known cap graph has target and ordinary
residue both equal to one, so its `-k` multiple cancels the target and leaves
only

```text
(target, ordinary residue)= (0,-k).
```

The coefficient character of this residue is exactly the four-corner
`-delta` already constructed by the physical endpoint-odd Cartan prism.
Standard graph transport cannot supply it because every standard corner
obeys `R=D`, whereas the desired class has `D=0` and `R!=0`.

Hence the local frontier is one source-labelled comparison:

```text
11:110000 / pure-00 response occurrence
          ->
01211222 (1211222 after deletion) / labelled repeated P3+K2 cap packet.
```

It must carry the cap generator and its principal companion, the residue
class, anchor and physical `q`, `W`, and the shifted Kähler ridge with its
eta/sigma contractions.  Physical Cartan descent in the target grade is
already proved; what is missing is the word/fine/repeated-grade bridge into
that packet.

This rules out three misleading next attacks: adding further Segre
relations, constructing a separate mixed target representation, or calling
the local residue dual a terminal before augmented source placement.

The full unary and pure-target rows sharpen the support alternative.  Every
offdiagonal completion enters the proved private-site fan and hence the
four-good-or-coloop route.  All 1,440 viable orientations at the smallest
axis-pure support have unavoidable crossed monomials.  The only remaining
support escape is therefore a larger axis-pure packet using multi-term
cancellation; this is parallel to, not a replacement for, the cross-word
comparison.

Verification:

```text
python3 computations/verify_h3_centered_shear_to_cartan_single_bridge_reduction.py
python3 -O computations/verify_h3_centered_shear_to_cartan_single_bridge_reduction.py
python3 -I -S computations/verify_h3_centered_shear_to_cartan_single_bridge_reduction.py
```

Frozen ledger SHA-256:

```text
4be857848f9e83390378f7304f77fb9819fe981d408ff15069b0d3e472f41da4
```
