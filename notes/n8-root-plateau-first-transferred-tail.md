# First exact transfer below the N=8 root plateau

The 31 pure matching-triple root orbits form the diagonal-12 plateau.  Their
31 incident even-complement column orbits have rank 24.  The existing exact
lex contraction retains provenance in the original columns and leaves seven
kernel source columns, with canonical indices

```text
18, 19, 21, 23, 26, 29, 31.
```

Replaying those seven rational kernel combinations against the **full**
105-term hafnian fibres, rather than only their root entries, computes the
first actual transferred relations.  All diagonal-12 terms cancel exactly.
The seven nonzero tails contain 694 terms:

```text
per tail:       66, 95, 47, 153, 157, 61, 115
diagonal 10:    343 occurrences on 147 state orbits
diagonal 9:     169 occurrences on 104 state orbits
diagonal 8:     182 occurrences on 110 state orbits
```

The seven diagonal-10 initials have rank seven (a single modular computation
already proves exact rank seven because there are only seven columns).  Thus
none of the seven top-kernel column classes dies at this first transfer: each
has an independent leading differential into the next nonempty diagonal
level.  This is the first genuine homological-perturbation output and is not
the raw bottom incidence matrix.

The next smallest closed calculation is the connected diagonal-10 maximal
plateau generated by the 147 leading state orbits, together with all maximal
even-fibre columns incident to it.  Contracting that plateau will transfer
these seven relations to levels 9 and below.  No claim about the final bottom
homology is made before that contraction.

These statements are in the `S8 x S3` orbit-compressed incidence module.  The
later provenance audit in `n8-orbit-transfer-localization-guard.md` shows that
all seven kernel representatives mix columns with no common incident root
chart.  They are not individual `P_j`-localized membership certificates.

The exact checker is
`computations/verify_n8_root_plateau_transferred_tail.py`.  Its frozen ledger
SHA-256 is
`99b60126095523705554a78ac08c41b85230f7d255979e09b07e0531285e8c57`.
