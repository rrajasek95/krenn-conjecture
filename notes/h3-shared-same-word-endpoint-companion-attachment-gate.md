# Shared same-word endpoint/companion attachment gate

## The convergence is uncoloured, not source-graded

The leftover in `66af3a5`, at face (v=1) and (N=23\mid45), is

\[
 q_{1,N}=a_{23}^{21}a_{45}^{12},
 \qquad \partial\bar b=-\Omega _1+q_{1,N}.             \tag{1}
\]

The canonical E14 private cycle has the common internal tail

\[
 a_{24}^{11}a_{35}^{11}
\]

and exchanges the endpoint orientations

\[
 E_+=(p_1@0)(s_1@1)a_{24}^{11}a_{35}^{11},\qquad
 E_-=(p_1@1)(s_1@0)a_{24}^{11}a_{35}^{11}.             \tag{2}
\]

Swapping residual sites (3\leftrightarrow4) sends the uncoloured matching
(24\mid35) to (23\mid45).  There are exactly eight residual-site
permutations with this property.  None extends, even after any global colour
permutation, to a decorated relabelling: the E14 tail has colour pattern
`1111`, while (1) has pattern `2112`.  A global colour permutation cannot
turn a constant pattern into a two-colour pattern.

The source-operation grades also differ.  Equation (2) has endpoint grade
((\deg p_1,\deg s_1)=(1,1)) and two internal (q)-cells.  The companion in
(1) is endpoint-free with two internal (q)-cells.  Its physical source word
is `01211222` (zero-endpoint chart `00211200`), whereas the E14 target word is
`111111`.  Multiplication may match polynomial degrees, but cannot change
these source labels or convert (p/s) factors into (q)-cells.

## The two missing colour changes form a Segre square

On the fixed matching (23\mid45), write

\[
\begin{array}{ll}
 q_{00}=a_{23}^{11}a_{45}^{11},&
 q_{10}=a_{23}^{21}a_{45}^{11},\\
 q_{01}=a_{23}^{11}a_{45}^{12},&
 q_{11}=a_{23}^{21}a_{45}^{12}.
\end{array}                                             \tag{3}
\]

The four monomials are linearly independent coefficient coordinates.  They
satisfy only the multiplicative square identity

\[
 q_{00}q_{11}=q_{10}q_{01}.                            \tag{4}
\]

The four one-site colour-bar differences have rank three, and
(q_{11}-q_{00}) is their two-edge path.  But either path passes through
(q_{10}) or (q_{01}), a distinct decorated word.  Thus (4) is not the
linear, same-word source attachment required by either proof.  This is the
precise point at which the apparent relabelling stops.

## Smallest sufficient shared attachment

Use feature order

\[
                 (E_+,E_-,\Omega_1,q_{1,N}).
\]

The already certified rows contain

\[
 S=(1,1,0,0),\qquad B=(0,0,-1,1),                      \tag{5}
\]

where (S) is the E14 signless endpoint row and (B) is the rootless
colour-bar boundary.  The smallest common filler would be one **literal
same-source-word** row

\[
 A=(1,-1,1,-1).                                        \tag{6}
\]

It must have zero target, old-ores, anchor-incidence, and (W) readouts.
Then

\[
 D=A+B=(1,-1,0,0),\qquad
 E_+={S+D\over2},\quad E_-={S-D\over2}.                \tag{7}
\]

Hence over characteristic zero (equivalently after inverting (2)), (6)
both cancels the rootless ridge/companion pair and splits the E14
endpoint-orientation cycle.  Integrally, the endpoint lattice generated by
(S,D) has determinant (-2), making the only denominator explicit.

## Exact boundary

No such (A) is present in the two audited inventories.  The common
uncoloured tail is real, but the source-valid bridge must simultaneously

1. change endpoint orientation;
2. change the two residual decorations `11|11` to `21|12`;
3. contribute (+\Omega_1-q_{1,N}); and
4. retain a single literal fine/source word with protected readouts zero.

So the convergence identifies one precise new mixed endpoint--two-colour
attachment; it does not allow the old rows to be identified by relabelling
or multiplication.  This is an exact interface theorem/no-existing-
relabeling result, not an all-source no-go.

Verified by
`computations/verify_h3_shared_same_word_endpoint_companion_attachment_gate.py`.

Frozen ledger SHA-256:

```text
809408d026ad9d29966d816002bd8984819972875c37ece5298081756bfd38c7
```
