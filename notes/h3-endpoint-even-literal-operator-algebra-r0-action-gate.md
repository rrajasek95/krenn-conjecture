# The augmented target action is tautological; the sole primal lift is Eq/P2

## Result

The endpoint-even cap packet does support all objectwise pieces of the
smallest divided-Hasse/trigger algebra, but the current physical constructors
do not extend that action across the response-to-cap comparison.  The first
failure is a single transported operation/Eq class.  The two mixed-target
normals previously visible as a rank-two cokernel are **not** two further
annihilator conditions: on the full augmented pair `(source equation,target
tensor)` they are the forced action on the normalized target of `r0` and are
already totalized by the even Cartan--Spencer cone.

Consequently the exact obstruction ladder is

```text
source-only quotient:       [one Eq class, two target normals]
augmented A-module pair:    [one Eq class, zero target classes].
```

There is still no source-valid module action in the current constructors.
If the missing occurrence-local Eq/P2 placement is formally granted, its
first divided-Hasse Leibniz relation exports the already typed, nonzero
`dq23:21` reinsertion conormal.  Its primitive detector has value `35/72`.

Checker:

```text
computations/verify_h3_endpoint_even_literal_operator_algebra_r0_action_gate.py
```

## 1. The smallest face-closed operator algebra

Let `A_min^even` be the dg operator category generated by the literal pieces
needed by the fixed `M/N/q01` face:

- the endpoint Reynolds idempotent `P+=(1+s)/2`;
- the restrictions `D_q01,D_q23,D_q45` and reinsertions `I_q23,I_q45`;
- the two site-root principal-parts actions
  `E_10^(site0),E_01^(site4)`;
- their first divided-Hasse insertion faces `dq23,dq45`;
- the even target actions `Jstar_23,Jstar_45` and their root-decorated
  `K_Eq` Spencer faces; and
- the private `q01` Taylor-to-Spencer cylinder already present on the
  response side.

The tested relations are

\[
 P_+^2=P_+,qquad sP_+=P_+s=P_+,
\]

the commuting two-root PP square, the literal restriction/insertion
Beck--Chevalley faces, and

\[
             dI_q-I_qd=I_{dq}.                         \tag{1}
\]

The endpoint matrix

\[
 P_+={1\over2}\begin{pmatrix}1&1\\1&1\end{pmatrix}
\]

satisfies these projector identities exactly.  The two site roots give the
literal square boundary

```text
A0 + B1 - A1 - B0,
```

whose differential is zero.  Thus `A_min^even` is not a hand-added mixed
arrow: every generator above is a pinned source/target operation.

The proposed comparison `Phi(e_R)=r0` is deliberately **not** a generator
of `A_min^even`.  It must be the cyclic `A_min^even`-linear morphism whose
existence is tested.

## 2. The exact `M/N/q01` module relation

Fix

\[
\begin{aligned}
 M&=01\,23\,45\,67,\\
 N&=07\,12\,34\,56,\\
 K&=07\,23\,45\,67.
\end{aligned}
\]

The selected `q=01` Taylor face is physical on the response side.  Its two
kept restrictions are `q23` and `q45`; coefficientwise they are precisely

```text
0112/q23:21 -> B1,
0121/q45:12 -> B4.
```

The common `V=Q^90` augmentation supplies the top and the undecorated proper
faces.  It does not supply these word/fine/repeated/operation-labelled
landings.

For a cyclic module map the first chain equation is

\[
                  d_C\Phi(e_R)=\Phi(d_Re_R).           \tag{2}
\]

In row order `((H-u)_response,(H-u)_Eq,cap)`, its two sides are

\[
                         (0,1),\qquad(1,0).             \tag{3}
\]

They have rank two.  Their difference `(-1,1)` is detected with normalized
value one by

\[
                   {1\over2}(-\delta_R+\delta_{Eq}).   \tag{4}
\]

Before transport there are 24 root-by-label mixed-square classes.  The
strongest root/label naturality relations have rank 23, leaving exactly the
one class (4).  The physical inventory has

```text
Hom^0(response,cap)=Hom^1(response,cap)=0.
```

The simultaneous `D4/P2/K_Eq/d_even` transfer has rank 24 and determinant
64.  It closes all protected rows once (2) is supplied, but common `V` alone
leaves the exact `(lower,ores)=(-E,+E)` debt.  Thus the nonsingular solve is
a uniqueness/compatibility theorem, not an existence theorem.

## 3. Why the target normals are actions, not relations

For either lower cut `c=23,45`, let `N_c` be the primitive mixed-target
normal of the natural endpoint action.  Retain the two augmented rows
`(target,reduced Eq)`.  The literal lower action, normalized even Cartan
action, and root-decorated Spencer face are

\[
\begin{array}{c|cc}
 &target&Eq\\ \hline
B_c&N_c&0\\
J_{*,c}&-N_c&-N_c\\
K_{Eq,c}&0&N_c.
\end{array}                                             \tag{5}
\]

Their sum is zero.  Every proper pair in (5) has rank two, so all three
columns are necessary and the cone is minimal.  Across the two cuts the six
columns have rank four and kernel dimension two; the physical cut symmetry
exchanges the two kernel relations.

Equation (5) changes the interpretation of the old pairing

\[
 \begin{pmatrix}2&0\\0&2\end{pmatrix}.
\]

That matrix correctly proves that the lower target actions are independent
modulo local diagonal target rows.  It does not prove that they should
vanish on `r0`.  On the augmented module, they are the forced values of
`B_c` on `target(r0)=1`; `J_*` and `K_Eq` provide their module-relation
faces.  The exact residual is

```text
mixed target       0,
root reduced Eq    0.
```

No additional mixed-target generator is required.

The source-labelled remainder is instead

```text
complete Eq          -delta_plus,
labelled residue     (B1+B4)/2,
word objects         0112/q23:21 and 0121/q45:12,
root-word debt       lower/private +E, word-ores -E.
```

These are the boundary data of the one missing Eq/P2 action orbit, not four
independent obstructions.

## 4. First literal Leibniz export

The ambient complete source already has the two-root PP square.  What it
does not have is the endpoint-even occurrence-local section which places
the Eq/P2 action in the selected lower object.  The complete response row
contains only the sum of its twelve occurrence coordinates.

Suppose this placement is granted.  Reinsertion by the physical factor
`q23:21` must obey (1), namely

\[
              d(q_{23}S)=q_{23}\,dS+(dq_{23})S.        \tag{6}
\]

For the exact endpoint-even private `B-4` preimage, the second term of (6)
has twelve occurrence coefficients, augmentation zero, and

\[
 (e_0^*+e_3^*-e_1^*-e_6^*)((dq_{23})S)={35\over72}.   \tag{7}
\]

Its scalar ordinary-residue value is zero, so it cannot be discarded by an
aggregate residue equation.  It is already typed as

```text
word                 0102,
parent cut           0112/q23:21,
operation face       dq23:21 reinsertion conormal,
endpoint parity      even.
```

The second cut is its sigma transport.  Hence a bare Eq landing does not
define an `A_min^even`-module: after the landing, (6) is the first exact
Leibniz relation which exports a nonzero missing physical row.

## Shortest positive theorem

It is enough to construct one endpoint-even, sigma-covariant Eq/P2 action
orbit on `r0` carrying all of the following as one source-labelled packet:

1. the two word placements `0112/q23` and `0121/q45`;
2. complete `Eq=-delta_plus` and labelled residue `(B1+B4)/2`;
3. the hidden root-word `(-E,+E)` lower/residue faces needed by the
   simultaneous totalization; and
4. the divided-Hasse `dq23/dq45` conormals forced by (6).

The target columns in (5) are already canonical and need not be postulated
again.  Once this orbit exists, cyclicity determines `Phi(e_R)=r0`, and the
pinned rank-24/determinant-64 solve fixes its normalization.

## Scope and verification

The calculation is exact over `Q` for the fixed `M/N/q01` face, the
endpoint-even six-parent quotient, all root/label transports included in the
pinned first-face theorem, both target cuts, and the representative typed
`0102/q23` Leibniz face with its sigma mate.  It does not construct the
Eq/P2 action or prove that the displayed relations exhaust the full global
annihilator.

Run:

```text
python3 computations/verify_h3_endpoint_even_literal_operator_algebra_r0_action_gate.py
python3 -O computations/verify_h3_endpoint_even_literal_operator_algebra_r0_action_gate.py
python3 -I -S computations/verify_h3_endpoint_even_literal_operator_algebra_r0_action_gate.py
```

Frozen ledger SHA-256:

```text
31b3cd278f8a0fcb50fd68d24d43dd9f80853d0c72b3dd9bb4c2490d7e96dc53
```
