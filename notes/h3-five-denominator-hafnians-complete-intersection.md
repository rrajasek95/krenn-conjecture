# The five denominator hafnians form a complete intersection

Research reduction only.  This does not construct the physical cap lift or
prove Krenn's conjecture.

## Exact result

Write `x_ij` for the mixed-colour internal edge selected by the word
`12112`.  The five reset defects are the complementary four-site hafnians

\[
 h_v=\operatorname {Haf}(x|_{\{1,\ldots,5\}\setminus\{v\}}),
 \qquad v=1,\ldots,5.
\]

In the ten-variable polynomial ring

\[
 R=\mathbb Q[x_{12},x_{13},x_{14},x_{15},x_{23},x_{24},x_{25},
              x_{34},x_{35},x_{45}],
\]

the ideal `I=(h_1,...,h_5)` has height five.  Since `R` is regular and `I`
has five generators, the `h_v` form a regular sequence.  Their minimal free
resolution is the Koszul complex, with Betti numbers

```text
1, 5, 10, 10, 5, 1.
```

In particular there is no hidden lower denominator-only syzygy.  Every
first syzygy is generated by

\[
                     h_i e_j-h_j e_i.
\]

No polynomial combination with unit constant face coefficients can turn
the five labelled boundaries `h_v Y_0` into one primitive aggregate.

## Proof certificate

The dependency-free checker computes a degree-reverse-lexicographic
Groebner basis by Buchberger's algorithm and independently reduces every
S-pair to zero.  It then computes the height of the initial monomial ideal:
for a monomial ideal, height is the minimum size of a set of variables
meeting every leading monomial.  Exhausting the `2^10` subsets gives height
five.  Krull's height theorem supplies the matching upper bound because
there are five generators.

This is stronger than linear independence of the five quadrics.  It proves
that the entire universal denominator algebra has no earlier syzygy which
could replace the physical reset construction.

## Consequence for the proof frontier

The prolonged Hasse presentation already provides derived fillers for the
five faces.  The present theorem shows that their descent cannot be repaired
inside the polynomial algebra of the `h_v` alone.  The primitive cap must
therefore come from one of the two mechanisms already isolated in the proof
map:

1. a genuinely physical endpoint/full-source comparison cell descending
   the common conormal `(H_0-u)e_Eq` with all augmented readouts; or
2. a non-flat full-source Tor class whose face transgression has nonzero
   aggregate.

Thus searching for additional Bianchi identities among the five quadrics is
finished.  The next constructive target is the pointed augmented comparison,
not a larger denominator-only ansatz.

## Verification

```text
python3 computations/verify_h3_five_denominator_hafnians_complete_intersection.py
python3 -O computations/verify_h3_five_denominator_hafnians_complete_intersection.py
python3 -I -S computations/verify_h3_five_denominator_hafnians_complete_intersection.py
```

Frozen ledger SHA-256:

```text
55be0632a8070738e1e8a90dd6746cfb910bae94493dbf1363994975458ca2bb
```
