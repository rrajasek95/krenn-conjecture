# The endpoint-odd Cartan prism descends through the physical source orbit

## The missing functoriality

Let `A` be the affine space of decorated physical edge coefficients and let

\[
                    F(A)=H_B(A)
\]

be the complete perfect-matching tensor.  A local colour change at a site
acts both on the incident edge coordinates and on the corresponding output
tensor factor.  Because every perfect matching contains exactly one edge at
that site, the two vector fields are `F`-related.  On coefficient functions,

\[
                 X_{\rm src}F_w=F_{X_{\rm out}w}.       \tag{1}
\]

The checker verifies (1) term by term on every complete direct-free ternary
row for both root directions at tail sites `2` and `5`: `8,748` complete
rows and `787,320` physical matching terms.

Equation (1) is exactly what was missing from the universal Cartan theorem.
Pullback along an equivariant polynomial map commutes with exterior
differentiation and contraction by related vector fields:

\[
 F^*\iota_{X_{\rm out}}=\iota_{X_{\rm src}}F^* .       \tag{2}
\]

Functions and their differentials generate the principal-parts de Rham
algebra, so (1)--(2), together with the Hasse product rule, put every term of
the finite root homotopies inside the **complete physical principal-parts
source resolution**.  No ordinary matching column is being invented; the
new object is a genuine relative source cell generated by physical
coefficient-space vector fields.

## Why this is not a formal target mapping cylinder

The endpoint involution is the actual residual-site permutation `0 <-> 1`.
It preserves the forbidden direct edge and transports all `90` monomials of
every one of the `3^8` complete rows literally.  It commutes with the tail
colour action at sites `2,5`.

The tail Weyl action does not preserve the GHZ target separately.  However,
its target defect is invariant under `0 <-> 1`, since the two endpoint sites
have the same colour in every monochromatic target term.  Therefore

\[
            (1-s)(w-1)\Delta=0.                         \tag{3}
\]

This cancellation occurs between two actual coefficient-space orbit paths.
It does not adjoin a new target vertex and then declare the desired boundary.
Consequently the universal homotopy

\[
 K=(1-s)H_w,\qquad dK+Kd=(1-s)(w-1)                   \tag{4}
\]

is a source-provenant relative chain for the physical fibre.

## Augmented output

The pinned order-six theorems now apply to (4) without a remaining descent
hypothesis:

```text
ordinary residue             = (-1,+1,+1,-1) = -delta,
D,W,target,anchor,pure-Eq     = 0,
physical word/module degree  = canonical faces-(3,5) repeated grade,
terminal ridge               = commuting -dOmega_v eta/sigma packet.
```

The five cyclic translates of (4) span the saturated rank-four face-edge
lattice.  Only the primitive aggregate remains.  On the complete exhaustive
relative source complex the physical six-term readout gives the exact
alternative:

1. it is nonzero on the protected kernel, producing the normalized relative
   anchor; or
2. it factors through the protected map and gives the complete physical
   left separator.

Thus the local augmented interchange theorem is closed in this canonical
grade.  A fourth or higher relative-cell census cannot create a third branch.

## Remaining scope

This does **not** prove the conjecture by itself.  The constructive route
still needs source-typed transverse landing (or support deletion), and the
global clean-point proof still needs uniform entry or completion of the
inactive dual routing.  What is removed is the first hard local arrow:
physical Cartan descent is now a consequence of perfect-matching
equivariance and principal-parts functoriality.

Verification:

```text
python3 computations/verify_h3_physical_cartan_source_orbit_descent.py
python3 -O computations/verify_h3_physical_cartan_source_orbit_descent.py
python3 -I -S computations/verify_h3_physical_cartan_source_orbit_descent.py
```

Frozen ledger SHA-256:

```text
814cb57088a6c1effce5f4c0ce3b78b9fa0ab89b3a5b5e3d44b7436f87616c69
```
