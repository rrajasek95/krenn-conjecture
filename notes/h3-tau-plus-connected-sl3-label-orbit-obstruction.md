# Connected `SL3` supplies the root decoration, not the missing even placement

## Verdict

The last `tau_plus` orbit cannot be constructed solely by replacing the
formal product-rule cell with an explicit connected two-root `SL3`
unipotent/Weyl homotopy.

There is a genuine positive theorem here.  Every local colour root field on
coefficient space is related to the corresponding output root field for the
complete perfect-matching tensor.  The signed Weyl element is a product of
root unipotents, so its Cartan homotopy is source-provenant and can create the
required mixed root-word decoration of a **correctly placed** source column.

The obstruction is placement.  Local `SL3` changes colours but never changes
an underlying site edge, multiplier graph, matching label, or repeated-edge
label.  On the six canonical complete columns its `B`-label action is the
identity.  Adding the physical involution `rho` gives only

```text
(B0 B5)(B2 B3), with B1 and B4 fixed.
```

Therefore the actual omitted-`25` product-rule bypass remains in

\[
                       O=\langle B_0,B_2,B_3,B_5\rangle,        \tag{1}
\]

while the required even landing is

\[
                       v={B_1+B_4\over2}.                       \tag{2}
\]

The primitive label functional

\[
                       \lambda_F=(0,1,0,0,1,0)                 \tag{3}
\]

kills the entire connected orbit (1) and reads one on (2).

Checker:
[`verify_h3_tau_plus_connected_sl3_label_orbit_obstruction.py`](../computations/verify_h3_tau_plus_connected_sl3_label_orbit_obstruction.py).

## Literal complete-row audit

The checker reconstructs the six pure columns in the canonical
faces-`(3,5)` component.  Each has a distinct uncoloured multiplier graph
and a complete 90-feature boundary.  It then applies all six off-diagonal
`sl3` root directions at both selected root sites to all 540 features:

```text
six B columns
  x 90 boundary features
  x two root sites
  x six ordered colour-root directions
  = 6480 exact feature/direction checks.
```

Every nonzero derivative term has exactly the original uncoloured site
multigraph.  This is structural: an infinitesimal root action replaces a
colour decoration on an incident coefficient variable and leaves its two
site endpoints unchanged.  Iterating and exponentiating the root actions,
and hence forming the Weyl product and Cartan prism, cannot leave that
multiplier-label block.

The result is stronger than a failure of one chosen Weyl word.  The full
connected local `SL3` generated by all root subgroups has the same
block-diagonal label action.

## Starting from the fixed product-rule occurrence does not help

The divided product rule has a formal fixed occurrence whose two oriented
resolutions are `B1` and `B4`.  Its source label is

```text
(matching 4, repeated edge 02),
```

and it is fixed by `rho`.  The actual `tau_plus` omitted source orbit is

```text
(matching 2,  repeated edge 01)
(matching 10, repeated edge 04),
```

with the two labels exchanged by `rho`.

Local `SL3` does not act on either the matching index or repeated edge.
Thus starting from the formal `B1/B4` occurrence stays in the shared-`02`
grade; starting from the actual omitted pair stays in (1).  The connected
group can solve the colour/root part on either side, but it cannot provide
the source-valid repeated-grade transport between them.

## Consequences for the full interface

Relative to the local bypass,

\[
 \delta_+=v-{B_0+B_2+B_3+B_5\over4}.
\]

The functional (3) reads one on `delta_plus`.  Tensoring it with one nonzero
root coordinate gives exact values `2` and `-2` on the required packets

```text
mixed target       -2 D tensor v,
reduced-Eq face    +2 D(H0-u)Eq tensor v,
```

where `D=(-1,1,-1,1)`.  It also reads one on the labelled ordinary-residue
vector `v`.  Hence connected root decoration does not separately solve the
Eq or residue projections: all retain the same missing `B1/B4` placement.

There is an additional parity caution for the requested `W=0`.  The known
protected-readout theorem uses the endpoint-odd operator `1-rho`; it kills
endpoint-even rows.  The present root-even operator is `1+rho`, which doubles
rather than kills a generic endpoint-even row.  Thus `W=0` is not a formal
consequence of connectedness or even parity.  It must be carried by the
same totalized product-rule/Bianchi cell (or capped by an additional
physical face).

## Shifted frontier

The connected-group test removes one possible shortcut but also clarifies
what is already free:

```text
correct B1/B4 source placement
        |
        +-- connected SL3/Weyl Cartan --> mixed root-word decoration
        |
        `-- still required ------------> repeated-grade transport,
                                         full-nine/reduced-Eq comparison,
                                         labelled ores and W=0
```

So the smallest missing object remains the one rho-even nondegenerate
relative product-rule/Bianchi orbit isolated in `a872264`.  It is a source
placement/comparison cell, not a new abstract group homotopy.

The `beta=0` selected `D0` membership `1 in theta(Z)` remains independent.

## Verification

Run:

```text
python3 computations/verify_h3_tau_plus_connected_sl3_label_orbit_obstruction.py
python3 -O computations/verify_h3_tau_plus_connected_sl3_label_orbit_obstruction.py
python3 -I -S computations/verify_h3_tau_plus_connected_sl3_label_orbit_obstruction.py
```

The frozen ledger SHA-256 is `ad34db2ac949510f41d877c24679e532c9b2db28f31ce35cd32ec1b7283517a4`.
