# Rootless Component III: the complete typed inventory still has one primitive attaching class

## Outcome

At \(h=3\), combine all of the proof-facing data named in the rootless
Component III route:

* the exact terminal class of cd52b2b/cdc746d;
* the genuine companion-corrected full-nine row of 9dac232;
* both differently labelled diagonal anchors and the crossed zero row;
* all nine starting rows followed by every literal Hamming-one and
  Hamming-two response tag;
* shared-star Segre exchanges and the static two-chart transport; and
* both formal target/ordinary-residue-cancelled chart candidates, together
  with their Bianchi comparison.

Even after granting the last two chart candidates (which is stronger than
the underived physical source module), the joined presentation has rank
\(9\) in dimension \(10\).  The missing row \(K=0\), equivalently the
response-grade \(Q_3\) attaching row, raises the rank to \(10\).  At
selected direct scalar \(\alpha\ne0\), a square minor after adjoining it is

\[
                         -48\alpha .                    \tag{1}
\]

Thus the complete typed inventory does **not** construct the rootless
Macaulay annihilator or an active clean cap.  Its earliest exact obstruction
is still one primitive source-relative attaching class.  The minimal new
physical comparison must have total pure-anchor incidence \(-1\) while its
normalized cap boundary, target, and ordinary residue are all zero, and it
must change the pure output-word/endpoint grade into the selected midpoint
response grade.  No named row in the inventory has that type.

This is an obstruction theorem for the complete listed inventory, not a
theorem that no larger source resolution exists.

## 1. Literal fine degree leaves only the selected response jet

Give \(p_i,s_j,d_{ij}\) their separate left/right endpoint-label degrees.
For zero, one, or two response tags, reaching the selected degrees

\[
 (e_a,e_b),\qquad2(e_a,e_b),\qquad3(e_a,e_b)             \tag{2}
\]

from all nine possible starting rows has exactly one route at each level:
every starting label and every tag is \((a,b)\).  This is the nonnegative
degree argument of
[the Hamming-two audit](h3-two-chart-h2-tagged-reinsertion-cokernel.md),
now replayed for all three levels.  Each of the \(81\) shared-star Segre
switches preserves the two degree vectors separately.

Consequently the complete literal through-Hamming-two block in the selected
grade is exactly

\[
 J_{\le2}=\begin{pmatrix}
 \alpha&1&0&0\\
 0&\alpha&2&0\\
 0&0&\alpha&3
 \end{pmatrix}                                         \tag{3}
\]

on \((Q_0,Q_1,Q_2,Q_3)\).  Its terminal vector is

\[
 k_\alpha=(-6,6\alpha,-3\alpha^2,\alpha^3).             \tag{4}
\]

The clean row \(c=(0,0,\alpha,1)\) reads
\(c(k_\alpha)=-2\alpha^3\).  Hence the Hamming-two block has rank \(3/4\),
and cleanliness raises it to \(4/4\).

The certified static two-chart block containing the two labelled diagonal
anchors and crossed zero row has determinant \(-3\).  It occupies another
endpoint fine degree, so its direct sum with (3) remains rank \(7/8\);
adjoining cleanliness has determinant \(6\alpha^3\).  This is why merely
retaining both anchors does not touch the terminal line.

## 2. Complete source-word rows do not supply the missing grade change

There are \(3^8=6561\) complete source-word companions.  Cancellation by
the full-nine rows is the identity on these labels.  The \(6558\) mixed
words are target-zero; the other three are the independently labelled pure
anchors.  Therefore a target-zero companion cancellation has zero
coefficient at each pure word separately.

The twenty binary midpoint rows used by the attaching class are all mixed.
They kill the literal aggregate \(S\), but source-word cancellation cannot
move a pure anchor coefficient into that aggregate.  A reduced or cyclic
word change does not help: as proved in
[the reduced ternary bar audit](h3-reduced-ternary-bar-companion-cokernel.md),
retaining a pure companion uses its same-labelled anchor and leaves its
target; cancelling that target recreates the ordinary \(E_q/w\) defect.

Thus the full Hamming-one/two and full source-word inventories split for a
load-bearing reason: the first preserves endpoint fine degree, while the
second preserves the complete output-word label.  A new attaching map must
change both in a source-controlled way.

## 3. The joined attaching/conormal presentation

After the Hamming-two quotient use coordinates

\[
 (T,S,K),\qquad T=Q_3,                                  \tag{5}
\]

where \(S\) is the literal twenty-row midpoint sum and \(K\) is the genuine
companion-corrected class.  The committed relations are

\[
 S=0,\qquad K-16\alpha T-S=0 .                          \tag{6}
\]

These are the exact relations of cd52b2b/8e1075f, with the scope
correction of 9dac232: \(K\), not the separately internal defect, is the
row which must vanish.

Now grant both complete formal chart candidates after their target and
ordinary residue have already been cancelled.  In coordinates

\[
 ([F_D],[F_L],\widehat w)
\]

they and their Bianchi comparison are

\[
 N_D=(1,0,1),\qquad N_L=(0,1,1),\qquad E=(-1,1,0).     \tag{7}
\]

Their rank is two, and the primitive covector

\[
                         (1,1,-1)                      \tag{8}
\]

kills all three but reads \(-1\) on the desired invisible boundary
\((0,0,1)\).  This is the exact conormal obstruction of 04abf04, now with
both chart labels retained.

Form the pushout identifying the committed connecting boundary \(K\) with
the normalized \(\widehat w\) class.  Add the full-rank static block.  On

\[
 (\text{static}_4;T,S,K,[F_D],[F_L],\widehat w)         \tag{9}
\]

the complete available presentation is killed by the primitive covector

\[
 (0,0,0,0;\ 1,0,16\alpha,-16\alpha,-16\alpha,16\alpha).
                                                               \tag{10}
\]

It reads \(16\alpha\ne0\) on \(K=0\).  The available rank is \(9\), the
rank with \(K=0\) is \(10\), and (1) is the determinant of an independent
closed presentation.  Equation (10) is the earliest exact obstruction in
the complete typed inventory: response terminal, normalized cap boundary,
and total pure-anchor incidence are the same unresolved class.

## 4. Exact next physical comparison and stopping rule

To turn, say, \(N_D\) into the desired conormal-free boundary requires a
new lower face of type

\[
 \begin{array}{c|rrrr}
 &\text{total pure-anchor incidence}&\widehat w&\mathrm{tgt}&\mathrm{ores}\\
 \hline
 C_{\rm rel}&-1&0&0&0.
 \end{array}                                            \tag{11}
\]

It must simultaneously transport a pure labelled companion into the
selected three-response/midpoint grade.  Ordinary Hamming rows, Segre
switches, wordwise companion cancellation, anchor contrasts, and Bianchi
chart differences all miss at least one part of (11).

There are only two proof-valid ways forward:

1. construct (11) as genuinely new source-resolution data; or
2. prove a packet-conditioned ordinary source unit.  By the monic-anchor
   theorem, an underived polynomial cancellation of the pure conormal is
   already such a unit.  If the resulting scalar-zero pure-target response
   is concentrated on one physical edge, the intrinsic single-edge cap
   theorem (a67ec1d) immediately gives an ordinary unit or an
   inactive-clean cap.

Absent one of these, more Hamming-one/two, selector, Ward, Segre, or static
anchor manipulations cannot alter (10).

## Verification

Run

    python3 computations/verify_h3_rootless_component_iii_complete_typed_inventory.py
    python3 -O computations/verify_h3_rootless_component_iii_complete_typed_inventory.py

The checker pins the seven terminal/attaching artifacts named in the task
and the load-bearing Hamming, word-companion, conormal, monic-anchor, and
single-edge-cap audits.  It enumerates every endpoint-labelled route through
Hamming degree two, every Segre degree switch, all \(6561\) word labels and
the twenty midpoint words, and verifies the exact ranks, covector, and
determinant of the joined presentation.
