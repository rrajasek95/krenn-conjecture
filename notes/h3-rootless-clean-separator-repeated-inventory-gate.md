# The clean separator first fails at PP typing, then at a physical stabilizer

Research boundary only. This does not construct the common-companion lift,
the physical terminal annihilator, or a new higher source generator.

## Exact first failure

Use the augmented row order

\[
 (\Omega,Q,r,\mathrm{Eq},W,\mathrm{tgt},\mathrm{ores},
      \mathrm{ainc},\mathrm{chart})
\]

and the proposed clean covector

\[
 \Lambda=(1,1,1,0,0,0,0,0,0).                       \tag{1}
\]

The complete source-labelled endpoint route is

\[
                         B=(-\Omega,+Q;\mathrm{ores}=1), \tag{2}
\]

so \(\Lambda(B)=0\). Matching switches, endpoint Bianchi squares, and the
complete response companions do not change this.

The first source-valid repeated-site PP degree has two oriented physical
routes. In the committed coarse augmented map they are

\[
 C_v=(-r_v;\mathrm{ores}=1),\qquad
 -C_w=(+r_w;\mathrm{ores}=-1).                        \tag{3}
\]

Therefore

\[
                         \Lambda(C_v)=-1,\qquad
                         \Lambda(-C_w)=+1.             \tag{4}
\]

Their zero-residue S-pair is \(-r_v+r_w\), which \(\Lambda\) kills. Thus
the aggregate separator is valid on the completed correction-cycle
quotient, but it is not yet a covector on the complete coarse generator map.

This is not repaired by adding ordinary residue to the covector. Keeping
unit values on \(\Omega,Q,r\), equation (2) forces the ordinary-residue
weight to be zero, while the first column in (3) forces it to be one. The
physical pure-residue column also forces it to be zero.

## The exact missing source lift

The PP calculation factors out its common matching companion and remembers
only the scalar residue in (3). The required refinement is

\[
 \widetilde C_v=(-r_v,+Q_{v,N};\mathrm{ores}=1),       \tag{5}
\]

with the same labelled \(Q_{v,N}\), endpoint word, chart sector, and
repeated \(P_3\sqcup K_2\) fine degree as (2). Then

\[
 \Lambda(\widetilde C_v)=0,\qquad
 B-\widetilde C_v=-\Omega_v+r_v.                      \tag{6}
\]

All four readouts \(W,\mathrm{tgt},\mathrm{ores},\mathrm{ainc}\) vanish in
the difference. Consequently the missing \(\Omega_v\)-to-\(r_v\)
comparison does not require a second independent formula once (5) is
source-valid: it is the difference of the endpoint route and the lifted PP
route.

This sharpens the first typing datum. One must prove that the private
ordinary-residue companion of the individual PP route is literally the
same all-derivation matching coordinate \(Q_{v,N}\), including the
three external cap-shift slots and both chart labels. The current collision
checker proves only the factored coarse column (3) and the adjacent
zero-residue pair; it does not construct this common-companion lift.

## Same-labelled \(Q\) does not repair physical zero indeterminacy

Even granting (5), the proposed separator fails on an earlier physical
source-kernel family.  On the marked open

\[
                          t=q_{pq}^{00}\ne0,
\]

the five colour-diagonal target-stabilizer tangents \(\eta_z\) satisfy

\[
 J\eta_z=0,\qquad \eta_z(Q_{v,N})=0,
 \qquad \eta_z(r_v)=0                                      \tag{7}
\]

for every currently defined companion and rootless-ridge readout.  Their
endpoint readout is

\[
 d\Omega_v(\eta_z)=
 \begin{cases}
 -1,&v\ne z,\\
 -1-u_z/t,&v=z,
 \end{cases}
 \qquad u_z=q_{xz}^{00}.                                  \tag{8}
\]

Hence

\[
                         \Lambda(\eta_z)=-5-u_z/t.         \tag{9}
\]

This is not cancelled by inserting the same-labelled \(Q_{v,N}\) in the
PP route: all those \(Q\)-coordinates have value zero on \(\eta_z\).

There is also no less symmetric correction using different weights on the
five faces or their three matching companions.  Write

\[
 A_v=\Lambda(\Omega_v),\quad
 B_{v,N}=\Lambda(Q_{v,N}),\quad
 C_v=\Lambda(r_v),\quad d=\Lambda(\mathrm{ores}).
\]

The pure-residue column gives \(d=0\).  The endpoint and lifted PP routes
give

\[
                         B_{v,N}=A_v=C_v,                 \tag{10}
\]

and the five clean collision edges make all \(C_v\), hence all \(A_v\),
equal to one scalar \(A\).  Equation (8) then pairs as

\[
                         -A(5+u_z/t).                    \tag{11}
\]

Over the marked localized coefficient ring, the coefficient of \(u_z/t\)
forces \(A=0\).  The exact constraint matrix has 26 covector variables,
rank 25 before the five stabilizer families, and rank 26 after them.  Thus
the only same-labelled-\(Q\) refinement annihilating the audited physical
kernel is the zero covector.  On the exceptional locus
\(u_z=-5t\) for every \(z\), the old one-dimensional functional survives;
no committed source theorem forces that locus.

## Complete known repeated-site inventory

The audit includes:

* 30 homogenized endpoint routes and 30 matching-Bianchi differences;
* ten individual PP routes and their five clean zero-residue edges;
* all \(5\cdot288=1440\) one-chart full-nine multiplier columns;
* all 1440 pairwise two-chart comparison generators;
* the clean denominator-Tor kernel;
* normal and weighted-normal faces through order three;
* the old cap block; and
* the 239-dimensional natural degree-five Tate correction kernel.

Every completed target/residue-zero *correction cycle* in these families
has \(\Lambda=0\). No known normal, cap, chart, clean-Tor, or Tate cycle
breaks the separator.  This statement deliberately excludes physical
source-kernel columns: the \(\eta_z\) above are the first such failure.

The full-nine blocks also introduce no hidden dual obstruction. In each
repeated fine degree there are six distinguished endpoint-companion
\(Q\)-features at most. Every one of the 288 full-nine columns has at least
42 literal boundary features owned by no other column. Whatever weights
the \(Q\)-features receive, choose a private non-\(Q\) feature in each
column and give it the negative current pairing. These choices are
independent, integral, and kill all 288 columns. The two chart copies have
identical boundaries, so the same extension kills both and every chart
difference.

Thus the complete audited polynomial multiplier inventory can absorb the
dual once the source labels in (5) are available. It neither supplies nor
obstructs that lift.  Because \(J\eta_z=0\), the private output features
used for this extension vanish on \(\eta_z\); they cannot alter (9).

## Exact remaining interface

The first source-typing map is the source-valid companion synchronization

\[
 (-r_v;\mathrm{ores}=1)
       \longmapsto(-r_v,+Q_{v,N};\mathrm{ores}=1).     \tag{12}
\]

in one literal repeated endpoint/chart degree. It must retain the pure-zero
output word, the matching \(N\), and the external \(x,p,q\) cap-shift
typing. Constructing (12) gives (6), but does **not** produce a terminal
annihilator because of (9).

The minimal further datum is a physical \(\Omega\)-to-\(r\) comparison (or
equivalent terminal correction) whose rootless readout on every \(\eta_z\)
is \(5+u_z/t\), with target, ordinary residue, and anchor incidence zero.
Equivalently, the terminal \(r\)-readout must reproduce the endpoint
\(\Omega\)-readout on the target-stabilizer kernel.  This is precisely the
zero-indeterminacy/source-exhaustivity comparison; it cannot be replaced by
same-labelled \(Q\) bookkeeping alone.

No additional obstruction occurs in the complete known polynomial
multiplier/correction inventory, but the physical terminal annihilator is
not constructed.

Run:

    python3 computations/verify_h3_rootless_clean_separator_repeated_inventory_gate.py
    python3 -O computations/verify_h3_rootless_clean_separator_repeated_inventory_gate.py
    python3 -I -S computations/verify_h3_rootless_clean_separator_repeated_inventory_gate.py

Frozen ledger SHA-256:

    219badcc9c5e9b81d3230fc79011bb38607b8bf842f760c1a4624df9da755e5c
