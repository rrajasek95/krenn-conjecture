# The last even interface is one full orbit, not one bare tail

## Result

The generic `tau_plus` comparison has only one missing equivariant source
line.  Its two omitted labels are exchanged by `rho`, so their invariant
combination

\[
             e_+={e_\ell+e_{\rho\ell}\over2}
\]

has rank one.  The divided product rule has coefficient one on this line,
and the two fixed loop resolutions have exact average

\[
                       v={B_1+B_4\over2}.              \tag{1}
\]

Thus **one rho-even relative orbit is the minimum possible generic
extension**.  But it must be a full product-rule/Bianchi orbit, not a cell
specified only by (1).  In the actual omitted-`25` repeated grade, its full
interface type is forced simultaneously by four projections:

1. complete full-nine landing, equivalently the same-grade transport

   \[
   \delta_+=v-{B_0+B_2+B_3+B_5\over4}
     ={(-1,2,-1,-1,2,-1)\over4};                     \tag{2}
   \]

2. normalized root-even mixed target `-2 D tensor v`, with
   `D=(-1,1,-1,1)`;
3. the compensating reduced-Eq face
   `+2 D (H0-u) Eq tensor v`; and
4. same-grade labelled ordinary residue `v`.

The formal third-cofactor totalization supplies the useful anchor incidence
`ainc=-1`, so the signs are compatible with the desired pure-`r0` repair.
It is not a physical construction: its source-descent operator sends the
source equation to one, its endpoint ridge space has rank six with a
rank-five `Omega` obstruction, and it hits no selected midpoint source word.

Checker:
[`verify_h3_tau_plus_full_interface_product_bianchi_extension_gate.py`](../computations/verify_h3_tau_plus_full_interface_product_bianchi_extension_gate.py).

## Why all four projections are load-bearing

The complete-column obstruction is literal.  Put

\[
                   D_6=(-1,2,-1,-1,2,-1).
\]

Every available pure full-nine or projected `M_v` column ties its selected
private pivot to the same labelled Eq coefficient.  The covector

\[
          \chi_{\rm full}=\sum_i(D_6)_i
                   (\operatorname{private}_i-\operatorname{Eq}_i)
\]

kills that whole tied subspace and reads `12` on `(D6,0)`.  Therefore the
bare endpoint matching-Bianchi factorization of (2) is not a complete source
boundary.

The residue projection is independent.  In the six-label residue quotient,

\[
 \chi_{\rm res}=(0,1,-1,0,1,-1)
\]

kills the committed diagonal scalar residue and the pinned Cartan residue
line, but reads one on `v`.  This is deliberately only a projected guard:
the repository still has no termwise ordinary-residue definition on every
complete source column, so no bare or coarse residue grant is promoted here.

The old fourth-Hasse filler also does not silently provide the Eq face.  In
the reduced `(Eq,w)` plane it is `(1,1)`, while the required correction is
`(1,0)`; the primitive covector `(1,-1)` separates them.  Finally, a diagonal
cap row has zero mixed-word coordinate, whereas `D` does not.  These are four
different checks on one proposed source orbit, not four optional ways of
describing the same bare tail.

## Exact smallest physical extension

The smallest remaining positive object is:

> one rho-even, nondegenerate, source-valid relative product-rule/Bianchi
> orbit in the actual omitted-`25` word/fine/repeated grade, together with
> its forced endpoint-ridge and word-changing faces, whose complete boundary
> realizes (2), `-2D tensor v`, `+2D(H0-u)Eq tensor v`, and labelled residue
> `v`.

Adjoining a formal generator with those projections is rank-one and would
close the generic comparison by definition.  The open theorem is that its
boundary is the image of a physical source cell (or, dually, that one of the
displayed primitive functionals extends over the complete source complex).
The current product-rule coefficient proves only the occurrence shadow.

This sharpens the frontier: there is no need to search for several unrelated
generic repairs, but there is also no valid shortcut through a pure residue
section, a bare-Q Bianchi difference, or a projected fourth-Hasse face.

## `beta=0` remains independent

On the generic open set,

\[
                       J_*=-3\alpha\beta I.
\]

The normalized root-even cell uses `(alpha beta)^(-1)`.  Before
normalization every polynomial construction from `J_*` vanishes at
`beta=0`; after normalization it has no regular specialization there.  The
beta-zero cap rows span only the complementary `D2` line, while the missing
selected quotient is generated by `D0`.  The primitive functional `D0*`
kills `D2` and reads one on `D0`.

Consequently the even orbit above cannot be advertised as closing the
singular branch.  The independent exact condition remains

\[
                         1\in\theta(Z),                \tag{3}
\]

or the residue-field dual when (3) fails.

## Verification

Run:

```text
python3 computations/verify_h3_tau_plus_full_interface_product_bianchi_extension_gate.py
python3 -O computations/verify_h3_tau_plus_full_interface_product_bianchi_extension_gate.py
python3 -I -S computations/verify_h3_tau_plus_full_interface_product_bianchi_extension_gate.py
```

The frozen ledger SHA-256 is
`9488629927a079e70d1ac1cc629fca3453f172880b174e5b16e76cee2322bfbe`.
