# The degree-four reset reaches the common Eq cone, not the primitive cap by itself

## Result

The five quadratic denominator faces of the mixed odd word `12112` are

\[
\begin{aligned}
h_1&=x_{23}x_{45}+x_{24}x_{35}+x_{25}x_{34},\\
h_2&=x_{13}x_{45}+x_{14}x_{35}+x_{15}x_{34},\\
h_3&=x_{12}x_{45}+x_{14}x_{25}+x_{15}x_{24},\\
h_4&=x_{12}x_{35}+x_{13}x_{25}+x_{15}x_{23},\\
h_5&=x_{12}x_{34}+x_{13}x_{24}+x_{14}x_{23}.
\end{aligned}                                           \tag{1}
\]

There are two sharply different statements about their matching-switch
faces.

On the five-cycle specialization

\[
 (a,b,c,d,e)=(x_{12},x_{23},x_{34},x_{45},x_{15}),       \tag{2}
\]

only one monomial in each (h_v) survives.  In cyclic face order
`(1,3,5,2,4)` they are

\[
                         (bd,ad,ac,ce,be).                \tag{3}
\]

The five cubic switches then close and their projected face boundaries are
the oriented incidence columns of `C5`.  They have rank four, their integral
image is the saturated lattice `ker(sum:Z^5 -> Z)`, and their primitive dual
is `(1,1,1,1,1)`.

This is only a projected cap statement.  Before the cycle specialization,
the first switch is

\[
\begin{aligned}
x_{12}h_1-x_{23}h_3={}&
 x_{12}x_{24}x_{35}+x_{12}x_{25}x_{34}\\
&-x_{14}x_{23}x_{25}-x_{15}x_{23}x_{24},                \tag{4}
\end{aligned}
\]

and the other four cyclic switches have the same form.  Their twenty
monomials are pairwise disjoint across the five residuals.  Hence the full
residual map has rank five and five primitive private-coordinate duals.  No
nonzero combination of these five projected switches is a literal universal
source cycle.

Thus:

```text
cycle-specialized projected cap image: rank 4, dual all-ones;
full universal source residual:         rank 5, kernel zero.
```

Calling the first line a physical matching-switch nullhomotopy discards the
twenty full-row coordinates in the second line.

There is a separate positive physical statement which must not be confused
with those cubic PP switches.  In the twenty cap rows

\[
 \{\Omega_v\}_{v=1}^5\sqcup
 \{Q_{v,N}:N\in\operatorname{PM}(D\setminus\{v\})\},       \tag{4a}
\]

the fifteen already constructed routes

\[
                         b_{v,N}=-\Omega_v+Q_{v,N}          \tag{4b}
\]

have rank fifteen.  The independently committed endpoint-odd physical
Cartan orbit supplies the cyclic standard directions in the quotient by
(4b).  Route plus Cartan columns therefore have rank nineteen, with
primitive dual

\[
                         \epsilon=\sum_v\lambda_v,          \tag{4c}
\]

represented by coefficient one on every row in (4a).  Each quadratic reset
face has `epsilon`-value three, and `p=-Q_(1,N0)` has value minus one and
completes the rank to twenty.

Thus the physical cap quotient really does reduce the five reset faces to
one primitive aggregate.  What fails universally is the attempt to realize
that reduction by the five denominator-marked cubic PP switches alone.  The
source-provenant Cartan paths and the source-invalid specialized PP
expressions are different objects.

## 1. Denominator-only higher syzygies do not help

Commit `c18b531` proves that the five polynomials (1) form a height-five
complete intersection in the ten internal edge variables.  Their minimal
resolution is Koszul, with Betti numbers

```text
1, 5, 10, 10, 5, 1.
```

Consequently every denominator-only first syzygy is generated by

\[
                         h_i e_j-h_j e_i.                 \tag{5}
\]

All coefficients in (5) vanish with the (h_v).  No denominator-only higher
Bianchi/Koszul combination has a unit constant face coefficient, so none can
produce the primitive aggregate required by the cap lift.  The rank-four
cycle Tor is specialization-created; it is not a universal lower syzygy of
(1).

## 2. What the degree-four Hasse reset does construct

The complete external/internal four-cube gives a derived filler on each
face.  In the notation of the shifted chart totalization, it has

\[
 d n_v=h_vYw,\qquad
 \operatorname{tgt}(n_v)=\operatorname{ores}(n_v)=0.       \tag{6}
\]

All Boolean Hasse faces cancel in the derived presentation.  Projection to
the literal underived physical differential, however, leaves

\[
                 h_v(H_0-u)e_{\rm Eq}.                    \tag{7}
\]

The degree-four mixed/pure Koszul cell

\[
 K_m=H_mr_0-(H_0-u)r_m                                   \tag{8}
\]

is closed.  Adding an arbitrary polynomial multiple of (8) cannot change
(7).  Even after summing all five faces, a general correction `b r_m` would
require

\[
                  bH_m=-(H_0-u)\sum_vh_v.                 \tag{9}
\]

Specializing `H_m=H_0=0` turns (9) into

\[
                         0=u\sum_vh_v,                    \tag{10}
\]

whose right side has fifteen distinct nonzero monomials.  Thus the committed
Hasse reset is a positive **derived** filler, but it is not yet the physical
primitive cap cell.

## 3. Exact relation to the even/Bockstein program

The residual in (7) has the same scalar physical conormal

\[
                         E=(H_0-u)e_{\rm Eq}               \tag{11}
\]

already isolated in all three projections of the central reduced-Eq cone:

- the odd quiver cylinder needs `-E`;
- the generic rho-even packet needs
  `+2D E tensor (B1+B4)/2`; and
- the beta-zero correction `V` has `+E` and zero `rho0/rho2`, so that
  `U-V=rho0` is the cap Bockstein.

Therefore the five-face calculation and the even/Bockstein calculation do
meet at one algebraic class.  But this is equality of the scalar Eq factor,
not equality of physical cells.  The deletion-face reset, the rho-even
generic orbit, and the special `D0` face still have different word,
repeated-grade, ridge, labelled-residue, target, `W`, anchor, eta, and sigma
data.  Without one integral source-labelled comparison over `k[beta]`, it is
not valid to call the reset aggregate a Bockstein.

The shortest positive construction is now the pointed augmented physical
cone `K_Eq(beta)`.  Its normalized deletion-face projection must cancel
(11).  Composing that one cell with the already constructed degree-four
derived filler would produce the invisible cap lift `n`; then the relation
from `471bc20`,

\[
       p=(-Q,-\operatorname{ores}),\qquad
       n=(+Q,0),\qquad p+n=(0,-\operatorname{ores}),       \tag{12}
\]

finishes the primitive cap transgression.  The current calculation reduces
the search to that one physical comparison; it also proves that a larger
denominator-only Bianchi ansatz cannot replace it.

## 4. The direct-free guard leaves two quadratic normal classes

There is a useful further reduction at the sparse direct-free support

\[
                         x_{12}=a,\qquad x_{14}=b,
                         \qquad ab\ne0,                  \tag{13}
\]

with all other internal edges zero.  The Jacobian of (1) is

\[
\begin{array}{c|l}
h_1&0\\
h_2&b\,dx_{35}\\
h_3&a\,dx_{45}+b\,dx_{25}\\
h_4&a\,dx_{35}\\
h_5&a\,dx_{34}+b\,dx_{23}.
\end{array}                                             \tag{14}
\]

It has rank three.  Its two-dimensional cokernel is generated by

\[
                         h_1,\qquad ah_2-bh_4.            \tag{15}
\]

Both classes start in exact two-new-cell degree.  The first has its three
quadratic matching terms from (1), while

\[
 ah_2-bh_4=
 a x_{13}x_{45}+a x_{15}x_{34}
 -b x_{13}x_{25}-b x_{15}x_{23}.                        \tag{16}
\]

This makes the connection with the E14 unit theorems precise but
conditional.  If a physical word/fine/repeated-grade comparison carries
either class in (15) to a two-new-internal-cell extension of a canonical
E14 chart, the exhaustive E14 two-cell theorem supplies an ordinary source
unit.  A product with one additional normal cell lies in the support range
of the E14 three-cell theorem.

The needed comparison has not been constructed.  The E14 theorems start
with cells already placed in a canonical bright chart; they do not map the
denominator normal classes (15) into that chart or preserve the reset's
augmented rows.  Moreover the three-cell closing row depends on the chosen
support and is not one universal full-support identity.  Thus (15) is a
promising terminal alternative **after** the same physical lift, not a way
to bypass `K_Eq(beta)`.

## Scope

The rank-four statement is exact only after the displayed cycle
specialization and projection to the five cap-face coordinates.  The
rank-five private residual is an exact universal full-polynomial guard.  The
Hasse filler is source-valid in the committed derived totalization, while
its underived physical descent remains conditional on `K_Eq(beta)`.  No
physical Bockstein or primitive cap cell is claimed here.

## Verification

```text
python3 computations/verify_h3_degree4_reset_five_face_aggregate_gate.py
python3 -O computations/verify_h3_degree4_reset_five_face_aggregate_gate.py
python3 -I -S computations/verify_h3_degree4_reset_five_face_aggregate_gate.py
```

Frozen ledger SHA-256:

```text
1764d43ec6e85f44dde547036cc2bdc7202722070648ffb6985aca9622072c85
```
