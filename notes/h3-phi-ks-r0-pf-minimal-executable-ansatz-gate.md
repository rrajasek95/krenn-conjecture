# The smallest literal `Phi_KS,r0/P_f` ansatz stops at the cross-word section

## Outcome

Grant, as strongly as possible, the four coefficient-side inputs requested
for the canonical `h=3` comparison:

1. one `p/s`-odd order-two selector cell on each marked cut
   `0112/q23:21` and `0121/q45:12`;
2. the selected six-term `db01` response face;
3. both fixed-window operation switches `(A+B)H2345` and `(A+C)H2345`; and
4. the complete named cap packet containing `r0`, `dr0=E`, the literal
   `B=Eq` tie and all protected rows.

These grants solve every coefficient-side equation in its own literal
summand.  They do **not** assemble into a physical comparison.  The first
exact left kernel is supported on the two root-labelled off-diagonal
operation coordinates

```text
response occurrence/KS  ->  AugP2/K_Eq cap r0.
```

It kills every named and granted column and reads one on the required
`Phi` boundary.  Therefore the first missing physical atom is one natural
root-labelled response-to-cap section schema, instantiated on both the
`A/B` and `A/C` roots:

\[
          11110000=11{:}110000\longrightarrow01211222.       \tag{1}
\]

Conditional on adjoining those two sections, the next left kernel is the
primitive mixed `K_Eq` mapping-square incidence.  Conditional on adjoining
both mixed cells, the two shifted `gamma=-dOmega` ridges form the final
independent proper face.  With all three levels present, the finite equations
have the unique normalized formal chain-map shape

\[
        \Phi_1(\epsilon_s)=r_0,
        \qquad \Phi_0(c_f)=-E.                              \tag{2}
\]

Thus there is a precise candidate bicomplex, but the listed four ingredients
alone are not that bicomplex.

Exact checker:
[`verify_h3_phi_ks_r0_pf_minimal_executable_ansatz_gate.py`](../computations/verify_h3_phi_ks_r0_pf_minimal_executable_ansatz_gate.py).

## Literal finite blocks

The executable map retains seven direct-sum row blocks:

| block | rows | literal grade |
|---|---:|---|
| lower selector at `q23` | 12 | `0112`, `q23:21`, endpoint-role occurrence |
| lower selector at `q45` | 12 | `0121`, `q45:12`, endpoint-role occurrence |
| selected `db01` carrier | 360 | `11:110000`, squarefree response first PP |
| fixed window | 48 | four words, three charts, three `C4` matchings, retained `r` |
| augmented cap | 27 | `01211222`, `t*q_(v,N)`, repeated `P3+K2`, `AugP2/K_Eq` |
| root-labelled sections | 10 | two copies of operation plus response/cap word incidence |
| augmented root pair | 8 | two copies of `(R_E14,E,kappa,gamma)` |

The total row dimension is `477`.  Before the four strong grants, the named
blockwise matrix has rank `262`.  The two selector columns, selected `db01`
and both switches raise it to `267`:

```text
named map                                      rank 262
+ two odd selectors + db01 + two switches     rank 267
+ two response-to-cap sections                rank 269
+ two mixed K_Eq cells                        rank 271
+ two shifted ridges                          rank 273.
```

The low final rank is not a defect: the matrix deliberately retains every
presentation carrier and unrelated cap cokernel.  Membership of the required
boundary is decided by the three normalized left kernels below.

## What each strong grant actually solves

For either order-two cut, the old even inventory is generated by the
complete response and the residual-`q` pair.  Its rank is two in twelve
coordinates.  The granted primitive

\[
                         o_f=e_f-e_{\tau f}             \tag{3}
\]

raises it to three.  The two cuts are related by `(2 5)(3 4)`, but remain
separately labelled in the matrix.

For `db01`, retain 180 response flags, 180 carrier flags, all 180 monic
termwise graphs and both complete rows.  Their rank is `181`; the selected
six flags raise it to `182`.  The selected face remains exactly

\[
 p_0s_1\sum_{23|45,24|35,25|34}
        (dq_e q_{e'}+q_e dq_{e'}).                    \tag{4}
\]

For the fixed window, the 100 tag-preserving word, `C4`, response and
relative-carrier columns have rank `46` in 48 rows.  Granting both

\[
 (Dq_{01}+p_0s_1)H_{2345},
 \qquad(Dq_{01}+p_1s_0)H_{2345}                       \tag{5}
\]

gives rank `48`.  This uses the exact chart identity

\[
 L=-4(A+B+C)+3(A+B)+3(A+C),
 \qquad L=(2,-1,-1).                                  \tag{6}
\]

Finally, the protected balanced cap combination is

\[
                   r_{0,0}+r_{0,1}-r_{0,2}-r_{0,3}.   \tag{7}
\]

Its augmented readout is

```text
B      = ( 1, 1,-1,-1)
Eq     = ( 1, 1,-1,-1)
target = ( 1, 1,-1,-1)
M, ainc, q, P_f, ores, W, ridge, eta, sigma = 0.
```

Thus `(7)` is exactly `B=Eq` tied and every protected correction cancels in
the balanced sum.  Nothing in (3)--(7), however, changes the response object
idempotent into the cap object idempotent.

## Three successive exact detectors

For each root use section coordinates

```text
(id_response, id_cap, Hom(response,cap), response_word, cap_word).
```

The old operation algebra contains only the first two axes.  A literal
section has the third axis together with word boundary
`-response_word+cap_word`.  The covector with value `1/2` on each of the two
`Hom(response,cap)` coordinates kills the entire listed ansatz and reads one
on the desired paired section.  This is the first certificate.

After granting the sections, use per-root augmented coordinates

```text
(R_E14, clean E, mixed-square kappa, shifted ridge gamma).
```

The old `D4` return and clean `K_Eq` edge give the first two axes.  The
required comparison is `(1,1,1,0)`.  The covector `1/2` on each root's
`kappa` axis kills everything through the word sections and reads one on the
paired comparison.  Equivalently, the four edge boundaries of the pointed
mapping square have primitive cycle

\[
                            (1,-1,1,-1),               \tag{8}
\]

and `H1=Z` until one mixed two-cell fills it.

After the two mixed cells are granted, `1/2` on each root's `gamma` axis is
the final detector.  It reads one on the paired shifted ridge.  Root labels
prevent cancellation; forgetting them in the orientation which produces
`z=2A-B-C` gives coefficient two, not zero.

## Conditional candidate and protected readouts

After adjoining the two sections, mixed cells and ridges, an explicit
solution is obtained with coefficient one on every selector, `db01`, switch,
section, mixed cell and ridge, and cap coefficients `(1,1,-1,-1)` on the
four `r0` corners.  The chain equation is

\[
                         a+b=0,                        \tag{9}
\]

so monic KS and normalized cap target force `(a,b)=(1,-1)`, giving (2).

At each selected `db01` term the conditional cap readout, ordered as

```text
B, Eq, target, M, ainc, q, P_f, ores, W, ridge, eta, sigma,
```

is

```text
(1,1,1,-1,-1,0,1,0,0,0,0,0).                         (10)
```

Hence `B=Eq`, `q=M-ainc`, and all residue/`W`/ridge/eta/sigma protected rows
are exact.  There is no remaining scalar or sign obstruction.  The missing
content is physical construction of one natural paired collision mapping
bicomplex whose proper faces are exactly the two mixed cells and two ridges.

## Scope

This is an exact rational finite ansatz theorem.  The two odd selectors,
selected `db01`, and raw switches are deliberate strong grants; the checker
does not retroactively construct them.  The completed solution is a precise
candidate schema, not a source construction and not an all-`h` result.

Run:

```text
python3 computations/verify_h3_phi_ks_r0_pf_minimal_executable_ansatz_gate.py
python3 -O computations/verify_h3_phi_ks_r0_pf_minimal_executable_ansatz_gate.py
python3 -I -S computations/verify_h3_phi_ks_r0_pf_minimal_executable_ansatz_gate.py
```
