# WIP: deciding the decoration fork via summand separation

Historical working plan from 2026-08-03, committed for provenance.  Do
not cite.  Follows the seven
committed h=3 results ending at `d0cb381`.

## The observation to be tested

The fork (`h3-denominator-face-decoration-fork.md`) asks which chart
decoration the denominator face carries.  The fourth-Hasse audit's
smallest honest ambient cone is the DIRECT SUM (its (19))

    C = J_I(K) + R_v^{pq/pr} + D_v + G_v,

with the four diagonal differentials, and it states: "An off-diagonal
comparison from (18) to the cap is a genuine mapping-cone map only when
its Hom differential vanishes."

Observation: an attaching map for the four-cube cell is exactly a set of
off-diagonal Hom components.  Under the audited conventions:

1. The cell's base K_v lies in R_v^{pq/pr}, whose marked Rees symbol is
   the external square S_v with DETERMINATE + sign (four-cube (9)).
   So the cell's two-sector tail receives +S_v, pairing +I_5 -- the
   same as the bare comparison, NOT the required -I_5.
2. The denominator face lies in D_v, whose differential lands in the
   output rows e_c (slot separation, fourth-Hasse section 3).  If the
   attaching map's components respect the summand structure -- i.e.
   the D_v part maps within word-space/cap targets, never into the
   marked two-sector space -- then the denominator face contributes
   NOTHING to the connecting class, under any sign and any scaling.

If (1)+(2) hold for every admissible attachment, the fork resolves
negatively at this level: the repair fails, and the polar/four-cube
route is dead in the audited conventions.  A chart-odd denominator
contribution would require an attachment component

    psi : D_v --> (marked two-sector leading space of R_v^{pq/pr})

that mixes summands.  The open question becomes: does the mapping-cone
condition (vanishing Hom differential) permit such a psi, and if so,
does it force alpha = beta (chart-neutral) or allow alpha = -beta
(chart-odd)?

## Concrete next computation (analyzer first, checker after)

Build, in the fourth-Hasse encodings:

1. the strict chart complex generated by K_v with its marked Rees
   grading (the two-sector space of the Schur no-go);
2. the fifteen-column denominator prolongation (the support ladder
   (24) forces all fifteen);
3. the space of ALL R-linear Hom components psi: D_v -> two-sector
   space in the relevant fine degree, subject to
   (a) the mapping-cone condition d_R psi = psi d_D on the displayed
       generators, and
   (b) matching the polynomial faces (9)-(11) of the four-cube;
4. then compute, by exact elimination, the possible values of the
   pairing Lambda_v(tail of the attached cell) over that solution
   space.

Outcomes: {+1 only} => fork closed, route dead (major pruning);
{+1 and -1 both reachable} => the fork is genuinely free at this level
and must be decided by physical source data (also valuable: it
sharpens WHERE the freedom lives); {-1 only} => the repair is forced
and the polar route reopens (would be the surprise).

## Finding (2026-08-03, analyzer `analyze_attaching_map_sector_freedom.py`)

The plan's steps 3-4 were run in miniature, with two results.

1. **Abstract freedom.**  In the bare two-sector space, the audited
   face-matching constraints (Reynolds uniformity + no-leakage) leave
   the decoration pair (alpha, beta) completely free: solution space of
   dimension 2 per face, pairing (alpha - beta)/2 surjective onto Q.
   The fork is irreducible at that level.

2. **Cone-restricted forcing.**  Within the audited four-summand cone
   (fourth-Hasse (19)) the chart block is generated by K_v alone with
   dK_v = 0, so (a) the Hom-differential condition on a denominator ->
   chart component is VACUOUS (the chart block has no output degree),
   and (b) every available image is an R-multiple of K_v, whose marked
   leading symbol is chart-ODD by construction.  Chart-neutral material
   (r^pq + r^pr) exists in no audited artifact.  The mass criterion
   pins the repair-capable attachment to psi(d_{v,m_v}) = -K_v at
   leading order.

So the decoration question, restricted to everything audited, resolves
chart-odd -- the escape-OPEN branch -- and the genuinely missing datum
is relocated: **prove that the -K_v attachment's cap ordinary residue
is zero** (the frontier's own formulation of the live object, and
exactly what the Reynolds-attach coupled obstruction says cannot be
done sequentially in the old cap span).  The next work unit is that
ores computation, not further decoration analysis.

## Synthesis: the dependency chain after this session's closures
   (2026-08-03, WIP observation -- NOT a theorem; every link cites an
   audited artifact but the assembly itself is unaudited)

Tracing the ores question for the -K_v attachment through the session's
seven committed results yields a single funnel.  Each step, with its
source:

  1. The cell needs derived typing (d, tgt, ores) = (Y_0 w_v, 0, 0);
     declared boundaries are inadmissible (mapping-cylinder exclusions,
     no-go section 5 and chart-25 dual-lift section 5).
  2. In the old cap span <T, rho>, target-zero with boundary gamma*w
     forces ores = gamma != 0: the bare-cap lock
     (frontier: Reynolds-attach coupled obstruction).  So the typing
     must come from outside <T, rho>.
  3. The only constructed ores-zero source of a w_v-boundary is the
     prolonged Koszul chain n_I = s_I - T (fourth-Hasse (10)-(11)),
     which lives in the formal cone and must DESCEND.
  4. Descent is blocked by the defect (H_0 - u) e_0 (fourth-Hasse
     (14)); the cell itself cannot cancel it -- its boundary lives in
     the cap row w, and the defect in the independent row e_0
     (fourth-Hasse section 3, slot separation, quoted verbatim above).
     So no bootstrap: the cell and the descent are not mutually
     completable within the smallest cone.
  5. The defect's cancellation channels are now closed by this
     session's theorems, EXCEPT one: a row with a phi-surviving
     edge-degree-0 boundary term (row-space theorem's sole exception)
     -- the q-zero unit, i.e. the order-four Spencer generator.
  6. The unit's source-faithful construction is constrained but NOT
     excluded: the template form is impossible (T1), the commuting
     R-linear-coefficient cascade is closed (phi-closure theorem), so
     what survives is: coefficient-prolonging (non-R-linear)
     conventions, non-commuting or multiset towers, or an enlarged
     cone with symmetric chart material.

CONCLUSION OF THE FUNNEL (hedged): within the audited conventions,
every path through the four-cube branch -- including the chart-odd
attachment that the cone forces -- terminates at the same single
object: a source-faithful q-zero unit.  The three surviving
construction channels for it are exactly the three hypotheses my
theorems had to assume: R-linearity of coefficients, commutativity/
squarefreeness of the tower, and the smallest cone.  Whoever attacks
this next should attack the coefficient-prolonging convention first,
and constructively: a probe (2026-08-03) shows the phi-obstruction
DISSOLVES there rather than weakening.  Under jet-Leibniz
d(a r[U]) = sum_S D_S(a) (dr)[U\S], the e_0[V]-equation at |V| = 3
reads phi(a_V) + phi(a_full) g_i + phi(D_i(a_full)) = 0, and the
third term is a fresh unknown not determined by phi(a_full): every
equation introduces new coefficient-jet unknowns, so the
unitriangular induction has no purchase at all.  The right next move
is therefore not another no-go but a CONSTRUCTION ATTEMPT: solve the
coefficient-prolonged system for the physical d_4 -- which is
exactly the frontier's "realize it as an R-linear map from a genuine
principal-parts totalization, glue its pq/pr faces, and prove that
physical ordinary residue remains zero" (the higher-Koszul note's
remaining gap).  If the system is solvable, the q-zero unit exists
and the four-cube route completes; if the gluing or the ores proof
obstructs, THAT obstruction -- not the phi-cascade -- is the real
boundary of the route.

FOLLOW-UP PROBE (same day): the coefficient-prolonged phi-system IS
solvable -- e.g. all coefficient jets zero except
phi(D_full(a_full)) = -1, realized by a_full = -z_1 z_2 z_3 z_4 under
the coordinate tower (mixed product: phi kills it and all proper
derivatives; the full fourth derivative is -1).  But substituting
back shows this solution is a REPARAMETRIZATION of the known formal
chain n_I: it lives in the formal cone, and its physical realization
still requires the jet generators e[U], U != empty, which do not
exist in the physical complex.  So the coefficient-prolonging channel
does not evade the wall; it converges to the SAME terminal object as
the other two channels: the order-four Spencer generator (fourth-
Hasse (28)) with a physically derived unit coefficient.

FINAL SHAPE OF THE FUNNEL: all three surviving channels
(coefficient-prolonging conventions, non-commuting/multiset towers,
enlarged cones) terminate at the single question of physically
realizing the fourth mixed-row Spencer generator r_m[I] -- equation
(28) of the fourth-Hasse audit -- while preserving every EqSystem
equation.  The four-cube branch of the program is now a one-object
problem.  Its difficulty is genuine: the transversality it needs
(Psi_J(H_m) = 1) is exactly what source-validity forbids at the
template level (T1), so the realization, if it exists, must derive
the unit at the associated-graded level rather than the operator
level.  That precise tension is the recommended starting point for
the next session.

## Cautions from this session's audits

- The Hom-space computation must be built from the literal generators,
  not from a hand-picked family ("displayed family" != "reachable").
- Every boolean in the ledger computed, geometry content-hashed, the
  imported model's word and direct-free pair pinned.
- The mapping-cone condition needs the differentials on BOTH sides
  stated from audited artifacts; the D_v differential is (18), the
  chart side has dK_v = 0 -- the condition may be weaker than hoped,
  in which case say so rather than inventing constraints.
- tgt/ores on any new component are conventions to be stated, not
  facts.

## Opening of the associated-graded unit (2026-08-03, first stone)

The realization of (28) must derive its unit at the associated-graded
level (T1 forbids the operator level).  First verified fact: the
gr-level relative unit EXISTS.  Under the weight grading (edge 1, u 4)
both generators of I are weight-4 homogeneous, so I^2 is generated in
weight 8 and contains nothing of weight 4; hence [A] = [H_m] is
NONZERO in I/I^2.  A source-relative Spencer generator can therefore
carry the class [A] as its terminal symbol: the "unit" becomes the
statement that r_m's boundary generates its own symbol class, which is
a tautology at gr level -- exactly the kind of reformulation that
escapes T1, whose obstruction lives at the operator level.

Observation for the construction: this is the same "source-relative"
structure as the chart-25 Schur-Bockstein interface (24)-(25) -- the
connecting maps there are also I/I^2-level objects (lambda T = mu A is
an identity of symbols on the filtration).  The four-cube/Spencer
branch and the parity/Schur branch are plausibly the SAME question in
two languages, and a dictionary between them (Spencer generator with
terminal symbol [A]  <-->  source-faithful lift (M, Lambda) of the
chart-25 cochain) would let progress on either side transfer.  Making
that dictionary exact is the recommended opening move of the
associated-graded unit: it is finite, checkable, and if it holds, the
chart-25 4D class's KNOWN nonzero pairing becomes usable on the
four-cube side.

## The dictionary, verified at the axiom level (2026-08-03)

Textual fact, not construction: both branches declare themselves
instances of the SAME criterion.  The chart-25 dual lift says its
cochain "is literally the lifted leading cochain in the
Schur--Bockstein criterion" (n8-filtered-macaulay-bockstein-schur-
criterion.md), and the h=3 no-go calls its I_5 result "the exact
analogue of the 153 apparent degree-five chart classes removed by the
filtered Macaulay Schur test, but here the connecting map is the
identity."

Correspondence table:

    chart-25 (n=8)                     h=3 four-cube
    ------------------------------     -----------------------------
    filtered block M = [[A,0],[T,B]]   lower rows + jet tails
    lambda B' = 0                      Lambda kills leading block
    lambda T = mu A (56 labelled       Lambda T' = M A' (the missing
      equalities -- CONSTRUCTED)         lift -- OBSTRUCTED, I_5)
    secondary value lambda c - mu b    kappa-residue target (25)
    4D class (nonzero pairing 1)       the connecting class I_5

Why the lift exists on one side and not the other: chart-25's audit is
ONE-CHART -- its 56 incident labelled columns have tails proportional
to their lower boundaries (mu A = lambda T = 1/4 columnwise), and no
kernel detects a sector asymmetry.  The h=3 obstruction is created
exactly by the pq/pr TWO-CHART pair structure: columns come in pairs
with equal lower boundary and sector-split tails, so the 5-dim kernel
of chart differences detects the asymmetry and the connecting class is
the identity.

Prediction (finite, testable, n=8 side): a two-chart version of the
chart-25 56-column audit -- the source-faithful comparison demanded by
its own interface (24) -- will develop the same chart-difference
kernel and an identity-type connecting class, hitting the same
terminal object (the source-relative Spencer generator carrying [A]).
Testing this is heavy n=8 computation in the OG's lane; if confirmed,
the two branches are one question and every h=3 closure theorem
transfers.  If refuted -- the two-chart chart-25 lift exists -- then
the n=8 setting has source material the h=3 model lacks, and THAT
material is the construction the whole program needs.

Either outcome of the prediction is decisive progress.  This is the
recommended coordination point with the OG workspace.

## Concrete formulation of the prediction's experiment (2026-08-03)

Reconnaissance of the chart-25 machinery
(`verify_n8_chart25_boundary_dual.py`): a chart is a localization at
ONE target orbit row (`target_orbit_rows()[24]` for chart 25; index 25
is chart 26), with an 8-element support stabilizer.  The h=3 pq/pr
analogue therefore exists at n=8: the same source orbit columns appear
in the localized complexes of two charts, with the SAME global
boundary and different chart-local decompositions.

The experiment: build the joint two-chart complex of charts 25 and 26
on their shared source orbits (both are "minimally coupled" charts per
the frontier, and the chart-26 side already has the pure-anchor
bypass machinery); compute the kernel of the joint lower block; test
whether the chart-difference vectors span it; then evaluate the five
(or however many) leading cochains' connecting classes on that kernel.

  * Identity-type connecting class  => the branches unify; the h=3
    closure theorems transfer; the terminal object is confirmed
    universal.
  * Vanishing connecting class / lift exists  => n=8 carries source
    material absent at h=3; extract it -- it is the program's missing
    construction.

Scale warning: the chart-25 degree-4 layer has ~913,608 source-column
orbits (dual-lift note section 1), so this is hours of compute and
careful modular-first strategy (prime 1009 as in the analyzers),
not a session-tail job.  Suggested split: shared-orbit census first
(cheap), kernel structure second (moderate), connecting class last
(heavy, only on the kernel found).

STAGE 1 RUN (2026-08-03): 31 target orbit rows total; charts 25 and
26 have 12 support edges each and SHARE 5.  The overlap is
substantial and the experiment well-posed.

STAGE 2 PARTIAL RUN (2026-08-03), with a METHODOLOGY TRAP recorded
for whoever runs the full version:

  * TRAP: `canonical_column` canonicalizes under the chart-specific
    SUPPORT_STABILIZER (8 elements for chart 25, 4 for chart 26), so
    canonical keys are NOT cross-chart comparable; and it is lru_cached,
    so mutating the stabilizer between passes poisons the second
    census.  A naive orbit-key intersection returns 0 at every degree
    and is silently wrong.  Fix: load a FRESH module instance per
    chart and intersect RAW columns (word, multiplier) after expanding
    each orbit representative.
  * Corrected census: degree 2 -- no shared raw columns (components
    genuinely disjoint near their supports); degree 3 -- chart25 has
    588 orbits / 4440 raw, chart26 1270 / 4952 raw, and they SHARE 2
    raw columns, first word 12102000.  The two-chart pairing structure
    exists at n=8, appearing first at degree 3 and presumably growing
    at degree 4 (the ~913k-orbit layer where the 4D class lives).

  Remaining stage 2: the degree-4 shared census (heavy; use the
  modular-first strategy, prime 1009) and the joint lower-block kernel
  on the shared columns.  Then stage 3: connecting classes on that
  kernel -- the prediction's decisive test.
