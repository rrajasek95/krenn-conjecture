# WIP: prioritized attack map (five-agent scoping fan-out, 2026-08-03)

Uncommitted synthesis of five parallel read-only scoping agents.  Do not
cite as authority; each claim below traces to an agent report whose
computations were run in scratch, not audited.  Companion:
`wip-attaching-map-summand-separation-plan.md`.

## Headline convergence

Three agents, from three unrelated starting points (branch unification,
Spencer realization, two-chart experiment), converged on the same
terminal object — the source-faithful q-zero unit / order-four Spencer
generator — and each supplied a DIFFERENT cheap decisive test for it.
The membership agent removed its angle entirely (pending one closing
test).  The label-split agent found a probably-provable new theorem
with an unused repo theorem as its engine.

## Tier 1: decisive and cheap (minutes-to-hours; run first, in order)

1. **h=3 same-class test** (branch unification).  Three stages, all
   inputs frozen in existing checkers: compute the joint filtered left
   kernel on one packet carrying both ledgers and decide whether the
   rootless and all-inactive obstructions are ONE iterated cochain
   class or two.  Free sharpest falsifier: both branches' secondary
   values must land on -kappa*Y_0 with the same sign.  GATES tier 2:
   "same class" means the n=8 two-chart experiment buys nothing and
   the program is one-object; "different" means the translate-pair
   experiment is the right heavy job.  Traps: do not identify sector
   copies before evaluating the connecting map; build leading blocks
   from literal generators (this doubles as the first test of O3).

2. **C1 dual-certificate search** (Spencer).  The four-cube branch IS
   one finite exact system: 11,790 unknowns x 14,610 equations — does a
   constant-coefficient order-4 operator satisfy the filtration-
   lowering condition with unit trace?  Cheap first: sample evaluation
   functionals at points of V(I); a single dual certificate closes the
   branch, span stabilization without one is strong feasibility
   evidence.  Facts already run: the coordinate operator FAILS the
   n=1 lowering condition on all 15 selections (cross-term not in I);
   filtration-lowering (not preserving) is exactly where T1's chain
   breaks; two phi-surviving unit sources exist in the model
   (partial_J A = 1 formal; partial_u B = -1 real and unexamined).

3. **Crossing-pairs-are-good lemma** (label split).  The probe on the
   binding omega guard found the good-pair graph EXACTLY equals the
   crossing graph of its unique live split (16 = 16, no exceptions).
   Prove: in an exact ternary source, a crossing pair of a live split
   is good (bad-pair graph is 4-degenerate — counting room exists).
   Engine: the UNUSED proved theorem
   proofs/diagonal-hafnian-recurrence-obstruction.md + Lemma 0
   (haf(W_c) = 1 unconditionally) + Theorems A/B (live splits force
   >= 2 off-diagonal cells on a common crossing matching at
   N in {6,8,10}).  K_4 has zero live splits — boundary exactly right.
   Remaining hard half: uniformity in N.

4. **n=6 support-translation test** (membership; closing move).  The
   n=6 non-membership is genuine over Q (2-row integral dual, orbit
   masses 360 vs 1080 — stabilizer arithmetic, not geometry).  Test
   whether one support-exponent translation separates the two dual
   rows.  Either answer closes or permanently demotes the direct-
   membership route.  Verdict from the scoping: the route is a trap —
   every positive certificate in the repo lives on a specialization
   that does not lift, and K_4 (an exact GHZ(4,3) graph — verified)
   forbids any uniform-in-n identity.

## Tier 2: heavy, gated by tier 1

5. **Translate-pair two-chart experiment** (run only if #1 says
   "different classes").  CRITICAL design corrections from the
   measured reconnaissance: (a) the old scale warning was attached to
   the wrong object — the degree-4 truncated component is 21k x 74k,
   a two-minute census (measured; joint 25/26 kernel already computed:
   7-dimensional genuinely coupled part, nullity 30 vs baseline 23);
   (b) charts 25/26 are the WRONG pair — trivial joint stabilizer,
   empty swap coset, and the 4D class has NO row with d26 <= 4, so a
   degree-4 truncation structurally cannot see it (a null there is an
   artifact); (c) the right pair is a TRANSLATE pair g*S25 at overlap
   10 (40 group elements achieve it; e.g. vertex double transposition
   (45)(67)): genuine Z/2 chart exchange, order-16 pair group, 3,424
   filtration-mixed columns, class transports by construction.
   Identity-type signature (all four required): nonzero chart-
   difference subspace; perfect pairing on it; Smith normal form over
   Z all-ones; nonzero values only on the sigma = -1 eigenspace.
   Block-structured elimination (unshared columns per chart first,
   then the small coupling matrix); raw columns, never orbit; chart
   tag inside every cache key; positive control (S25,S25) must give
   coupled dimension exactly 16.

6. **Graded lock** (Spencer no-go side).  gr_I(R) = (R/I)[a,b] is
   polynomial (regular sequence — proven en route), so b^n not in (a)
   forever; if this survives the prolongation bookkeeping it closes
   the four-cube branch for the two-generator ideal.  Its one escape:
   the nine-row EqSystem ideal may not be a complete intersection —
   nobody has checked; cheap phi/edge-degree census of the nine-row
   generators decides.

7. **O3 identification** (the "most under-noticed gap in the whole
   frontier"): neither ledger block (Q_{nu,f} rootless, d_c inactive)
   has ever been exhibited as the leading block of the literal source
   complex; the h=3 no-go used the pure-denominator faces as a proxy.
   Without the chain map, the unified theorem's specializations are
   not type-correct.  Tier-1 #1 stage 1/2 doubles as its first test.

## Tier 3: uniformity and long-range

8. Theorems A/B write-up + checker; then uniformity in N (the
   diagonal obstruction is proved at N in {6,8,10} by SAT relaxation;
   notes/diagonal-recurrence-unfolding-lemma.md is the standing
   uniformity attempt; the spine needs every even order).
9. O4 (derived augmented target column, coupled with O1 — the
   Reynolds-attach obstruction says not sequential), O5 (R_adj
   nonvanishing on the trace-collision branch).
10. The forced off-diagonal RESPONSE classes (Z_jk = R_ja R_ak H_a
    nonzero at the synchronized representative) as a second,
    independent route into the label-split gap (G2).

## Named missing objects (from the unification agent)

O1 chart-odd comparison cell (mass -3 own face, 0 others);
O2 physical order-four Spencer generator with gr-derived unit
   (= tier-1 #2's feasibility system);
O3 ledger-block identification (tier-2 #7);
O4 derived augmented target column (coupled with O1);
O5 uniform R_adj nonvanishing on the diagonal branch.
Banked for free: zero lift indeterminacy (Schur typing, properties
2-3 of the chart-25 dual lift).

## Cross-cutting cautions

- Two NEW instances of the module-state/cache trap were found during
  scoping (silent 28-instead-of-23 dual rows; the recorded stage-2
  canonicalization trap).  Chart tag in every cache key; assert
  module state pristine at exit; never call chart-dependent helpers.
- "Truncation cannot see this class" must be a distinct reported
  outcome, never conflated with a vanishing connecting class.
- Filtration conventions (mixed columns lower vs leading) change the
  content of the experiment; run both, report both.

# EXECUTION RESULTS (same day, five parallel attack agents + one resumption)

All scripts in the session scratchpad (sameclass/, c1feas/, crossgood/,
n6trans/, twochart/).  Repo untouched by agents.  Every claim below is
labelled proved-over-Q vs mod-p by its agent; nothing here is audited
yet -- commit-worthy items must go through the write/audit/commit cycle.

## 1. C1 feasibility (four-cube branch decider) -- SPLIT VERDICT

GENERATOR-LEVEL: FEASIBLE over Q.  Explicit solution: D = (1/90) A(del)
(the symmetrized apolarity operator of A), alpha = 26/15 forced, via the
integral identity sum_{M in mon(A)} Q_M = 156*A.  The 11,790 x 14,610
system block-diagonalizes by site-multidegree into 1107 blocks; only the
1^8 block matters (90 x 91).  Every single selection fails (all 90);
only the symmetrization works.  The same identity K*1 = 156*1 that
gives the solution kills every dual certificate.

IDEAL-LEVEL (D(I^2) subseteq I): INFEASIBLE over Q, exact local
certificate.  The site-multidegree forcing lemma: multidegrees with a
zero site see nothing of (A), forcing c_M = 0 for every non-matching
support (17,460 of 17,550 unknowns) via singleton delta-classes; then
each of the 195 |S|=2 conditions has full local rank and kills the 90
matching coefficients one at a time (15 of the certificates read a
single monomial coefficient).  Redundant rank checks at 3 primes.

REMAINING for the branch: hybrid operators involving del_u (del_u is a
genuine ideal-level filtration-lowering operator with unit del_u B = -1
-- being closed by a follow-up run), and variable-coefficient
operators.  The obstruction is a grading fact, hence stable.

## 2. h=3 same-class test -- NOT TYPE-CORRECT / DIFFERENT CLASSES

Falsifier does NOT fire: both branches land on -kappa*(odd residue),
ledger-verified on exact packets -- but comparability needs the
unproven identification Ybar_c == Y_0 (O3 in target clothing).
Branch-1 rootless block verified (mu unimodular, ker = 0, l_2 forced
zero); its missing annihilator is a NORMALIZATION problem (726-dim
affine family exists, nothing normalizes it).  Branch-2 inactive block
IS constructible (rank 100, ker 143; the mixed row's entries are the
five face hafnians, ONE copy each -- no sector doubling), but the two
blocks are TRANSPOSED to each other (row index of one = coefficient of
the other): the chain map is the precise missing object (O3 sharpened).
Under the only available chart-blind identification, N_inact = 0
identically; the O1 cell repairs branch 1's I_5 exactly and is
invisible to every branch-2 cochain.  Also: no repo packet has both
branches simultaneously non-degenerate.  GATE: not one-object; the
two-chart experiment retains value.  Independent confirmation of the O1
spec (chart-odd partner needed for exactly -I_5).

## 3. Crossing-good lemma -- THEOREM CLUSTER PROVED, WEAK FORM REFUTED

PROVED (hand proofs + exact verification): Lemma 0 (haf(W_c) = 1 for
exact sources); Theorem A (split identity + >= 2 crossing edges by
parity); live-split existence at N in {6,8,10} (via the previously
unused proofs/diagonal-hafnian-recurrence-obstruction.md; its checker
re-run VERIFIED at n=6,8) => every exact ternary source at those orders
has >= 2 forced off-diagonal cells on a common crossing matching;
the ESSENTIALITY STRUCTURE LEMMA (E1: vanishing rows, E2: scaled unit
row, E3: deleted-pair tensor is a nonzero pure colour tensor) with
corollaries: colour injectivity, essential colour != own part colour
(closing the a = chi_u branch of the target lemma), disjoint-bad-pair
rigidity, and the counting bound #good crossing pairs >= X - 2N with
the failing shapes tabulated -- both standing guards sit exactly on
saturating shapes.  Curvature half FREE (rectangles at forced cells
automatically nonzero); goodness is the binding constraint.

REFUTED: the weak form under weak hypotheses -- explicit 727/729-
exactness guard with both forced cells on bad pairs; the two leaking
words are exactly what would force E3-purity at the carrying pairs.

REMAINING (conjectured, crisp): exact + live split + deleted-pair
tensor pure in the third colour => contradiction with liveness.
Uniformity in N untouched (engine exists only at n in {6,8,10}).

## 4. n=6 support translation -- DUAL SUCCESSION WITH A COLOUR LAW

Non-membership at n=6 proved over Q (2-row dual identified
geometrically: 6-cycle orbit vs pairwise-(0,0,1) orbit).  Single shifts
kill it; a support-3 dual (pairing 48) replaces it; exponent
irrelevant.  THE INVARIANT IS COLOUR SUPPORT: one-colour shifts
obstruct, >= 2-colour shifts are clean of target-supported duals --
terminating at a degree-2 two-colour monomial, exactly as n=4 (where
full elimination confirms membership holds for >= 2-colour shifts,
mod-p at 2 primes).  ROUTE UPGRADED from the scoping verdict: candidate
uniform localized law "(>= 2-colour support monomial) * H_0H_1H_2 in
I_mix", consistent at n=4, unrefuted at n=6.  Open: the full >= 2-
colour shifted elimination at n=6 (~5*10^5 columns).  GHZ(4,3)
exactness verified over Q (all 78 mixed hafnians zero).

## 5. Translate-pair two-chart experiment -- TRUNCATION CANNOT SEE

All regression anchors reproduced.  Convention conflict found and
resolved: tagged rows make the experiment vacuous; FUSED is the only
content-bearing convention (positive control = 16 exactly).  Stage 3:
P identically zero -- but structurally: the chart-25 dual is a left-
null covector of the UNTRUNCATED Macaulay matrix (all 54 touched
columns pair to zero exactly over Q), lives entirely in chart A's
block (zero chart-B-only columns), and the translate's +2 degree shift
pushes the leading cochain outside chart B's degree-4 window.  NO
conclusion about dictionary transfer.  Fix precisely scoped: extend
chart B to off-support degree 6, or find a pair with shift 0 on the
dual's support.

## Updated priority queue after execution

1. Commit pipeline (write/audit/commit) for: the crossing-good theorem
   cluster (biggest proved haul); the C1 split verdict pair; the
   same-class O3 sharpening + branch separation.
2. The conjectured E3-liveness contradiction (one crisp statement, has
   a near-maximal guard to test against).
3. The degree-6 chart-B window (or shift-0 pair search) for the
   two-chart experiment.
4. The >= 2-colour shifted elimination at n=6 (heavy; decides the
   colour-law candidate).
5. Hybrid del_u class (running); then variable-coefficient operators
   if the program needs the constant story extended.

## 6. Apolarity-descent attempt (the needle-threading construction) -- KILLED

The chain n = r_0 + A r_0[D] - B r_m[D] - T exists in the D-prolonged
complex (dn = Yw, tgt 0, ores 0, verified exactly; D = (1/90)A(del),
D(A) = 1, D(B) = 0).  Three independent kills, in increasing strength:
(K1) the one-direction prolongation is DECLARED, not derived -- an
order-4 operator's product rule needs all 16 faces, and the coherent
version is literally the repo's formal chain s_I (already formal-only);
(K2) NEW INVARIANCE PRINCIPLE: presentation-invariance (r_0 -> r_0 +
h r_m presents the same equations on the source; the D-prolonged
complexes are non-isomorphic, closed-form defect (deg_z(A)/90)z not in
I by the weight grading).  Strictly weaker than source-validity and
still violated -- this criterion is reusable and audit-independent of
the phi machinery (D has no order-one part, so T2 has no purchase);
(K3) criterion-free: the complex makes B e_0 a BOUNDARY -- the
construction deletes the obstruction rather than resolving it (cokernel
R/I relabelled onto the new slot); pi_Delta(n) = r_0 - T with the
identical defect (H_0-u)e_0 (eq 14 bit-for-bit).
Also: two premise corrections (D(A^2) = (56/15)A, the 26/15 being the
proper-split part; D(AB) = B only because B is mixed-free) and the
du one-direction prolongation closed (A | B^2 required, fails by mdeg;
structural reason: du's unit sits on the r_0 row which already carries
target 1 -- a unit only helps on r_m, exactly what source-validity
forbids).

## 7. Hybrid/constant-coefficient closure -- ALL ORDERS

Z^8 x Z^8 bigrading (u -> (0,1^8)): the A-slot unit is unreachable by
ANY constant-coefficient operator of ANY order in ANY of the 55
variables under ideal-level lowering; achievable units = B-slot only
(realized by -du), and B-slot pulls the membership back to
B^{n+1} in (A), failing for every n by mdeg.  Six forcing steps by
bidegree uniqueness.  Side identity: A and H_0 are the SAME polynomial
under mixed<->pure relabelling (asymmetry lives entirely in u).
Remaining crack, precisely: VARIABLE-COEFFICIENT operators (bidegree
compensation defeats uniqueness).  Plus, independent of operators: the
ordinary-residue descent through the 15-column denominator
presentation.

## Pipeline state (end of 2026-08-03 execution wave)

Artifacts built in-repo, under audit:
  computations/verify_exact_source_live_split_forcing.py +
  notes/exact-source-live-split-forcing.md  (digest 4351392d...)
  computations/verify_h3_apolarity_operator_split_verdict.py +
  notes/h3-apolarity-operator-split-verdict.md  (digest 8c024cc3...)
Owed artifacts (scratch-proven, not yet built): the apolarity-descent
kills incl. the presentation-invariance principle (K2) and the
all-orders hybrid closure; the same-class/O3 findings; the n=6 colour
law; the translate-pair truncation-blindness data.

## 8. O3 CHAIN MAP: EXISTS, UNIQUE, FORCED CHART-ODD (unaudited, major)

Scripts scratchpad/o3map/.  The comparison map d_c -> (no-go complex)
exists on the A-summand, is unique up to scale, and chart-oddness is
FORCED (shared lower boundary G_i + 81 monomials divisible by no h_v
give (a+b)*1 = 0), not chosen.  psi_1(c_v) = k_v exactly: the
distinguished denominator column maps to the chart-difference kernel
vector.  New structural lemma: d_c's 105 columns have pairwise
disjoint supports, so d_c is INJECTIVE (the ker=5 of stage2 was a
specialization artifact).  Consequences:
  (i) DECORATION FORK RESOLVED toward CHART-ODD (escape-open branch);
  (ii) the same-class branch-separation verdict is OVERTURNED -- the
      chart-blind identification is not a chain map (dim 0); under the
      forced map, branch 2's connecting class pulls back to I_5 and
      the O1 cell kills BOTH branches with one vector;
  (iii) the full-block obstruction is exactly the Q*t_c*B term, and
      the repair is a concrete 10-column augmentation tau_{uv} with
      prescribed tails (the Q-entries of d_c at m) -- matching the
      no-go ledger's own required_additional_row = tau_<site>.
Flags: strict-vs-projected ambient convention (both run; projected
uninformative); one-sided in filtration (branch 2's lower block not in
repo); Side 1 is the no-go's pure-denominator proxy.  NEEDS the full
build/audit cycle before being trusted -- it contradicts an earlier
agent verdict and the resolution argument (chart-blind not admissible)
must survive an adversarial audit.

THE FUNNEL, FINAL FORM: with rows, operators (all orders, all
coefficients -- pending the weight-splitting confirmation), and the
bare polar all closed, and the fork resolved chart-odd, the entire
four-cube/Schur complex reduces to ONE construction: derive the ten
tau-columns' lower boundaries A'(tau_uv) source-faithfully and prove
the cap ordinary residue vanishes.  Ten columns, thirty monomials,
exact spec in scratchpad/o3map/ S10.

## 9. Shift-0 search: DEGREE-4 IMPOSSIBLE (proved), MINIMAL WINDOW = 5

Exhaustive 241,920-element sweep + a proved counting bound: S is a
proper 3-edge-colouring; overlap 11 impossible (3 edges of a colour
class force the 4th); best genuine overlap 10 = Kempe 2-swap inside one
colour class; exact delta identity d_gS(r) = d_S(r) + |r cap {a,b}| -
|r cap {e,f}|; multiplicity count on the 10 top rows gives
max_r d_gS >= 5 for every gS != S (and >= 6 for colour-0 swaps, which
is why the original pair failed worse).  Minimal window 5, attained by
4 supports; best pair g_v = (0,2,1,4,3,6,5,7), g_c = (1,0,2):
11/23 dual rows already inside the degree-4 component, 2601/17092
shared rows/cols, swap element exists (pair group order 2).  The
dictionary test therefore runs at degree 5, not 6.
Scripts scratchpad/shift0/.

## 10. Colour law vs n=8: NOT REFUTED + new confirmation + A REDIRECT

New full-source n=8 measurement (exact over Q): the 903 columns
violating the balanced dual decompose by required-shift colour exactly
as the law predicts (666 one-colour, 145 two-colour, 92 three-colour;
581 need only a single support variable -- n=6's "single shifts kill
it" replicated; the 180 zero-shift columns reproduce the note's own
census, calibrating the machinery).  The three positive certificates
tabulated: 60-edge = degree-10 THREE-colour (confirms), chart-26 =
degree-3 TWO-colour (confirms), chart-25 one-generator = zero-colour
membership BUT a non-lifting specialization (defused; its own note
records the non-lift).  Minimal >= 2-colour shift at n=8 (degree-2
bicolour, 48 candidates) has NEVER been tested.

REDIRECT (independent of the law, act on it): the frontier's t^k
saturation escalation (t^7 not in I^h, "no termination bound") is
testing 1 in the normalized ideal -- the UNIT -- which is strictly
stronger than the F^h membership the conjecture needs; t is COLOURLESS
(introduced after the colour torus is quotiented).  The law predicts
exists N: t^N F^h in I^h while predicting nothing about t^N in I^h.
The escalation as framed can never terminate even if the conjecture-
relevant statement is true.  Flag to the OG workspace.

DECISIVE EXPERIMENT SPEC: paired degree-2 shift at chart 26, full
252-variable source, adaptive dual-guided search (not brute Macaulay):
Arm B (two-colour, s_03 s_25) FIRST -- a target-supported dual there
kills the law immediately; Arm A (one-colour, s_03 s_16) predicts a
successor dual.  ~1 machine-day both arms.  Free 5-minute precursor
(reweighted-dual survival across all 60 shifts) RUNNING now.

## 11. THEOREM C CLUSTER (E3-liveness attack) -- MAJOR, builder running

PROVED (hand + instances, scratch e3live/): Theorem C -- deletion
identity with saturating-family correction; if nothing saturates, a
nonzero crossing matching exists with ALL crossing edges good (the
label-split application verbatim; UNIFORM in N).  Lemma F (disjoint
bad-pair deletion leaves 0 or a pure colour tensor).  Lemma G (each
site in <= 1 bad pair per essential colour; #bad <= 3N/2 TIGHT at K_4;
each bad colour class is a matching; <= 1 saturating family per
colour).  C4'/C5': #good crossing >= X - 3N/2; ONLY shape (0,2,N-2)
with N <= 8 can fail => GOOD CROSSING PAIR EXISTS UNCONDITIONALLY AT
EVERY EVEN N >= 10; at N=8 only shape (0,2,6) survives, residual = one
scalar identity.  New 720/729 stall guard: anchors + split + liveness
+ E1/E2/E3 at both bad pairs, sole surviving Theorem-C term is the
saturating family (+1 cancelling -1) => the conjecture is unprovable
from anchors+split+E1-E3; the saturating term IS the obstruction.
Complementary to the committed E3-breaking guard.  Dead ends recorded
(Boolean route; R1-in-isolation).  COMPOSITION: with live-split
existence (committed at 8,10; uniformity agent running on exactly the
surviving shape), label-split forcing holds at all even N except the
single N=8 (0,2,6) saturating case.

## 12. TAU DERIVATION: CONSISTENT BUT INERT -- the direct-free polar
      route is closed for ALL derived material (terminal, unaudited)

Scratch taucols/.  The tau columns EXIST and are DERIVABLE: complete
classification of 2106 second-polar derivation solutions; the
V-admissible CAP family gives derived boundaries A''(tau_uv) = H_w
(9 words/pair; D2 family: all ten from ONE fully-mixed parent row
w* = 01211200, of which the five no-go rows are one-site resets).
They complete the o3map S10 repair spec exactly (Hom 0 -> 2, a+b = 0
re-forced by an independent Q-block certificate).  BUT the connecting
matrix becomes [I_5 | 0]: tau squares provably orthogonal to the
obstruction.  UNIVERSAL ARGUMENT: derived lower boundaries are
chart-blind (the two chart rows of a word share one global boundary --
geometry-forced), the required covector is chart-odd; row(A'') can
never contain it, NO MATTER WHAT IS ADJOINED.  Chart-parity tension
resolved: a derived chart-odd tail forces boundary ZERO = a rescaled
kernel vector = no repair.  Rigidity reconciled (tau tails x-bearing,
outside the swept scope; Fact C analogue holds).  Corrections to my
task premises recorded: the ledger's tau_<site> is a different object
(5 vs 10 names); the tau monomials are x-incident, NOT internal D x D.

REDIRECT: chart-blindness rests on the DIRECT-FREE specialization
(A_pr = 0 empties the pr-direct sector).  In the TILTED model the two
chart tails need not coincide -- chart-sensitive derived boundaries
become possible exactly where the frontier's primary target says
"tilted".  Tilted probe RUNNING (scratch tilted/).  If the tilt breaks
chart-blindness and the repair becomes reachable, the route reopens
where the unified target always pointed; if not, the h=3 Schur route
is dead at both specializations and the uniform theorem needs a
different mechanism entirely.

## 13. Colour-law precursor: NO cheap proxy exists

All 90 shift arms kill the old 100-monomial dual (one- and two-colour
alike; control m=1 reproduces the frozen 706/0 census exactly).  The
law's content is successor-dual existence -- invisible to old-support
survival tests AND to incidence statistics (violating-column counts
cluster by stabilizer orbit and MIX colours; no colour signature at
any cheap resolution).  A third orbit/cache trap documented: orbit-
representative filtering is invalid for non-stabilizer-invariant
shifts (caught before a false corollary shipped).  The decisive
experiment is now size-matched: Arm A = s_34^0 s_67^0 (794 avail/88
viol) vs Arm B = s_34^0 s_23^2 (809/101), ~2% apart; run B first (a
target-supported dual there kills the law immediately).  ~1 machine-
day, adaptive dual-guided, next session or OG.

## 14. UNIFORMITY ATTACK: R2 refuted, pencil identity found (scratch uniformN/)

INVERSIONS: shape (0,2,N-2) is provably DEAD on the Hamiltonian-triple
family at every even order (cycle argument: M_r cup M_s Hamiltonian =>
only two perfect matchings => two-part splits dead); R1's deletion
induction structurally dead (Lemma U2: pairs in two co-supports are
common non-edges of all three colours).  The earlier "one scalar
identity at (0,2,6)" framing was over-optimistic.

NEW COMPOSITION AT N=8: drop-(0,2,6) is UNSAT (new SAT theorem, exact
encoder reproduction) => every diagonal packet at N=8 has a live split
of shape != (0,2,6) => by C4' it has a GOOD CROSSING PAIR.  Caveat:
weaker than Theorem C's all-crossing-edges-good; the saturation
analysis still needed.  At N >= 10 the analogous drop-(0,2,N-2)
instance is OPEN (n=10 unresolved in 25 min, harder than full).

THE PENCIL IDENTITY (proved, verified n=4,6,8; found nowhere in repo):
haf(x0 W0 + x1 W1 + x2 W2)[V] = sum over ordered even partitions of
x^{|S|/2} * h0(S0)h1(S1)h2(S2).  The diagonal engine's target is
exactly DIAG-infinity: haf(pencil) = x0^k + x1^k + x2^k has no
solution, k >= 3 -- uniform, relaxation-free.  Two-colour shadow
ALWAYS satisfiable (alternating 2k-cycle) => all content is the
simultaneous three-colour condition; explains every two-colour-blind
route's death.  FERMAT LEAD: rank-2 essential stars have kernel points
ON the Fermat curve sum x^k = 0 -- first uniform geometric constraint
linking goodness to plane curves; iteration to contradiction open.
Unfolding-lemma note assessed: single-order, cannot yield induction.

## 15. TILTED PROBE: THE TILT CHANGES NOTHING (model-independent death)

Scratch tilted/.  Fact A is a tautology of the chart convention (a
chart partitions ONE row) in both models; Fact B's four-sector closed
form (ABC split by use of site p) shows the tilt's new material (the
B-piece) lands only in the two sectors Lambda_v never reads, and
B = 0 IDENTICALLY for the cap marking a_pq^00 ({p,q} and {p,r} share
p): the marked profile is bit-identical between models on all 32805
cases.  Connecting matrix I_5 in both models.  All 8 tilted
augmentations: repair unreachable, same witness (k_v in ker A'',
Lambda T''(k_v) = 1 -- both tilt-independent).  Only escape found:
the dehomogenised chart-column convention (CONV-X5), which breaks
Fact A but trivializes BOTH models identically -- not a tilt effect.

=> THE h=3 MARKED-CHART SCHUR MECHANISM IS DEAD AT BOTH
SPECIALIZATIONS FOR ALL DERIVED MATERIAL.  Capstone builder running
(verify_h3_schur_route_model_independent_closure).  Program weight
redistributes to: the diagonal/pencil lane (DIAG-infinity, Fermat
lead, Theorem C), the n=8 two-chart test (dict5 running -- if n=8 has
source material h=3 provably lacks, the secondary operation lives
there), the colour law, and the frontier's non-Schur components
(Hamilton descent, chart-26 propagation).

## 16. DICT5: LIFT-EXISTS AT n=8 (the day's most hopeful result)

Scratch dict5/.  On the optimal translate pair at the proven-minimal
degree-5 window: all 23 dual rows inside both charts' components;
positive control passes (coupled dim = 556 = full left-nullity);
joint nullity 1,138 (baseline 1,112).  The pairing P is EXACTLY ZERO
on all of K over Q -- and a UNIQUE exact mu is exhibited with 0
failures replaying Lambda T' = mu A' on all 1,452,322 raw joint
columns.  NOT degenerate: the leading action is nonzero on 20 lower
columns; structural proof (any Macaulay column meeting a dual row has
chart-A min degree <= 4) shows no deeper window changes it.  mu
concentrates on 10 filtration-mixed rows straddling the charts --
THE source material h=3 provably lacks (h=3's I_5 obstruction is
ABSENT at n=8).  sigma-table: Lambda_A - Lambda_B realizes the
chart-difference direction at sigma = -1 inside the joint kernel but
carries no pairing.

CONSEQUENCE: the source-faithful two-chart comparison (interface (24))
is CONSTRUCTED at n=8.  The corrected cochain lies in ker(connecting)
trivially; the target test (25) Lambda c' - mu b' = kappa R_adj is
well-defined for the first time.  REMAINING GAP ON THIS ROUTE: the
raw augmented targets b', c' (never constructed in the repo) and the
evaluation.  Target-evaluation attack launched.

## 17. FIRST SECONDARY-VALUE EVALUATION: B = -1 (the anchor), decoupling
      mechanism identified (scratch target25/)

The secondary Schur value on the constructed translate-pair lift:
B = Lambda c' - mu b' = -1 exactly over Q; well-defined (mu unique at
CUT2/CUT3; 80 kernel perturbations invariant; both target conventions
agree; CUT4 proven ill-defined and discarded); genuine two-chart
reduced-residual certificate R = r1 + r2 (the 4D-class analogue; x0
uses 275 chart-A-leading columns made lower by chart B -- irreducibly
two-chart).  BUT the value is the chart-25 anchor lambda_25(4D) = 1
reproduced -- no kappa, no Y.  MECHANISM: on a translate (symmetric)
pair the two duals are completely decoupled (0 of 1,452,322 columns
meet both supports); the joint value equals the one-chart value by a
bookkeeping identity.  Also: dict5's replay check was tautological as
written -- re-verified honestly (0 failures), conclusion stands.
Formula-(12) double-count reproduced (-2 vs -1) and avoided.

NEXT STEP ON THIS ROUTE (unambiguous): build the PRIMED FULL-NINE
blocks at n=8 (interface (24)-(25) proper -- A', T' are the full-nine/
curvature blocks, not another chart's) and run this exact evaluation
asymmetrically.  The machinery (lift construction, well-definedness
discipline, raw-layer bookkeeping) is now validated end-to-end.

## 18. n=6 two-colour elimination: INCONCLUSIVE, law NOT refuted

Scratch n6trans/ (s7-s14).  All anchors reproduced exactly.  Stab(s)
order 96 computed; full shifted component 1,829,235 x 891,874 rows/
cols, 13.0M nonzeros (7x projected rows).  No >= 2-colour dual at
depth 0 or 1 (control on the one-colour shift correctly returns the
obstruction -- the test has teeth); the bounded-support route provably
cannot decide (full row rank until ~depth 4 = the whole component;
n=4 certificate first appears at depth 5/8: PROVABLY NON-LOCAL).
Full sparse elimination: true-Markowitz reached 600k/891k pivots at
both primes before the dense core outgrew Python.  Suggestive: the
n=4 matrix-free reduction remainder has mass 0 exactly for the
2-colour shift (48/12 for obstructed ones).  CLOSING MOVE (external
tool): block Wiedemann/Lanczos over GF(1009) on the exported matrix;
s13_elim_mk.py::build regenerates in ~340 s.  Everything consistent
with the colour law; nothing contradicts it.

## 19. THE ENDGAME COMPUTATION: I_5 WAS A RED HERRING; THE REAL
      OBSTRUCTION IS B' = Y_0 (scratch fullnine8/; unaudited, major)

On the repo's only literal full-nine n=8 model (the h=3 direct-free
specialization, extended to all 6561 words x 2 charts = 13,122
columns): (1) the polar cochain family is PARITY-INADMISSIBLE for
interface (24) -- sector-odd cannot restrict to the sector-even
collision-fibre cochain; I_5 was the parity phenomenon (ker A' IS the
odd part), not the interface obstruction.  (2) For admissible (even)
cochains THE LIFT EXISTS: 404-dim admissible space, Lambda B' = 0 and
Lambda T' = M A' with 0 failures on all 13,122 columns; exactly 2
columns pair nonzero (the 56-equality analogue).  (3) The secondary
value Lambda c' - M b' is IDENTICALLY ZERO on the whole admissible
space -- proved by rowspace membership -- because of a structural
coincidence: THE PURE DENOMINATOR FACE IS THE PURE WORD'S MARKED TAIL
(B'-support = Y_0-support), so Lambda B' = 0 kills the target and the
pure-word lift equation kills the lower term.  Non-degeneracy control:
the same cochains take many nonzero values on the selected-polar
target.  (4) Exact dichotomy: even -> lift, value 0; odd -> I_5 now
sitting AT the adjacent-power row, no lift, value still 0 (target is
even).  (5) Missing objects made precise: no synchronized nine-row
curvature packet exists in computations/ ("full-nine" = the 3x3 block
entries); kappa exists only target-side (4 abstract packets); the
direct-free specialization DEGENERATES kappa to a monomial.

THE NEW TERMINAL QUESTION: supply a leading block that SEPARATES the
denominator face from the adjacent-power tail (B' != Y_0) -- the
no-go itself noted retaining the pure faces as B' was a CHOICE
("stronger than taking the leading block to be zero").  And the
kappa side requires the tilted (non-degenerate) packet.  B'-separation
probe launched.

## 20. PENCIL-FORM DIAG-INFINITY IS FALSE (scratch fermat/; caught
      mid-build, artifact intercepted)

The 2k-cycle with odd edges x_0 and even edges x_1 - zeta_i x_2
(zeta_i^k = -1) solves haf(pencil) = x_0^k + x_1^k + x_2^k for EVERY
k >= 2 over C (two-line proof: a cycle has two matchings); rational
witnesses at k = 2 (K_4), 3 (Hesse-derived), 4 (ladder); k >= 5 over
Q open.  The mis-transfer diagnosed: the committed SAT theorem proves
the strictly stronger TERMWISE statement (every split product zero
individually); termwise => pencil-solution, NOT conversely (the cycle
solutions have 2..30 live splits).  THE UNIFORM TARGET IS THE
TERMWISE STATEMENT ONLY; pencil-insolubility cannot be the route.
The Fermat geometry IS proved and realized by the counterexamples:
star rank >= 2 (else Fermat smoothness violated); rank-2 pencil
points lie ON the Fermat curve; NEW TANGENT LEMMA (shared-point
rank-2 pairs force L_uw = the tangent line at p).  Refined lead: do
these constraints collide with the ADDITIONAL termwise vanishing?
k=2 consistency: no step separates k=2 from k>=3 (correct, target
false at all k).  Dimension counts useless (45 unknowns vs 10
equations at k=3).  SAT builder intercepted with corrections; the
2k-cycle becomes a committed GUARD (termwise strictly stronger than
pencil).

## 21. B'-SEPARATION VERDICT: INTERFACE (25) NOT DERIVABLE AT n=8 IN
      ANY ADMISSIBLE FORM (scratch bsep/; unaudited, terminal for the
      marked-comparison realization)

The B' = Y_0 coincidence is a TAUTOLOGY OF THE MARKING (two-line
derivation; 32805 columns verified; the 17010-marking sweep shows the
tail depends only on the residual site set, forced to be D minus {v}).
Corrected joint model (211 pure-odd lift equations, 2214-dim
admissible space; all fullnine8 verdicts survive).  FINAL-FORM
IDENTITY, verified on the full 2430-dim space: both raw secondary
values are linear functionals of Lambda B' ALONE (lift equation
pure-odd, target pure-even).  Block census (12 variants): contains
g_v diagonally => value == 0; omits it => value FREE over Q
(satisfiable, never derivable -- the no-false-correction violation);
mixed-face blocks INFEASIBLE against the collision-fibre calibration;
per-site sharp.  Tilt: no second route (the a_pq mark kills exactly
the restored (p,r) matchings; 0 mismatches).

=> THE MARKED DOUBLE-POLAR COMPARISON CANNOT REALIZE (24)-(25),
   at h=3 or n=8, direct-free or tilted, for any leading block or
   marking.  The "hafnian Bockstein", if it exists, is a different
   comparison type.  Surviving lanes: the TERMWISE diagonal question
   with the Fermat-tangent refinement; the colour law; the frontier's
   non-Schur components (Hamilton descent, chart-26 propagation); and
   comparison mechanisms not built from marked polars (reset/word-
   space operations were partially explored; the frontier's Component
   I-IV formulations remain the specification).

## 22. n=6 verdict machinery: LinBox working locally; Wiedemann RUNNING

Tool run (scratch n6trans/): LinBox 1.7 installed via micromamba
(conda-forge; system clang cannot compile its headers — use the
conda clang++ with -std=c++14 and rpath); lbrank driver validated at
n=4 against two independent eliminators.  elim.cpp: 50x faster than
Python, pinned the wall (fill doubles per ~10k pivots; core at 600k
pivots is 123k x 618k at 0.077% — too sparse for dense, too big for
sparse).  NEGATIVE: BFS-layer-graded elimination is ~6x WORSE fill.
NEW TECHNIQUE: BFS-ball membership ladder (sound; certifies at n=4
radius 4/7; walls at n=6 between 370k and 738k generators).  Export
package complete (SMS + anchors + runbooks — doubles as the AWS
bundle; c7a.8xlarge ~1 h).  BLOCK WIEDEMANN NOW RUNNING locally
(niced, ~10 h, both ranks; rank equal => colour law confirmed at
n=6; differs => extract dual, >= 2-colour refutes).

## 23. TERMWISE STRUCTURE CLUSTER (scratch termfermat/; COMMITTED
      364ccab after audit cycle — Theorems A, B, C all repo-grade;
      builder's post-audit find: n=8 load-bearing faithfulness
      instances DO exist, 4608/8208 parts admit a second matching,
      correcting the audit's structural claim)

BUILT 2026-08-03: verify_termwise_rank3_cubic_uniqueness.py (2311
lines, digest 55636a57..., 3.6 s) + notes/termwise-rank3-cubic-
uniqueness.md (852 lines).  Upgrades over the scratch: A1 DERIVED
from termwise-deadness (A2-A5 on anchored+TW2, 75 signed instances);
rank-2 vacuity over Z[s]/Phi_2k (integer minors, no division);
B3 reformulated as GAP-SIGNATURE PARITY criterion (O(k^2), k=14 in
0.3 s, cross-validated vs brute force k<=6 ONLY — the builder's
self-flagged largest exposure); stall measured exactly (k=3: all
48/32 profiles (2,2,2), (S1) alone kills k=3; k=4: 0/5832
unkillable).  20/20 mutations fire.  Self-flagged weaknesses: B3
reformulation unvalidated k>6; n=6 census never exercises
faithfulness; (B2b) prose compressed.  Independent audit dispatched
targeting exactly those three.

AUDIT VERDICT (FAIL #16, corrections dispatched to builder): all
mathematics CONFIRMED — B3 criterion verified on ALL arc systems at
k=7..10 + 300 matchings at k=12 (and rows k<=10 never depended on
it); (B2b) a complete proof; Theorem B evidence rebuilt from scratch
(k=2..6: 1/4/31/293/3326 graphs, exactly-three only K_4).  Defects:
D1 FALSE CLAIM — all 13 "faithfulness load-bearing" parts are
uniquely matched, so (S2) settles them with NO faithfulness; nothing
in the artifact exercises Theorem C's distinctive hypothesis;
structural at n<=8 (1884 anchor triples, no size-4 part with two
disjoint non-union crosswise edges); genuine instances exist at
D(10)/D(12) (auditor verified).  D2 SILENT HOLE — 3x3 determinant
cofactor sign flip passes with UNCHANGED digest ([[0,1,1],[1,0,1],
[1,1,2]] rank 2 reported 3); A4 rests on it.  New discipline entries:
RANK ROUTINES NEED PER-COFACTOR DESIGNED CONTROLS; "load-bearing"
claims about a hypothesis must be witnessed by an instance the
hypothesis alone settles.

FIFTH INVERSION: the Fermat lead is dead by THEOREM — termwise-
deadness forces every star to rank exactly 3 (A1-A5: essential edges
monochromatic, disjoint essential graphs, every vertex carries every
colour), so the rank-2 pencil geometry is VACUOUS on termwise
configurations; mutual exclusivity (A4 alone refutes the 2k-cycle
pencils).  THEOREM B (new, standalone, classical): K_4 is the ONLY
cubic graph whose three PMs form a proper 3-edge-colouring and are
its only PMs (minimal-arc proof; verified to k=14).  THEOREM C
(UNIFORM IN k): no anchored MATCHING-FAITHFUL termwise-dead
configuration for any k >= 3 (anchors -> monochromatic disjoint PMs
-> cubic union -> extra PM -> live split).  Complementarity: SAT =
cancellation, k <= 5; Thm C = all k, no cancellation.  THE STALL
(open, exact): can h_c(S_c) = 0 cancellation on matched sets be
arranged for EVERY extra PM simultaneously?  Narrowings proved
(|S_c| >= 4; non-anchor edges required); k=3 (2,2,2)-profile
unkillable.  k=2 boundary exact (K_4 IS termwise-dead; k >= 3 enters
at B1/B3).  Exhaustive 2^18 census at N=4: exactly 6 dead packets =
K_4's colour-orderings.

AUDIT LOG (termwise-census pair, 2026-08-03): FAIL #14 — ALL
mathematics independently confirmed (auditor rebuilt the SAT census
with a fresh encoder, identical numbers incl. the n=8 drop-(0,2,6)
UNSAT; re-derived the Hamiltonian-triple lemma to n=40; all 19
documented mutations re-fired).  Seven packaging defects: (1) FALSE
"identity in Z[s]/(s^k+1)" claim (not a domain; k=3 coefficient
1+s^2-s != 0 there — correct home is Q[s]/Phi_2k + ring-hom
transport); (2) U2 contrapositive check tautological (guard implied
by antecedent, fires 0/1.5M times); (3) u2_census inline complement
scan = silent M4-class hole (bug injection passes with digests
UNCHANGED — the one control-coverage gap); (4) six hard-coded ledger
constants vs "every boolean computed"; (5) opt-in n=10 path bakes in
unproven UNSAT expectation; (6) undisclosed factor-of-2 counting
convention; (7) misattributed diagnostic text.  Corrections
dispatched; new digests pending.  Lesson for the discipline list:
POSITIVE CONTROLS MUST COVER EVERY IMPLEMENTATION of a scan, not one
canonical copy — inline re-implementations dodge the control.

## 24. RESET LANE CLOSED (scratch resetlane/; the last comparison-type
      keystone candidate is dead; COMMITTED 6c35854 after audit cycle)

PAIR BUILT 2026-08-03: verify_h3_reset_lane_ores_descent_lock.py
(file dedc6090..., ledger 460b600e..., ~5.5 s, 3 mutations fire) +
notes/h3-reset-lane-ores-descent-lock.md.  Audit dispatched with
priority targets: the lock's u-extraction bookkeeping (does the
edge-degree-4 homogeneity really isolate A_0's constant term given
u has weight 4), Theorem D's arbitrary-g_U quantifier (symbolic vs
instance-based), and whether "cap-normalized" meets the totalization
note's actual Option 1 wording.

AUDIT VERDICT (FAIL #15, corrections dispatched to builder):
Theorems D, E, the LOCK all CONFIRMED (independent re-derivation;
all 101 requires fire, zero dead guards; "u has weight 4" hazard
checked — no weight truncation exists in the implementation, edge-
degree is a genuine grading, extraction sound).  ONE load-bearing
defect D1: the comparison refutation forces c = 0 only for SCALAR c;
counterexample c = H_m (the bottom Koszul cell pi_empty N) is a
physical element of type (cKYw,0,0).  REPAIR PROVED BY THE AUDITOR:
transport T and rho too — tgt(Lambda T)=1 forces c|_0 = 1 vs escape
transport c|_0 = 0, contradiction for ANY polynomial c; the
q*e_Eq-component escape dies by the rho-transport (1 = Y*sigma
impossible).  Option 1's literal codomain ("actual filtered source
resolution") exists as no committed artifact — refutation to be
rescoped to all comparisons landing in the committed module.
Plus D2 rescope Option 2, D3-D5 cosmetic.

Script reset_lane_ores_descent.py (exact Fraction, imports the
committed totalization checker; baselines re-run, digest ee3699d5
matches).  Reconstruction VERIFIED on all 15 (v,N) literal objects.
Then three theorems:

THEOREM D (full chain).  For ANY readout rho = sum_U g_U pi_U with
arbitrary polynomial coefficients: rho a chain map => ores(rho Z) =
-kappa*Y*g_empty*H_m -- a functional of the source equation ALONE,
identically zero on the source.  Same death shape as the marked-polar
lane ("value a functional of Lambda B' alone"), transported.

THEOREM E (15 columns).  On the denominator columns the r_m
compensator is absent, so defect functional == ores functional
(sum_U g_U d_U(h_s), checked on all 20 face/column pairs).  Column by
column: rho chain map <=> ores(rho Z_s) = 0.  The -kappa*Y_0 readout
is attainable ONLY with eq-defect (H_0-u)*eq.  "The leaks ARE the
eq-defect; killing them kills the readout" (leak ledger rank 12/22
monomials, no cancellation).

RESET-LANE LOCK (invariant form).  In the physical module
(+_a R g_a) + RT + R rho with dg_a = F_a*eq, dT = -Yw, d rho = w:
NO n with (d,tgt,ores)(n) = (kappa*Y*w, 0, 0), kappa != 0.  Proof via
the u-monomial at edge-degree 0 (u occurs only in F_0, coeff -1;
every hafnian row edge-degree-4 homogeneous over all 6561 colourings).
Single hypothesis HYP-L: coefficients polynomial (the only localized
escape inverts H_m itself).  Extends the bare-cap lock (9a) to the
full bounded EqSystem-row-plus-cap module.  COMPARISON REFUTATION:
the escape kappa(s_I - T) exists upstairs, but any cap-normalized
comparison Lambda transporting it forces c = 0 -- totalization note
sec.6 Option 1 is REFUTED, not constrained; Option 2 excluded for
source-valid corrections.

PARITY: the lane's chart-odd object N^pq - N^pr has zero w-boundary
and zero ores; the cap target kappa*Y*w is chart-EVEN.  Same
separation as the marked lane, transported.  Word parity mixes
(12112/22012/02012 odd->even) -- the honest reason the lane survived
the marked-polar identity -- but the lane dies by the u-degree/
eq-defect coupling instead.  Indeterminacy: second tag m'_8=01221222
verified (same top chain); physical-lift requirement holds VACUOUSLY
(the lift set is empty).

AUDIT FLAGS on committed artifacts (record for future correction):
A1 pr_direct identically empty in the direct-free model -- every
"did not enter pr-direct" assertion in the totalization checker is
vacuous; A2 r^pq and r^pr have IDENTICAL differentials there -- (23)
closure is antisymmetrization of two identical copies, no genuinely
distinct pr-chart row tested by any committed artifact; CONV-1 the
cap variable Y is never identified with Y_0 = [00000] by any
committed checker (unfixed convention); CONV-2 ores(s_v) = 0 is
declared, not derived, in the jet definition.

=> CONSEQUENCE: the comparison proof-style is now EMPTY of live
   candidates (marked polars closed model-independently; reset lane
   closed by the LOCK).  Unless one of A2/CONV-1/HYP-L is resolved in
   a way that changes the model, the keystone must come from the
   CERTIFICATE style (n=4 anatomy -> n=6 Wiedemann -> colour law).

## 25. N=8 SATURATION CENSUS: REDUCED TO TWO SCALAR CONFIGURATIONS
      (scratch sat8/; verify_saturation_census_n8.py, ledger 76f84119,
      3 mutations raise, unaudited)

NEW LEMMA H (empty-residue collapse, hand proof): if a saturating
family's residue V(F_a) = B is empty, then A_e(chi) = 0 for every
e in F_a -- Theorem C's saturating term vanishes and Cor. C1 holds
anyway.  NEW IDENTITY (dagger), the general-residue form:
A_e(chi)*h_a(R) = -sum_{r!=r'} A_ur(chi_u,a)A_vr'(chi_v,a)
h_a(R\{r,r'}); Lemma H is the R = empty case; verified nonvacuously
on the stall guard at both carriers.

EXHAUSTIVE CENSUS at N=8 (all ordered even splits, classified by
(k,|R|,t); dangerous iff |R| > 0): shape (0,2,6) excluded by the
drop-(0,2,6) UNSAT [step 2, unaudited scratch]; shape (0,4,4)
COMPLETELY CLEAN (all families empty-residue); shape (2,2,4) clean
UNLESS the saturating colour is the 4-part's colour, where exactly
TWO geometries survive:
  D1 (k=2,|R|=4,t=0): F_a a perfect matching between the 2-parts;
     harmful iff A_p1q1(b,c)A_p2q2(b,c) = A_p1p2(b,b)A_q1q2(c,c),
     both sides nonzero -- literally one source-block minor of
     unconditional-curvature-line-selection.md eq. (3) vanishing.
  D2 (k=3,|R|=2,t=2): one 2-to-2 edge + two S_a edges; harmful iff
     -A_f0 A_f1 A_f2 h_a(R) = h_0h_1h_2.
At N=10 every shape has dangerous configurations -- this is an
N=8-SPECIFIC gain.  Boolean route DEAD: SAT even with all of Lemma F
purity + pendant structure loaded (73548 vars, 6.1M clauses, SAT,
still SAT with D2 disabled).  The remaining content is SCALAR.
Stall-guard consistency required and verified (escapes purely on
residue count |R| = 2; the 9 defect words are NOT the mechanism).

=> MINIMAL OPEN STATEMENT AT N=8 (sharp): rule out D1 and D2 on an
   exact source with live split (4,2,2).  Everything else at N=8 is
   settled modulo auditing step 2.  Pipeline: audit + commit the
   census pair; attack D1 via the curvature-line-selection minor.

## 25b. K1 STAGE (a) VERDICT: PARTIAL — D1 DOES NOT DIE BY CITATION
       (scratch d1minor/confront.py; 136080 E3 instantiations
       enumerated exactly)

D1's minor IS an exact E3 instantiation of curvature eq. (3) in FORM
(sign-exact at (p1,q1,p2,q2; b,c,b,c), 4 symmetric variants, and
ONLY there — split-pinned, two-colour, no free site).  CITATION
FAILS on: (1) QUANTIFIER MISMATCH (the killer) — eq. (3) is
existential (SOME nonzero rectangle, possibly inside S_a on colours
involving a); D1 needs the PINNED rectangle {p1,q2}x{q1,p2} in
colours (b,c); (2) colour collision (centre colour = neighbour
colour, never controlled by the flat-branch machinery); (3) fan
hypotheses (>= 3 good neighbours not guaranteed at p1).
Entry-minimality is NOT an obstruction (bridges WLOG).  The note's
sec. 2 technique is inherently global (all-transitions-vanish
contradiction) — a single degenerate rectangle falsifies nothing.

NAMED BRIDGING LEMMA (what K1 now is): PINNED-RECTANGLE
NONDEGENERACY — exact source + live split (4,2,2) + F_a matching
the 2-parts + all four cells nonzero => the rectangle determinant
is nonzero; equivalently (pencil theorem) the transition
D_{q1,p2}^b(c,b) at centre p1 has nonzero (q2,c)-coefficient.
STAKES SHARPENED: D1 alone saturates the Boolean shadow (SAT with
all D2 disabled), so killing D2 alone does NOT collapse the census
— D1's pinned rectangle is the sharpest place an N=8 counterexample
could live.

## 25c. PINNED-RECTANGLE LEMMA: FREEDOM-SURVIVES LOCALLY — AND BOTH
       D1 AND D2 REDUCE TO THE SAME KERNEL OBJECT (scratch d1minor/
       pinned_rectangle.py)

The census fact set is PROVABLY INDEPENDENT of the D1 minor:
explicit exact rational witness on the canonical D1 geometry
satisfying EVERY census fact (Lemma F purity both subfamilies all
729 words each, residue purity all 81, E1 pendant sweeps, E2, dagger
nonvacuous at both carriers, liveness, corrected crossing sum = 0)
with a DEGENERATE rectangle (minor = 0, both products nonzero); a
one-parameter knob (A_23(c,c) = 1+t) keeps every fact and makes the
minor any prescribed value.  The minor is a FREE COORDINATE on the
local variety (harm locus = smooth codim-1); local system FEASIBLE,
dim >= 72.  STRUCTURAL REASON: every census fact evaluates a
sub-tensor omitting at least one carrier; the negative-product cells
appear in NO fact except liveness; no factorization couples the two
carriers.  => D1 cannot die by citation OR any local strengthening
of the committed lemma stack — it is a genuinely GLOBAL two-carrier
statement.

THE RESIDUAL, EXACTLY: any kill must consume an exactness word
impure at BOTH carriers simultaneously — minimal: the two-colour
words H_B(b,b,c,c,rho) = 0 (witness has H_B(b,b,c,c,a^4) = -58,
confirming independence from the fact set).  Under harm the
small-part skeleton locks to H_{p1p2q1q2}(b,b,c,c) = 2*A_02 A_13 +
A_03 A_12 — THE SAME OBJECT AS K2's D2 OPEN KERNEL (the mixed
small-part sum).  BOTH surviving configurations reduce to: does
exactness force the 4-site mixed tensor H_{S_b u S_c}(b,b,c,c) into
a fixed relation?  Next: the k2_step2-engine analogue over the D1
class with two-colour exactness words loaded (bounded,
engine-shaped) — merged into the K2 agent's run.

## 26a. K2 FIRST TEST: OBSTRUCTION PROVED ON EVERY SEARCHED CLASS —
       NO WITNESS, K2 SURVIVES (scratch k2test/, 5 scripts, 6
       mutations caught)

The witness search was excluded a priori at full N=6 exactness
(committed Theorem 1.1), so the content is mechanism: E3-purity +
sat != 0 ANNIHILATE every crossing contribution to two-colour
pair-inside words, collapsing them to inside-cell monomials the
anchors force nonzero.  Branch analysis (guard/mirror/exchange per
carrier side): (g,g),(m,m) => >= 6 forced defects (unit-monomial
pattern words); (g,m),(x,m),(x,x) => anchors identically zero;
(x,g) => {000000,111111,001001} core.  A 4-word family hits every
component.  Two-word cores C1 {000000,110000} (single monomials
differing in one cell) and C2 {002222,000000}.  WALL: the one
mechanical class-level composition (110 unknowns) needs a
triangular-decomposition backend (Maple/Singular scale) — hand case
analysis machine-verified per component.

BY-PRODUCT 1: NEW MAXIMAL GUARD 722/729 (ledger 86831209...) — the
committed 720/729 guard is NOT defect-minimal; 7 defects = 6
anchor-forced pattern words + 002222 (both repair routes cost >= 2
mixed-pair words).  Floor on the component is 6; 723/729 open.

BY-PRODUCT 2: N=8 D2 TRANSFER (identity level, proved) —
all-aligned components anchor-dead (STRONGER than N=6); the ONE new
freedom at D2: the 2-to-2 edge makes the small-part factor a
three-term sum H_0123 with exactly 3 surviving crossing-correction
matchings.  SHARPENED OPEN KERNEL of K2 at D2: can an exact-side
packet zero the mixed sums and use the 3 correction routes to
repair all pattern words while keeping anchors + live + sat?  THIS
IS WHERE K2 IS GENUINELY DECIDABLE (N=8 exactness is open) — and it
targets exactly the same D2 the census left standing.  Next: the
branch-elimination engine over the N=8 D2 class.

## 25d. UNIFIED ENGINE VERDICTS: D2 KILLED ON THE SWEPT CLASS;
       D1 PENDANT COMPONENT KILLED, FREEDOM LOCALIZED, GENERAL
       CLASS WALLED (scratch k2test/, d1d2_verdicts.json)

D2 KILLED (k2_step6_n8_d2_engine.py, census-mirrored geometry, all
216 branch combos): 54 anchor-dead, 108 by the Gamma-certificate
(unique two-colour partner at a residue site => H_B factors through
one cell; 3 words), 54 by the c-factor certificate (4 words), 0
SURVIVORS — the elimination engine was never needed.  MECHANISM
(pigeonhole): three carriers feed two residue sites => one site has
<= 1 two-colour feeder; 0 feeders kills anchors, 1 gives the
factorization certificate.  The H_0123-mixing freedom NEVER reaches
the killing words.  Certificates = anchors + <= 2 flip words, all
machine-verified polynomial identities, 3 mutations fail-safe.
CAVEATS: branch exhaustiveness is hand case analysis
(machine-verified per family, not one tree); orientation inert by
inspection.  => WITH THE CENSUS: D1 IS THE SOLE N=8 OBSTRUCTION,
modulo caveats + census audit.

D1: (i) control passed sharply — the witness violates EXACTLY the
18-word slice {0^8, 1^8} u {(....,2,2,2,2)}; any kill must consume
that slice + purity coupling.  (ii) NEW PENDANT KILL (proved): on
the witness's entire support class, H_B(c^8) = H_0123(c^4) *
H_Sa(c^4) identically and residue purity zeroes the second factor
=> both anchors identically violated (2-word certificate).  The
minor-freedom lives STRICTLY on supports with b/c-columns on
S_a-adjacent blocks where W1/W2 purities couple the carriers — the
K1 battleground, sharply localized.  (iii) WALL: general class 220
unknowns / 1488 equations; branch elimination does not terminate.
Deciding backends: regular-chains / multi-prime CRT elimination /
or (best) branch classification of the 3-term 4-site
residue-purity system (pure-tensor U-system analogue) enabling the
same staged sweep that killed D2.

## 25g. D1 DP-STRATUM FULLY KILLED; RESIDUAL CELL PINNED (scratch
       k2test/ steps 8-9; d1_dp_sweep_full.log)

LEMMA R3 (machine): det(a@b + c@d) == 0 => any inside pair-block
with det != 0 forces its partner PURE and reduces to the classified
D2 U-system.  SLOT DICHOTOMY (machine): per-slot U-system splits by
the dagger-entry s_slot — ND locus {g,x,d} vs degenerate locus
{e,f,D} (s_slot == 0 identically).
*** CORRECTION 2026-08-04 (D2-pair audit, primary): the claim that
this "retroactively completes the D2 caveat" is FALSE.  The auditor
constructed THREE explicit U-system solution families satisfying
saturation and lying outside all six swept families (x6/x7 with free
a-column at A_{u,keep}(.,a); d6/d7 with free a-column; all four
residue-adjacent blocks a-column-only).  The six-family list is NOT
complete under saturation.  What IS true and machine-verified by the
auditor: the realizable two-colour COLUMN SIGNATURES are exactly
seven (empty plus the six family signatures), 28350 saturated
solutions enumerated over GF(2), zero of which feed two-colour cells
into both residue sites — that is the sound form of the reduction.
AND the conclusion survives: the auditor's own extended 8^3 = 512
combination sweep gives 128 anchor-dead / 192 Gamma / 192 c-factor,
ZERO SURVIVORS.  So D2 remains killed, on a BROADER family set than
the artifact swept; only the completeness JUSTIFICATION was wrong.  HARM ENTRY (machine): harm forces >= 1 ND
slot per carrier — the only place harm is consumed.

SWEEP: ND locus 1296 combos ALL die harm-free (1100 anchor-dead,
196 by 3-word certificates); degenerate locus census over ALL 9216
harm-legal combos: 5184 anchor-dead, 2688 Gamma, 768 c-factor, 576
D-factor, 0 UNRESOLVED.  New certificates machine-verified
(singleton-f Gamma, singleton-D beta-factor, D-slot
pair-cancellation, product-split word).  cxhunt integration: every
certificate anchors on the MONOCHROMATIC PAIR (confirming the
corrected aim); stage-A family and d1minor witness both behave
exactly as controls require.

SURVIVING D1 CELL, EXACTLY: (non-DP inside strata) x (out-of-Sigma
crossing supports).  Staged tools: R3 reduces every
invertible-pair-block stratum; frontier = the all-singular non-DP
inside classification (e.g. the all-six-(a,a) diagonal).  Heavy
backends STILL not needed.  Caveats: {g,x,d,e,f,D} completeness is
hand case analysis (memberships + dichotomy machine-verified).

## 25h. D1 R3-STRATUM: REDUCTION PROVED, MINIMAL ESCAPE KILLED, NEW
       COUPLING LEMMA (GCL) BUILT; ALL-SINGULAR UNCLASSIFIED —
       ENDGAME THEOREM NOT REACHED (scratch k2test/ steps 10-11)

R3 machine-verified in 3-row form: an inside pair-block with
det != 0 forces its partner pure and reduces the four cross blocks
to the (S) U-system; all six families re-verified; the
(2,2)-equation splits mu != 0 (N determined) / mu = 0 (rank-2 E22
representation, instance realized).  HONEST NEGATIVE: on R3
supports A_67 carries two-colour cells, so residue sites pair
INSIDE at two-colour words — anchors gain 2-crossing routes, no
single-feeder factorization at sites 6,7, and the DP pigeonhole
(feeds <= 6 < 8) NO LONGER BOUNDS.  The site-local battery does not
transfer, and the agent did not pretend it did.

MINIMAL OUT-OF-SIGMA R3 SUPPORT KILLED, two independent machine
routes: (E2) all 256 two-colour words = A_01*A_24*A_35*A_67 with
every factor a unit under anchors => word 00000011 is a forced
defect (C4-style unit forcing PERSISTS on R3); (E1') the 16 purity
words (w0,w2,a,a,c,c) = A_67(c,c)*s_45 => anchors force s_45 = 0 =>
by the harm-entry identity A_02(chi) = 0, HARM FAILS.

NEW GENERAL TOOL — GCL (k2_step11_coupling.py, 18 generic
identities): the 6-site purity word splits as
A_67(c,c)[A_02*A_45 + s_45] + A_02[cross-pair] + X(small-into-{6,7}),
all three groups generically nonzero.  This converts "a residue
pair-block carries a two-colour cell" — the DEFINING feature of
every non-DP stratum — into an equation on the other pair's
crossing data.  E1' is its first corollary; GCL is the systematic
attack on the frontier.

ALL-SINGULAR NON-DP: nonempty (explicit exact instance, all six
blocks singular, full 81-word residue purity) but NOT CLASSIFIED —
it has NO nondegeneracy direction (DP branched on s_slot != 0, R3
on det != 0; here every case is a rank-<=2 compensation among six
blocks and the tree opens without a terminating invariant).

=> ENDGAME THEOREM NOT REACHED.  Chain complete through census ->
   D2 -> D1 on Sigma -> D1 DP (0 unresolved) -> R3 reduction + GCL
   + minimal-escape kill.  SURVIVING OBJECT (sharpest in the lane):
   the COUPLED CROSSING SYSTEM on non-DP inside strata off Sigma.
   Five caveats enumerated in d1d2_verdicts.json.  Next: GCL-driven
   frontier attack; heavy backends still not warranted.

## 25i. GCI GENERALIZED + RANK FORCING + THE COLOUR-DEFICIT
       MECHANISM — the frontier is no longer a case tree
       (scratch k2test/ steps 12-17)

GCI (PROVED, machine, 1458 generic identities, both carriers, all
3^6 words, NO stratum hypothesis):
  H_W(w_u,w_p,k) = A_up(w_u,w_p)*Hf(M(k)) + X(k) M*(k) Y(k)^T
with M(k)_{rr'} = A_{rr'}(k_r,k_r'), Hf the residue purity form, M*
the complement swap.  Step-11's GCL = the single case k=(a,a,c,c)
(18 of the 1458).  Consequences, each machine-checked: C1 GENERAL
HARM-ENTRY nu_R*A_up(w) = -sum over six pairs P of A_{P'}(a,a)*s_P(w)
(step-9's V3 is its DP case, reproduced exactly) => harm needs, per
carrier, a pair P with A_{P'}(a,a) != 0 AND s_P(chi) != 0; C2-I/C2-X
interior and a-cross probes (single-term forcing with every
correction carrying an off-Sigma column); C3 GCI-KILL (an impure
block kills its OWN harm weight; E1' is the P={6,7} case); C4 on
Sigma every correction vanishes => an INDEPENDENT second route to
the Sigma death, inert on all-pure residues (which is why the
controls survive).

TWO NEW LEMMAS (PROVED, machine) converting the frontier to RANK
CONDITIONS: AX (an a-cross-supported 3x3 has det 0, so det A_Q != 0
forces an interior two-colour cell; interior-pure blocks are exactly
the singular ones); RF (purity at all nine (w_u,w_p) IS the matrix
equation X(k) M*(k) Y(k)^T = 0; as a linear map on the 6-dim
hollow-symmetric space with E1 zeroing X's a-row it is INJECTIVE for
generic crossing data, forcing M(k) = 0 — all six residue blocks
vanish).  => RESIDUE IMPURITY (non-DP) FORCES CROSSING DEGENERACY AT
THE NON-a SITES: the exact quantified trade-off between being non-DP
and being off Sigma.  Full 256-pattern kernel classification
computed exactly.

COVERAGE (searched, support-level, conservative): over the 2^16
column patterns the 8 rank-forcing words see, 28672 are harm-shaped
and RANK FORCING KILLS 12375 (43.2%) OUTRIGHT by forcing
A_{P'}(a,a) = 0.

THE RESIDUE, CONCRETE: for the all-singular non-DP instance the
2x729 system reduces to the carrier definition plus 336 BILINEAR
equations, bilinear between pendant {0,1} and partner {2,3}, with
THE TWO CARRIERS DECOUPLED (168+168; 24+36 unknowns) — harm is a
LINEAR FUNCTIONAL ON THE PARTNER NULLSPACE, so harm-attainability is
an exact rank test.  An exact off-Sigma all-singular non-DP packet
was built with every census fact verified and HARM REALIZED
(A_02(chi) = A_13(chi) = -1, minor 0, products nonzero) — and on it
BOTH COLOUR ANCHORS VANISH IDENTICALLY.  Over 40 exact harm-carrying
solutions combined into 1600 packets: b-anchor alive 24, c-anchor
alive 0, both alive 0.

*** COLOUR-DEFICIT: REFUTED (2026-08-04, k2_step18_colour_deficit.py)
— it is NOT a terminating invariant.  TWO INDEPENDENT GAPS, both
machine-established, both with passing discrimination probes:
GAP 1 (the antecedent is never delivered): a case-(C) anchor
realization matches the four residue sites to four DISTINCT small
sites, two pendant and two partner, so per carrier the anchor forces
exactly ONE live X-column and ONE live Y-column.  All 12 such
patterns have kernel dim 5 with 5 of 6 pair-entries free —
essentially no rank forcing.  The lemma needed 4+4 live columns; the
anchor delivers 1+1.
GAP 2 (MONOTONICITY IS FALSE — the deeper finding): of 1024
single-column enlargements of the 256-pattern table, 151 turn a
FORCED-ZERO residue entry FREE.  Fully symbolic witnesses (no
sampling): x=(0,0,1,1),y=(0,1,0,0) forces pairs (1,2),(1,3) zero,
while x=(1,0,1,1),y=(0,1,0,0) makes all six free.  Reason: one live
Y-column gives sum_r X[i][r] M[r][1] = 0; two live X-columns = 2
equations in 2 unknowns (forced zero), three = 2 equations in 3
unknowns (1-dim space, all nonzero).  AN EXTRA OFF-SIGMA COLUMN
CANCELS RANK FORCING INSTEAD OF STRENGTHENING IT.  So minimal-
liveness censuses are INVALID as non-existence arguments, and
injectivity is GENERIC-ONLY, never universal.
CONSEQUENCE: the step-17 24-vs-0 anchor asymmetry is now UNEXPLAINED
— it must not be cited as proved or as mechanistically understood.
Any future D1 kill must be stated PATTERN-BY-PATTERN (or handle the
151 cancellation enlargements explicitly); no feeding-vs-forcing
trade-off argument can work.
SURVIVING AND UNAFFECTED: GCI (1458 identities), C1-C4, Lemma AX,
and Lemma RF IN ITS CORRECT SCOPE (per-word, per-pattern, generic:
for fixed k, if all eight columns of X(k), Y(k) are live and generic
then Phi_k is injective and M(k) = 0).  RF does NOT compose into a
dichotomy.

*** (SUPERSEDED — the original conjecture, kept for the record)  A residue configuration
can supply an INSIDE feed for the anchor of colour gamma only if
some pair-block has a (gamma,gamma) interior cell.  For the other
colour the anchor must be FULLY CROSSING-FED (all four residue sites
gamma-crossed from four distinct small sites) — and that full column
liveness is exactly what makes Phi_k INJECTIVE at the corresponding
probes, forcing the residue's impure cells and the harm weights to
zero.  The observed 24-vs-0 asymmetry IS this mechanism (A_45 has a
(b,b) cell, no (c,c) cell).  KERNEL CORANK OF Phi_k DECREASES AS
CROSSING FEEDING INCREASES, AND THE ANCHOR PAIR DEMANDS FEEDING IN
BOTH COLOURS. ***

STATUS: endgame theorem NOT reached; conjecture OPEN.  But the
frontier is no longer an open case tree — it is ONE MECHANISM TO
PROVE.  Kill chain: census -> D2 -> D1 on Sigma -> D1 DP (0
unresolved) -> R3 + GCI -> 43.2% of harm-shaped patterns by rank
forcing -> the all-singular residue, searched and anchor-dead.
NEXT: prove the colour-deficit lemma (bounded symbolic job,
determinantal statement readable off the 256-pattern table, then
combine with C1).  Caveats in d1d2_verdicts.json: step-14 is
support-level; step-17 is searched over 1600 packets of ONE residue
instance, not proved; all-singular still not classified.

## 29. THE LANDING (SP-CLEAN-BRIDGE) SCOPED — and it is NOT a step
      (scratch landing/probe_cap_factorization.py, 2.4 s, exact)

*** THE CORRECTION THAT MATTERS: SP-CLEAN-BRIDGE AT N=8 IS PROVABLY
EQUIVALENT TO (8,3) BEING EMPTY. *** clean-bridge-at-eight-is-the-
open-case.md sec. 2 proves (given certified SP-K6) that no
eight-vertex exact source admits an active clean cap at ANY pair for
ANY K.  So the bridge holds at N=8 IFF the case is empty.  THERE IS
NO VERSION OF THIS STEP WITH A WEAKER ANTECEDENT: any "candidate
argument for the landing" is a candidate argument for the whole
case.  A reformulation can only change WHICH STRUCTURE A PROOF MAY
CONSUME, never lower the logical bar.  Nobody should be sold a
"lemma" here.

ALSO CORRECTED: H5 (a free nonzero curvature rectangle at the
crossing pair) IS NOT DELIVERED BY THE PILLAR.  The repo proves two
different things: unconditionally (exact-source-live-split-forcing
sec. 8) only the FIRST product A_pq A_rs != 0 ("the minor vanishes
only through an exact coincidence", not a proof it doesn't); and
separately curvature eq. (3) gives an EXISTENTIAL nonzero minor at a
FAN CENTRE with >= 3 good neighbours and FREE colours — not pinned
to a crossing pair nor to the split colours.  Same quantifier
mismatch as section 25b.  H5 must be carried as a hypothesis or
purchased with the fan hypothesis.

THREE NEW FACTS (proved; FACT 1 apparently unrecorded):
* FACT 1 — ACTIVITY COLLAPSES ON A CROSSING LINE.  p,q in different
  parts => alpha != beta => the cap line is OFF-DIAGONAL,
  K_z = E_{alpha beta} + zI, so kappa_c(K_z) = z FOR ALL THREE
  COLOURS.  The inactive locus is {0} u {s-roots}: at most two
  points, and 0 is not an s-root by H2.  CONSEQUENCES: the
  frontier's LABEL SPLIT DISAPPEARS (a != b is free); the whole
  intrinsic-block/diagonal-line branch is OUT OF SCOPE at a crossing
  pair; the descent rescaling is colour-uniform (a single scalar);
  and >= 3 distinct common roots suffice for an active clean point.
* FACT 2 — THE CLEAN ERROR FACTORS (machine-verified, 3 caps x 729
  coordinates, nonvacuous): 3 E_{p,q}(K) = sum_{{a,b} in U}
  haf_R(U\{a,b}) tensor (3s A_ab + R_ab).  This puts the error in
  the program's WINNING SHAPE and makes r^2 = 0 a QUADRATIC
  sufficient condition where E is CUBIC.
* FACT 3 — CLEANLINESS AND ACTIVITY PULL OPPOSITE WAYS.  For
  rank-one K = phi (x) psi, r(K) = u~ . v~, so r = 0 iff phi or psi
  is in a star kernel.  At a BAD pair the cap is clean for free but
  Lemma E makes phi single-coloured => INACTIVE (the one-line reason
  the chi=0 landing is inactive).  At a GOOD pair K -> r(K) is
  INJECTIVE (measured rank 9/9; planting a 1-dim star kernel drops
  it to exactly 6 = 9-3).
  ONE SENTENCE FOR THE DIFFICULTY: cleanliness comes from star
  DEGENERACY, activity from star NONDEGENERACY, and the good
  crossing pair buys activity by paying with the error.  That is WHY
  the bridge is equivalent to the case.

PROBE RESULTS: P3 IS A GENUINE SURPRISE — the 1215 quadrics [r^2]_S
span only 36 of 45 dimensions in Sym^2(C^9), reproduced on two
independent packets, i.e. (K,K') -> r(K).r(K') has a 9-DIMENSIONAL
KERNEL on generic data (9 = dim K, suggestive and UNEXPLAINED).
That is exactly the input C1 needs.  P5 baseline: the 729 coordinate
cubics are GENERICALLY ROOTLESS (gcd degree 0) — so nothing about
the landing is generic, and anyone proposing to find a root must
say what exactness supplies.

*** C1 VERDICT: DEAD — and the 9-dim deficit is IDENTIFIED AND
PROVED (scratch landing/decide_square_zero.py,
activity_on_square_zero.py). ***
THE KERNEL: with U_i = sum_{a in U} A_pa(i,.) and W_j = sum_{a in U}
A_qa(j,.), r(K) = sum_ij K_ij U_i W_j (identity I1, machine-verified
nonvacuously).  The algebra is COMMUTATIVE, so
r(K) r(K') = sum K_ij K'_kl (U_i U_k)(W_j W_l) is symmetric in
(i,k) and (j,l) SEPARATELY => the map factors through
Sym^2(V_p*) (x) Sym^2(V_q*) (dim 36) and ANNIHILATES
Lambda^2(V_p*) (x) Lambda^2(V_q*) (dim 9) = the measured 45-36.
r(K)^2 IS BLIND TO Lambda^2 K, the 2x2 minors of the cap.  Sharp
form: r(K)^2 = Phi(sigma(K)) with sigma the PERMANENTAL square
(sigma_{(cc),(dd)} = K_cd^2), so Phi injective => {r^2=0} = {0}.
DISCRIMINATION PROBE CAUGHT A REAL ERROR: the first run reported an
empty variety because the harness silently tested the wrong pair;
POS1 (planted star kernel) failed and exposed it.  After the fix,
POS1 rank 18/36 nonempty, POS2 (concentrated star image, r != 0 but
r^2 = 0 at a GOOD pair) rank 30/36 with an explicit witness.
EMPTINESS: nonempty on both near-source packets (omega guard rank
9-11; D1 witness 25/30/30/36) — C1 survived stage 1.
ACTIVITY: DECIDED NEGATIVELY.  ker(K -> r) is EMPTY at every good
pair (FACT 3 confirmed); concentrated rank-one caps exist but the
concentrated covector is ALWAYS a coordinate vector e_c; and on the
D1 witness at ALL FOUR good pairs at least two of the three kappa_c
are forced zero on the variety, so EVERY POINT IS INACTIVE — robust
across knob values t in {0, 3, -1/2}.
FACT 4 (NEW LEMMA; hand proof for exact sources + 12 nonvacuous
machine instances, all with r(K) != 0 so cleanliness is genuine
cancellation): let {p,v} be a BAD pair with v essential at p of
essential colour a, and q any site with v in U.  Then for EVERY psi
the cap K = e_a (x) psi has E_{p,q}(K) = 0 and is INACTIVE
(kappa_c = 0 for c != a).  Proof: (E1) kills row a of A_px for
x not in {p,v}, so U_a is supported at the single site v, U_a^2 = 0,
r^2 = 0, E = 0 by the factorization; Lemma E makes the essential
covector single-coloured.  This converts the folklore "the chi=0
landing is inactive" into a GENERAL LEMMA AT GOOD PAIRS.
WHY C1 DIES: the square-zero variety at a good pair is populated by
clean caps INHERITED FROM THE PACKET'S BAD PAIRS, which carry those
pairs' single-colouredness and hence inactivity.  Cleanliness is
always purchased with a degeneracy, every available degeneracy is
single-coloured by Lemma E, and single-coloured means inactive —
the r^2 = 0 route does not escape that tension, IT IS IT.
=> RETIRE C5 as well (its first test has now effectively run twice:
   gcd degree 0 generically, and FACT 4 showing the only
   structurally forced clean caps are inactive).  Any usable cap
   covector must NOT be single-coloured at p — which puts the search
   back on FACT 1's crossing line K_z = E_{alpha beta} + zI, z != 0,
   and nowhere else.
RESIDUALS: no packet is an exact source (FACT 4 is proved for exact
sources by hand; the rest is measured); the omega guard is
OFF-HYPOTHESIS for LANDING-8 (diagonal => no nonzero crossing
matching) and its variety is NOT fully decided — do not quote it as
decided; the D1 witness has only four good pairs.

FIVE CANDIDATES, ranked by (P(survive first test)) x value / cost:
C1 SQUARE-ZERO CAP (r^2 = 0; quadratic vs cubic; the only candidate
   strictly cheaper than the target; ~0.25 survival) — TEST
   DISPATCHED;
C3 IMPORT SPARSIFICATION / 3-CUT (the one successful factorization
   shape never imported here; half a day of reading; ~0.15);
C5 DEGREE COUNT ON THE CROSSING LINE (minutes; almost certainly
   circular, but its NEGATIVE retires the whole "roots exist"
   branch);
C2 ROOTLESS SOURCE-PROVENANT CERTIFICATE (~0.6 survival but cost =
   the target itself; must first clear K5's filtration-depth
   verdict);
C4 ODD-CUT / TUTTE-BERGE (H_{B\{p,q}} is not a source, so an
   anchor-free identity has nothing to strand).

*** C3 / 4-CUT VERDICT: CLOSED — and the THREE DEAD ROUTES SHARE ONE
MECHANISM (scratch landing/four_cut_landing.py, 0.13 s). ***
T1 (verified, 729 words, 596 nonzero) — the per-colour error in
closed form: E^c(p) = 2 [z^2] prod_{i=0}^{3} (b_i(p_i) + z a_i(p_i)),
so "E^c == 0" is a statement about the z^2-coefficient of a product
of four linear binomials.  Slot-splitting classifies it: all four
slots proportional with e_2(lambda) = 0; or one free slot and three
proportional with e_1 = e_2 = 0 (needs cube roots of unity; over Q
degenerates).  Two free slots impossible.
T2 (proved) — because S is a cut, s has no neighbour outside
S u {t}, so the deleted endpoint star IS the S-star: "the colour-c
cut star vanishes" <=> {s,t} is a BAD pair.  Goodness therefore
excludes the ZERO-STAR branch outright (the exact analogue of
FACT 3).  T3 (measured, with discrimination) — but NOT the
proportional branch: generic good-cut data gives rank 6/6 forcing
gamma = 0, while a designed proportional instance with e_2 = 0 gives
rank 0/6 (E ~ 0) AT A GOOD PAIR.  So goodness is not the operative
filter.
DECISIVE UPSTREAM POINT: LANDING-8 constrains the CROSSING pair
{p,q}, not the CUT pair {s,t}, and supplies NO CUT.  Exhaustive
shapes for an 8->6 descent: |S|=2 excluded by MFCS 1.9
(3-connectivity); |S|=3 is the published d-free Thm 1.10 but our
live objects are 4-CONNECTED with no 3-cuts; |S|=4 is the new
reduction.  T4 (measured): the D1 witness has exactly TWO (2,4,2)
cuts, both existing only because its residue is the DP stratum —
ALREADY FULLY KILLED (25g) — while the surviving all-singular
non-DP frontier has a complete residue skeleton and hence NO (2,4,2)
cut at all (generic packet: 0).  WHERE THE TOOL APPLIES THE OBJECT
IS DEAD; WHERE THE OBJECT IS ALIVE THERE IS NO CUT.
AND even on the D1 cuts the reduction self-contradicts: the 81
E-forms span RANK 1 with E(gamma) = -4 gamma_a^2, so E == 0 <=>
gamma_a = 0, while lambda = A_st(c,c) is supported on colour a
alone so the normalization Lambda(gamma) = 1 FORCES gamma_a = 1.
Exhaustive gamma-grid: no admissible gamma.
R3/GCI do NOT decide it and are the wrong tools: E = 0 is a
STAR-PROPORTIONALITY (support/genericity) condition, and exactness
constrains hafnian VALUES, not star proportionality.
BY-PRODUCT (cheap new filter, kills nothing open): no exact source
on 8 sites has a (2,4,2) cut admitting gamma with E(gamma) = 0,
Lambda(gamma) = 1 and all gamma_c != 0.

*** THE UNIFYING MECHANISM — three independent confirmations ***
  clean cap at a bad pair (FACT 3): needs a STAR KERNEL; Lemma E
    makes the essential covector single-coloured => inactive.
  C1 square-zero cap (FACT 4): needs a star image CONCENTRATED at
    one site; (E1) supplies exactly that from the packet's BAD
    pairs, covector again e_a => inactive.
  4-cut reduction: needs four vanishing blocks + E = 0; Lemma F
    purity puts lambda AND the cut stars on colour a alone =>
    Lambda needs gamma_a != 0 while E needs gamma_a = 0.
  A DESCENT NEEDS A DEGENERACY.  EVERY DEGENERACY AN EXACT SOURCE
  ACTUALLY HAS IS SINGLE-COLOURED — THAT IS PRECISELY WHAT LEMMAS E
  AND F SAY.  AND SINGLE-COLOUREDNESS IS EXACTLY WHAT THE ACTIVITY /
  NORMALIZATION CONDITION FORBIDS.
=> The landing is NOT blocked by a missing computation; it is
   blocked by one recurring mechanism, and enumerating further
   degeneracy-consuming candidates will keep reproducing it.  This
   is the CONCRETE CONTENT behind clean-bridge sec. 2's abstract
   equivalence: the bridge is equivalent to the case because every
   route to it asks the source to be degenerate, and a source that
   degenerate is already reducible.
=> THE NEXT MOVE MUST BE A MECHANISM THAT DOES NOT ASK FOR A
   DEGENERACY.  C2 is the ONLY surviving candidate with that
   property: it consumes the ABSENCE of roots (gcd = 1, measured)
   rather than the presence of a degeneracy, and FACT 2's
   factorization is the shape its certificate must take.  ITS NAMED
   RISK IS K5's FILTRATION-DEPTH VERDICT — test that first.  If C2
   fails at the same layer, THE HONEST REPORT IS THAT THE LANDING
   HAS NO KNOWN MECHANISM AT ALL, and saying so would itself be the
   most useful thing the program can state about SP-CLEAN-BRIDGE.

*** C2 VERDICT: DEAD — and the landing now has NO KNOWN MECHANISM
(scratch c2test/, 4 probes with discrimination, nothing committed).
***
THE CERTIFICATE EXISTS: rootless on 14/14 instances (D1 witness at
t in {0,3,-1/2} x both good crossing pairs (0,3),(1,2), plus two
generic packets x four pairs), with an explicit exact Bezout
certificate deg c_w = 2 verified by direct expansion.
NEW CLOSED-FORM BRIDGE IDENTITY (machine-verified, all 729 words):
  E_w = D_w - s^2 (K contracted H_B)_w,  D_w = s^3 H_U(y)_w.
Exactness controls (K.H_B)_w — it replaces it by kappa(w) — and says
NOTHING about D_w, which IS the descended six-site hafnian, i.e. the
object the descent is trying to produce.  Measured: <c,kappa> = 0
exactly (the certificate's support words are impure), so on an exact
source the whole unit would have to be carried by <c,D>.  CIRCULAR.
MEMBERSHIP FAILS, BY GRADING, UNIFORMLY (proved, not measured):
E_{p,q}(K) is multihomogeneous of site-multidegree (3,3,1^6) with
A_pq-degree <= 1; the literal exactness rows have degree-4 part of
type (1^8) and A_pq-degree <= 1; the only block inside {p,q} is
A_pq, so EVERY multidegree-matched multiplier has A_pq-degree
EXACTLY 2.  The matched span lies in A_pq-valuation >= 2 and the
E-family in A_pq-degree <= 1: THEY MEET ONLY AT 0.  Test: valuation
0 on 14/14; designed members (valuation 2) ACCEPTED; designed
non-member rejected; planted degree-2 term detected; corrupted
certificate rejected.
STRONGER, FULLY PROVED: holding c fixed, sum_w c_w E_w is a DEGREE-6
FORM of the packet (global rescale A -> tA multiplies it by t^6,
verified t = 2,3,-1,1/2), so the "1" is not a unit but one value of
a form attained on a hypersurface; and since every E_w vanishes at
the zero packet, the ideal (E_w : w) lies inside the irrelevant
ideal and CONTAINS NO NONZERO CONSTANT AT ANY PACKET.  There is no
polynomial-coefficient Bezout certificate for a unit, ever.
HONEST NUANCE (the test was not rigged): the naive WORD-LEVEL type
check PASSES — the certificate's 2-word support is contained in the
8 live capped rows on both crossing pairs — so C2 is NOT killed by
surface K5-style word disjointness; it dies one layer down, at the
multidegree/filtration layer.  Residual loophole recorded so nobody
re-finds it: multipliers of type (3,3,1^6) hitting the CONSTANT
Delta_W of the pure-word rows make membership trivially true and
contradiction-free — it re-expresses rather than derives.

*** CORRECTED ACCOUNTING: FIVE DEAD CANDIDATES, TWO MECHANISMS. ***
C2 does NOT die of the degeneracy mechanism (it never asks for a
degeneracy).  The honest split:
  DEGENERACY / SINGLE-COLOUREDNESS (Lemmas E,F vs activity) — three
    deaths: bad-pair clean cap, C1 square-zero, C3/4-cut.
  FILTRATION DEPTH / GRADING DISJOINTNESS — two deaths: K5, C2.
No candidate survives either.
=> REPO POSITION ON SP-CLEAN-BRIDGE: it is provably equivalent to
   (8,3) being empty, and AS OF NOW THE LANDING HAS NO KNOWN
   MECHANISM.  Every descent-style route asks the source for a
   degeneracy that Lemmas E/F make single-coloured and hence
   inactive; every certificate-style route puts the needed unit in a
   filtration layer the available rows cannot reach.  Enumerating
   further candidates of either shape will keep reproducing one of
   these two.
=> WHAT IS STILL OUTSIDE BOTH OBSTRUCTIONS (coordinator's note):
   (i) LIMIT DEGENERATION (M1) — it CREATES a degeneracy by taking a
   limit in the 21-dim gauge torus's orbit closure rather than
   consuming one, so the first mechanism does not apply; its own
   test (is the frontier support already terminal?) is pending.
   (ii) FULL NULLSTELLENSATZ OVER F_p (M2) — the C2 death is about
   the CAP family's ideal (E_w : w), which provably contains no
   constant; the FULL exactness ideal contains 1 exactly if the
   theorem is true, so the grading obstruction does not transfer.
   (iii) SCALING THE PROVEN n=6 METHOD — a stratum audit, not a
   mechanism; strata count pending.

HOPELESS (do not re-derive): any route hoping the cubics acquire a
common root (measured rootless; clean-bridge sec. 3 already prices
"E(I)=0 with tau != 0" as vacuously true or equivalent to
emptiness); DOUBLE DESCENT 8->4 (N=4 HAS an exact source — every
landing must stop at 6; two good crossing edges are two CHARTS, not
two descents); Boolean/support routes; anything comparison-type;
Component III certificates; reaching r^2=0 by support concentration
(that is the near-pendant degeneracy goodness exists to exclude).

## 25j. K4 SWEEP COMPLETE — THE REFORMULATED K4 IS FALSE; a
       FACE-SIZE DICHOTOMY survives (scratch k4prop/)

Classes (chart-stabilizer orbits, the face-correct equivalence):
1427 chart 26, 738 chart 25.  Certified <= 5 rows: 246 + 158 (rows
164/45/18/2/17 and 62/56/39/0/1; 159 + 104 needed the WIDENED
vocabulary).  THE DISCRIMINATOR IS FACE SIZE, NOT ADMISSIBILITY:
every certified class has <= 15 influence directions and NOT ONE of
the ~1500 classes with >= 21 certified.  Clean cap <=> dirs in
{0,3,6,9,12}; dirs 0/3/9 certify 100%.
OBSTRUCTION: five classes with CLEAN ACTIVE CAP, exactly 12
directions and colour class 0101010 exhaust the UNTRUNCATED search
(cap 1e6, to 275k nodes, no timeout) with NO <= 5-row certificate —
and TWO are FULLY ADMISSIBLE with 16-17 surviving pure-anchor rows.
So admissibility + pure-anchor survival + clean cap DO NOT imply a
small certificate.  dirs=12 certify 0/18; dirs <= 9 certify 310/310.
LIMITS: "not found" is searched-in-a-stated-space, not a decision
procedure; 7 classes flipped at 10x budget so 714+388 timeouts are
unresolved; and THE EXPOSURE/ADMISSIBILITY FIGURES ARE NOT
INDEPENDENTLY REPRODUCED (verifier deep-chain convention mismatch) —
sweep-internal only.  Certificates ARE verified: 404 re-checked
exactly over Z, frozen verify_certs.py (a696560b...), 3 mutations
raise under python3 and -O.
=> K4 DEAD AS STATED; survives only as a face-size dichotomy whose
   usefulness depends on which direction-counts the spine's terminal
   Hamilton rows carry — dispatched.

VERDICT: K4 IS DEAD FOR THE PROGRAM'S PURPOSES (scratch k4decide/).
The spine's obligation set is EVERY terminal Hamilton row minus
those whose endpoint pair carries no support edge = 10748 rows
(5388 of 5596 chart 26 + 5360 of 5648 chart 25); it is NOT one per
orbit (the sweep's orbits are chart-stabilizer orbits, so orbit
coverage = row coverage).  Coverage: 1804/10748 = 16.8%.  82.4% of
needed rows sit at >= 21 directions where the machinery has produced
NOTHING.
CORRECTION TO THE EARLIER FRAMING: the dichotomy is NOT MONOTONE in
face size.  Among needed rows dirs 14 and 15 certify 344/344 while
dirs 12 certifies 0/74 — a TWO-SIDED EXCLUSION of 12, not a
threshold ({0,3,6,9,14,15} certify; {12} never; {>=21} never).
Also cap_clean <=> dirs <= 12 exactly, in both charts.
NEW, MACHINE-ESTABLISHED: THE WALL IS CHART-RELATIVE, NOT INTRINSIC.
All 14 refuted 12-direction classes and all 1153 >=21-direction
classes of chart 26 are explicit S8xS3 IMAGES OF CERTIFIED
<=9-DIRECTION CLASSES (chart 25: 567/580), each transported
coordinate-by-coordinate; every such element moves the chart
support.  Chart 25's and chart 26's supports lie in DIFFERENT S8xS3
orbits (checked over all 241920 elements), so there are >= 2 chart
types.  Only two bare-row types (0121012, 0101212; 80 rows, chart 25
only) never certify anywhere.  The direction count therefore
measures the row's POSITION RELATIVE TO THE CHART SUPPORT, not the
row.  Not a rescue (the straightening runs inside one fixed chart
and cannot re-chart mid-argument) but it changes what a proof must
do.
*** CAVEAT THAT COULD OVERTURN THE VERDICT: the sweep's cap policy
searches only (+-1)^9 SIGN CAPS, so for rows with no clean sign cap
it measures directions against a DIRTY cap — the >= 21 wall may be
partly a MEASUREMENT ARTIFACT.  Re-measurement with general caps
DISPATCHED; until it lands, "K4 is dead" is provisional. ***
Also provisional: the "untruncated search" claim for the two
chart-26 12-direction refutations depends on their "0 unexposed"
figure, which comes from the exposure data the predecessor's own
verifier FAILED to reproduce (deep identity failed for 117,
exposure_reverified: 0).  The 404 certificates themselves ARE
re-verified over Z.

## 25k. D1 DIRECT ATTACK: THE CELL IS FEASIBLE — D1 IS NOT KILLED
       (scratch k2test/k2_step19*..k2_step26_sos.py)

*** THE STRUCTURAL PILLAR DOES NOT CLOSE. *** An EXACT point on the
surviving cell was CONSTRUCTED carrying HARM AND BOTH COLOUR ANCHORS
SIMULTANEOUSLY: harm A_02(b,c) = A_13(b,c) = -1 (minor degenerate,
both products nonzero), H_B(b^8) = -S23_00 (free small symbol, not
identically zero), H_B(c^8) = -4.  Every census fact re-verified on
the built packet (1440 rank-forcing probes, 2x729 W-purity, 81
residue words, E1 pendant rows, off Sigma, all-singular, non-DP, 25
small blocks fully symbolic).
=> THE step-17 IMPRESSION (b 24 / c 0 / both 0 of 1600) WAS A
   SAMPLING ARTIFACT: solutions live on a CORRELATED subvariety
   (q ~ lambda p); uncorrelated sparse sampling finds 0 in 200000
   draws where a hand-derived normal form finds them immediately.
   ALSO: step 17's anchor_poly is WRONG FOR COLOUR b (it zeroes the
   carrier pairs when they sit inside a matching) — the "24 b-anchors
   alive" statistic was computed with a TRUNCATED anchor.

NOT A COUNTEREXAMPLE: the point fails full exactness.  Of 6561
words, 5937 hold identically, 540 affine-linear + 36 quadratic in
the 25 small unknowns, and 48 are NONZERO CONSTANTS with no
small-block freedom (defects -4/-2, including c^8 itself) — these
survive any diagonal gauge, so the point is dead up to gauge.  Over
53 exact sampled points of the harm+anchors locus the minimum was
123 unrepairable words; 15 fail at EVERY point.

NEW PROVED STRUCTURE (machine, symbolic, each with a discrimination
probe failing on fabricated data):
* SIX-COPY: the 168 rank-forcing equations per carrier are exactly
  the six substitutions (w_u in {b,c}) x (w_p in {a,b,c}) of ONE
  bilinear system Phi (28 equations on Q^12 x Q^12); all three
  partner rows lie in the SAME subspace; Phi constrains only residue
  pairs {4,5},{4,6},{5,7},{6,7} — the diagonals {4,7},{5,6} are free.
* ANCHOR CLOSED FORMS: H_B(c^8) = per(C) with C[s][r] = A_sr(c,c)
  and NO SMALL-BLOCK DEPENDENCE AT ALL; H_B(b^8) = per(Bm) + sum
  over inside small pairs.  Modulo the pure-c RF equations,
  per(C) = w47 w'56 + w56 w'47.  THIS IS THE REAL b/c ASYMMETRY —
  the c-anchor has no free-symbol route, the b-anchor has four — and
  it STRUCTURALLY REPLACES the refuted colour-deficit conjecture.
* C-BLOCK LEMMA: the 16 words (w0,w1,c,c,c,c,w6,w7) admit no inside
  residue block, so H_B(w) = per(C_w) with no small-block
  dependence, and 8 of the 12 permanent splits are LITERALLY RF
  equations.  Exactness therefore imposes on the CROSSING DATA ALONE
  the closed system F_uh G'_vg + G_ug F'_vh = delta_{(c,c,c,c)}.
  The general 16-unknown version IS SOLVABLE over Q (12 branches),
  so this lemma alone does not close the cell.
* SIGMA-SYMMETRIC SLICE: under the residue-automorphism ansatz,
  three of the 15 homogeneous C-block members are SUMS OF TWO
  SQUARES; over Q exactness forces six coefficients to vanish and
  those six make all 15 vanish identically.  FIRST ARITHMETIC/
  POSITIVITY CONSTRAINT IN THIS LANE — and exactly what all 53
  sampled points violated.
* WITHDRAWN BY THE AGENT ITSELF: an initial "H_B(c^8) = -(F^2+G^2)
  <= 0 vs target +1" impossibility claim is INVALID — a diagonal
  colour gauge preserves the residue instance, purity, E1, harm and
  the whole RF system while rescaling H_B(w), so only zero-vs-nonzero
  is gauge-invariant.

CAVEATS: all of this is for the ONE step-15 all-singular non-DP
residue instance; the sigma-theorem concerns an introduced ansatz
and the gauge does not preserve sigma-symmetry; the C-block system
uses 16 of 6561 words, the other 6545 unanalysed; locus sampling is
searched, not proved.
FOLLOW-UP (steps 27-29): INTERSECTED C-BLOCK IS SOLVABLE — NOT A
KILL.  Small-block-free words are exactly characterised (residue
restriction k with A_rr'(k_r,k_r') = 0 on all six pairs; |K| = 48 =>
1728 words for the step-15 instance, all proved to have
H_B(w) = per(C_w)).  But the extraction adds equations WITHOUT A NEW
SHAPE: the 28 rank-forcing equations kill 23 of the 54 symmetric
crossing products (3/9, 6/9, 6/9, 8/9 on the four constrained pairs,
NOTHING on the two diagonals {4,7},{5,6}), so uniformly over all 48
families every small-block-free word reduces to F G' + G F' on the
SAME two diagonals — 1728 equations of ONE shape, not 48 independent
shapes.  The branch F^s = 0, G^t = 0 (s,t != cc) satisfies all 47
homogeneous family equations identically; exact linear algebra gives
rank 44/54 with THE HARM FUNCTIONAL NOT FORCED TO ZERO (nor either
summand, nor the c-anchor generators); killing both diagonals
entirely (rank 46) STILL leaves harm free.
SIGMA-POSITIVITY IS A CURIOSITY, NOT A TOOL: explicit exact rational
solution of the general 16-unknown C-block system with G_bb = -1 !=
0, so the six forced vanishings are a consequence of the introduced
ansatz, not of the cell.
SCOPE, as demanded — TWO OF THREE FACTS ARE STRATUM-WIDE, THE
LOAD-BEARING ONE IS NOT:
* SIX-COPY STRUCTURE: stratum-wide, in fact GEOMETRY-WIDE (all 480
  carrier rank-forcing equations are row-relabelled copies of the
  single (b,a) system, on a fully symbolic residue).
* ANCHOR DECOMPOSITION: stratum-wide.  H_B(gamma^8) =
  M_small(gamma) H_R(gamma^4) + (2-crossing terms each carrying a
  residue (gamma,gamma) cell) + per(C_gamma), with residue purity
  killing the first group.  COROLLARY (stratum-wide, and THE PROVED
  REPLACEMENT for the refuted colour-deficit antecedent): the
  gamma-anchor has a free-small-block route IFF some residue
  pair-block carries a nonzero (gamma,gamma) interior cell.
* "H_B(c^8) = per(C), no small-block route" IS INSTANCE-SPECIFIC —
  residue purity kills only the HAFNIAN of the (c,c) cells, not each
  cell.  Counter-instance exhibited: an all-singular non-DP residue
  with full 81-word purity carrying a (c,c) cell (A_45 = E_cc+E_aa,
  A_67 = E_aa, A_46 = E_ca, A_57 = -E_ca) on which THE ASYMMETRY
  SWAPS (c gains free routes, b loses them).  NOTHING MAY REST ON
  THE c-ANCHOR'S LACK OF A ROUTE; only the dichotomy is stratum-wide.
CAVEATS: family/intersection are for the one step-15 instance (|K|
varies); only ONE branch of the relaxed system was decided, the
others unenumerated; the pull-back is exact linear algebra on a
RELAXATION, so infeasibility would have been a kill but feasibility
is necessary-not-sufficient, and no exact point of the surviving
variety carrying harm was constructed.
NEXT IF THE LANE CONTINUES: construct an exact point of the
surviving variety with harm, or enumerate the remaining relaxed
branches.  If such a point exists the CROSSING-DATA ROUTE IS
EXHAUSTED and any kill must come from the 4833 words that DO carry
small-block freedom.

THE WALL, NAMED: the full 6561-word system on the 145-parameter
variety (120 crossing + 25 small).  NEXT: extend the C-block
extraction to the other word families with no small-block freedom,
and test whether the six forced vanishings survive dropping
sigma-symmetry — bounded symbolic, Singular not needed yet.

## 30. SCALING THE PROVEN n=6 METHOD: 2590 STRATA — OUT OF REACH AS
      STATED, BUT ONE LEMMA WOULD CUT IT 6.1x (scratch scale8/, two
      independent enumerators sharing no code path)

DEGREE BOUND at general even n, verbatim from the committed
argument: slice-cover sec. 2 gives three rank-one incident edges per
vertex (one per colour, distinct), so d_R(v) >= 3, d_F(v) <= n-4,
|F| <= n(n-4)/2.  n=6: d_F <= 2, |F| <= 6.  n=8: d_F <= 4, |F| <= 16.
All INPUTS survive at n=8 (covering lemma needs m >= 2, applied with
m=7; palette rescaling needs mu_c^8, exists over C; the activity
clause is a 6-site hafnian needing only nonvanishing).  WHAT DIES:
"max degree two => disjoint paths and cycles".  At max degree 4
there is no structure theorem, and the P_k/C_k vocabulary EVERY
downstream checker is written against does not exist.  The defect
budget also degrades: at n=8 it forces D=0 only at |F|=16, whereas
at n=6 it forced D=0 at the top row |F|=6 (which is what powered
saturated-rank-graph-obstruction.md).

STRATUM COUNTS (up to plain graph isomorphism under S_n — validated:
both enumerators return 19 with distribution 1,1,2,4,5,4,2 at n=6
and reproduce table (7) NAME FOR NAME; both reproduce A000088):
  n=8, max degree <= 4: **2590 isomorphism types**
  by |F| = 0..16: 1,1,2,5,11,23,52,103,188,310,438,505,465,309,136,
                  35,6   (labelled count 53,470,067)
REACH OF THE COMMITTED TECHNIQUES: |F| <= 3 (the cheapest technique,
Laurent/low-rank) covers 9 of 2590 = 0.35% (at n=6 it was 8/19 =
42%); |F| <= 6, the ENTIRE range the six-site proof ever touched,
covers 95 of 2590 = 3.7%.  The bulk sits at |F| = 9..13 (2027
strata, 78%) with NO ANALOGUE AT n=6 AT ALL.

*** THE LEVER: A STRONGER FORCED-ANCHOR THEOREM ***
  d_F <= 4 (what is proved)  -> 2590 strata
  d_F <= 3  (needs d_R >= 4) ->  424 strata   (6.1x)
  d_F <= 2  (needs d_R >= 5) ->   46 strata   (56x)
d_R >= 5 is NOT reachable by strengthening the same lemma — there
are only three colours to index witnesses by.  So **424 IS THE
REALISTIC PRIZE, AND IT COSTS EXACTLY ONE EXTRA RANK-ONE WITNESS
PER VERTEX.**

FEASIBILITY: at n=6, 19 strata took ~7 entry-point scripts and 5
distinct techniques (~2.7 strata per checker), and the techniques
were chosen PER STRUCTURAL FAMILY — paths got rectangles, cycles got
the budget, the triangle got its own rigidity.  That organization is
exactly what the path/cycle structure theorem bought and it is gone.
With the same 5 techniques the ratio becomes 518 strata per
technique; individual instances are also ~10x larger (105 vs 15
matchings per coefficient, 6561 vs 729 colour tuples, 252 vs 135
entries).  A FLEET PARALLELIZES EXECUTION OF KNOWN TECHNIQUES, NOT
THEIR INVENTION, and 96% of n=8 strata lie where no committed
technique exists.  VERDICT: direct stratum-by-stratum port is OUT OF
REACH.  Only two things change it: (a) prove d_R(v) >= 4; (b) find a
reduction collapsing whole |F|-families at once — the six-site proof
has no such device, every one of its techniques is stratum-wise.

## 30a. THE FIVE NON-DEGENERACY MECHANISMS: FOUR ALREADY PRESENT,
       WRONG, OR REFUTED; ONE YIELDS A META-THEOREM (scratch newmech/)

M1 LIMIT DEGENERATION — **NOT NEW**.  notes/torus-polystable-fiber.md
already contains the whole thing, nearly line for line: the 21-dim
gauge torus, the closed-orbit reduction, the Hilbert-Mumford
criterion 0 in relint conv{e_s}, the unwound balance condition, and
the Kempf-Ness strengthening.  TWO COORDINATOR ERRORS CORRECTED:
(i) "M1 does not collapse into minimum-support" is WRONG — the repo
minimises |supp A| among ALL solutions of H_B(A) = Delta, not within
a gauge orbit, so a degeneration (which produces another exact
source of strictly smaller support) is excluded by that choice
outright; (ii) M1 is ALREADY PRICED: for a fixed colouring w every
perfect-matching monomial of H_w carries the SAME torus character
sum_u e_{u,w_u}, so along any 1-PS with a limit either every
monomial of a fibre dies or all survive intact.  **A 1-PS LIMIT CAN
NEVER ISOLATE TERMS INSIDE A COEFFICIENT EQUATION — it deletes only
fibres that were identically zero anyway, and so can never break a
cancellation, which is what the landing needs.**  The degeneracy it
manufactures is the WRONG KIND.
TERMINALITY MEASURED (the coordinator's flagged test, run exactly,
with exact rational certificates and passing discrimination probes):
the LIVE FRONTIER SHAPE (24 of 28 blocks, all 9 cells, 6-regular) is
TERMINAL, as is K_8-full, as are three disjoint monochromatic PMs.
Non-terminal only on sparse D1-stratum shapes (<= ~30 cells beyond
the anchors; threshold measurement: terminal 0/10 at k<=16 extra
cells, 9/10 at k=64, 10/10 from k=96).  **M1 IS VACUOUS EXACTLY ON
THE LIVE FRONTIER** — the concern flagged when it was proposed.
M2 CHANGE OF FIELD — the TRANSFER is sound and apparently
unrecorded (worth a lemma; and the rigorous single-prime route is:
use mod-p only as an oracle for the certificate's support and
degree, then reconstruct over Q and verify exactly).  But the
COMPUTATIONAL arm is DEAD: on the already-reduced 107-variable /
133-equation subsystem, EIGHT Singular std() runs — char 0 and
char p alike — had produced nothing after 1h28m.  The premise that
mod-p avoids the binding cost is false here; the mod-p runs are as
stuck as the rational ones.  The full gauge-fixed n=8 system is 231
parameters against 6561 quartics.  1-in-ideal at n=8 is out of
reach by direct Groebner in ANY characteristic.
M3 EQUIVARIANT DECOMPOSITION — **NOT APPLICABLE as proposed**: the
obstruction needs a LINEAR equivariant map, but Phi is homogeneous
of degree 4 and images of isotypic components do not decompose.  For
the connected symmetry the isotypic decomposition IS the repo's
multigrading (each of the 6561 equations is a torus eigenvector with
a distinct character).  Residual value: symmetrising a certificate
by the Reynolds operator as an accelerator.
M4 VALUATIONS / NEWTON POLYTOPES — NOT NEW and ALREADY REFUTED in
the repo: notes/nonarchimedean-route.md sec. 3 runs exactly the
coefficientwise tropical test at n=6 and finds the naive tropical
prevariety NONEMPTY (all 729 colourings satisfy the repeated-minimum
condition), and valuation-filtered-laurent-circuit.md records that
the uniform implication is false at the order-zero tropical level.
M5 CONSTRUCTIBLE IMAGE / BORDER POINT — not a new attack, but it
yields a REAL NEGATIVE META-THEOREM worth recording: the image is
constructible (Chevalley) and its closure is an irreducible variety
of dimension <= 135 at n=6 (<= 252 at n=8) inside a 729- (resp.
6561-) dimensional target, so MANY polynomials vanish on the closure
— AND EVERY ONE OF THEM VANISHES AT Delta.  Hence **ANY certificate
of the form "exhibit a polynomial in the target coordinates that
vanishes on the image but not at Delta" IS DOOMED A PRIORI** — every
elimination-ideal, invariant-theoretic or closure-type no-go.  That
explains structurally why the committed n=6 proof is a stratum/
support case analysis rather than a certificate, and it RETIRES A
CLASS OF PROPOSALS.  The border reading is MEASURED, not proved; the
valuable move is to PROVE it by constructing an exact one-parameter
family A(t) with H_6(A(t)) -> Delta_{6,3}.

## 25e. DESCENT FROM N=10: EMPTY, MECHANISMS LOCATED (scratch
       descent10/, 3 scripts, < 6 s, exact)

Extension formalized and machine-verified (two-pivot expansion
identity at 4->6 and 6->8; 8->10 is 59049 equations / 153 border
unknowns).  WHY EMPTY: (1) the padding functor (A + disjoint pair,
zero border) absorbs all structure — old stars/hafnians/splits
factor through A unchanged, and its 6 mixed-anchor defects
transport nothing new; (2) genuine border-active extensions are
obstructed at the bottom order (4->6: exact 3-word infeasibility
certificate — anchor, mixed anchor, two-colour near-mono), and
non-extendability does NOT constrain the base (K4 exists, does not
extend) so no new equation family on A; (3) conditional counting:
IF an exact N=10 source existed, shape (2,4,4) forces a good pair
entirely inside the old 8 sites (X_old >= 16 > 12), but goodness
does not descend (6 extra border columns), and all adversary
escapes route through the empty-part sector.  THIRD INDEPENDENT
CONVERGENCE: the extension obstruction lives in the SAME two-colour
mixed sector as the D1/D2 kernel H_{Sb u Sc}(b,b,c,c).  Angle
closed; the two-colour kernel remains the sole coupling.

## 25f. COUNTEREXAMPLE HUNT (D1 LOCUS): 6559/6561 REACHED ON A
       >= 22-DIM FAMILY — TERMINAL OBSTRUCTION IS THE MONOCHROMATIC
       PAIR, PROVED RIGID ON THE SUPPORT CLASS (scratch cxhunt/)

Defect profile of the pinned-rectangle witness: 6523/6561 satisfied
IDENTICALLY (zero dependence on free coordinates); 36 two-carrier
core words + b^8, c^8 violated.  REPAIR TRAJECTORY: frozen-chart
step PROVED infeasible (pencil-rank certificate — cross-block cells
alone can never fix two-carrier words); full-chart step SOLVED in
closed form (proportionality + collinearity ansatz makes the
grouped system rank-(1,1)).  RESULT, verified on the committed
oracles: explicit rational packet passing EVERY census fact and
6559/6561 exactness equations; exact Jacobian rank 22 => local
dim >= 22; a harmful-rectangle member (minor = 0, products nonzero)
also certified — harm coexists with 6559/6561.  ONLY b^8, c^8
remain, and on the a-column support class Sigma they are RIGIDLY
unsatisfiable (PROVED: every one of the 105 matchings contains >= 2
residue-incident edges, each (b,b)/(c,c)-dead => H(b^8) = H(c^8) =
0 identically vs required 1).

=> CORRECTED AIM FOR THE D1 KILL: the killing family is the
   MONOCHROMATIC PAIR, not the two-colour words (ALL repairable —
   this session repaired them).  STAGE B = the remaining branch:
   support growth out of Sigma (some residue-side b/c cell nonzero)
   revives 6478 residue-impure words + the Lemma-F purity system —
   EXACTLY the localized battleground the engine agent found
   independently.  TWO LANES CONVERGED on the same final cell.
   The conjecture remains OPEN; the stage-A family is definitively
   not exact.

## 27. STALL S2 REFRAMED: THE CANCELLATION CASCADE (scratch stall2/;
      seven new lemmas, k=4 exhaustive measurement, divergence probes)

PROVED (hand proofs, instance-verified, 5 mutation tripwires, py3/-O):
L1 level dichotomy (h_c(S)*h_c'(V\S) = 0 for all even proper S —
TW2 generalized to every level); L2 alive towers; L3 repaint cost;
L4 generalized S2 (>= 2 PMs needed on EVERY even cancelled set);
L5 pair-union component cost (>= 2 genuine cancellations per proper
component split); L6 CROSSING LEMMA (the main narrowing: cancelled
natural part + anchoredness forces TWO VERTEX-DISJOINT non-anchor
crossing cells — clean-split verified on all 32832 part/edge cases
at n=8; each cancelled part costs >= 4 non-anchor cells; explains
why the committed sec. 4.4 twins lose their anchors); L6' sound SAT
cut-clause family (2460 non-vacuous audits); L7 local 4-cycle
density.

MEASURED (exact): k=4 exhaustive over all 1884 anchor triples —
with L6 repairs ALL 1884 succeed and ALL 7668 extra-matching pairs
cancel simultaneously => EVERY local pairwise S3-candidate is FALSE
at k=4.  BUT the residual layer never clears: >= 6 live splits
always survive, and each kill's forced cells spawn more splits than
the kill removes.  Greedy cascade closure DIVERGES super-linearly:
D(8) 2->108, block8 6->566, D(10) 4->1763, D(12) 7->1365 (beyond
the SAT frontier).  Reach correction: in the cancelling world
8208/8208 parts admit second matchings (committed 4608/8208 is
faithful-world-only — flag for the note's next revision).

SHARPEST FORM: a termwise-dead packet at k >= 3 must be a FINITE
FIXPOINT OF THE CANCELLATION CASCADE (S1 kill + S2/L4 inside cost +
L6 crossing cost + L3/L4 TW2 obligations + L2 towers alive).  No
local obstruction exists (k=4); every probe diverges.  CONJECTURED
(open): the cascade has no finite fixpoint for any k >= 3.  Next:
(a) strengthened n=12 CEGAR with L5/L6'/L7 clauses; (b) hand-prove
no-fixpoint via a cell-debt potential (4 cells per kill, 2
obligations per cell vs finite budget + A3's per-vertex
alive-cofactor requirement).

## 27a. CASCADE FOLLOW-UPS: STRENGTHENED n=12 CEGAR RUNNING;
       POTENTIAL PROOF GAP NAMED = AMORTIZATION

CEGAR (s3_n12_cegar_strengthened.py): clause soundness re-proved
per family — A (L5/L1 two-part) SET-EQUAL to the empty-part slice
of the committed lazy family; B (L6' cuts with iff-AND pair gates,
|S| in {2,4,6}) sound by crossing-free factorization, 355
non-vacuous must-not-prune instances + designed must-prune; C (L7)
ADDS NOTHING (committed add_zero_forbids_unique already covers it).
Instance 153357 vars / 778698 clauses; rounds 1-9 < 6 s; ROUND 11
IS THE WALL (> 5 min, reproduced); run DETACHED pid 94350, log
stall2/n12_strengthened.log.  UNSAT would be a new machine theorem
(n=12 termwise-dead shadow infeasible; no DRAT in this harness —
flag for audit).  Escalations if stalled: branch-split round 11;
Family B at |S|=8.

CELL-DEBT: PROVED (D2+D3) every kill consumes >= 2 inside + >= 2
vertex-disjoint crossing X_c cells — >= 4 distinct, TIGHT (min
observed exactly 4 over all 5808 kills); safety lemma proved
(unmatchable complement => anchor pairs separated in H_c').  GAP
NAMED EXACTLY: AMORTIZATION — cells reusable across kills (max
share 4), layer-1 system under-determined (12.4 equations vs 21.3
unknowns per triple average), so NO counting potential closes the
fixpoint question at layer 1 in either direction.  The blow-up is
recursive; the residual decision IS the strengthened shadow.

## 27b. CASCADE: LAYERED POTENTIAL CANNOT WORK — AND THE stall2
       DIVERGENCE WAS A POLICY ARTIFACT (scratch stall3/, s4_barrier.py)

SIXTH+ INVERSION OF THE SESSION.  New PROVED lemmas: L8 trichotomy
(at most one colour alive on an even proper S, same colour on the
complement) + L8a colour-exclusivity; L9b supp(W_c) u supp(W_c')
CONNECTED unconditionally (strictly stronger than committed (B0),
no cancellation hypothesis); L10 BARRIER DICHOTOMY — a TW2
obligation from a foreign cell {x,y} is FREE iff x,y share a
barrier of G_c, an EQUIVALENCE replacing stall2's one-directional
safety lemma (which MISCLASSIFIED 130/516 = 25% of obligations on
real k=4 states — stall2 conclusions depending on it must be
re-checked); L11 monotonicity (cells only shrink the free graph);
L12 expansion (>= q_c - 1 kills forced, measured-vacuous: q_c in
{1,2}).

ADVERSARY-OPTIMAL MEASUREMENTS (minimum cells over ALL strategies,
committed shadow semantics): m(layer) = 3k for every layer below
the top, then INFEASIBLE — n=6 {9, inf}, n=8 {12, 12, inf}, n=10
{15,15,15,...}.  Demand is CONSTANT until infinite, so there is
NOTHING TO AMORTISE — no counting potential can close the fixpoint
question, in either direction.  LAYERS ARE JOINTLY, NOT
CUMULATIVELY, HARD: at n=8 {1,2,3} infeasible but {1,3} and {3}
alone are SAT at 12 cells (cause counted exactly: extra-PM split
profiles at n=8 are only (0,4,4) and (2,2,4); 960 triples have only
the latter, 492 only the former, so any single family leaves
hundreds of anchor triples free).  TRUE BRANCHING ~ 1.7, NOT >= 5:
78.3% of TW2 obligations are free by barrier-sharing (404/516) —
stall2's greedy divergences (2->108, 6->566, 4->1763) are a POLICY
ARTIFACT, not a property of the cascade.

ESCAPING STRATEGY NAMED: keep the support BARRIER-COARSE — place
each forced kill cell with both endpoints inside an existing
barrier class of the other two colours and its obligation costs
nothing (bare anchors have free-rate 100%; by L11 freeness is lost
only via the kill cells themselves).  THE SHARP REMAINING QUESTION,
FINITE AND DECIDABLE PER k: can all layer-0 kills be realised by
cells staying inside the other colours' barrier classes?

BARRIER-COARSE ESCAPE: DECIDED at k=4 (exhaustive, all 1884
triples, all minimal placements) and k=5 (1500 random of 159232,
seed 20260804); k=6 running with a live SURVIVOR detector.
(i) The escape is REAL but not universal: NO coarse placement exists
for 1260/1884 = 66.9% of k=4 triples and 27.5% at k=5 — those
cascades genuinely pay at layer 1 — so there is NO forced-cost
lemma at layer 1 and L10/L11 alone cannot close it.
(ii) The survivors DIE ANYWAY: pinned into the full every-pivot
recurrence shadow they are UNSAT — REFUTED FOR EVERY COMPLEX
WEIGHTING, < 0.1 s (all 624 k=4 survivors; at k=5 all 16443
placements die to the purely weight-independent test that a proper
3-split with uniquely-matched parts is live for all weights).
(iii) RESOLUTION: barrier-coarseness does NOT evade the joint
constraint — it is EXACTLY what the joint infeasibility forbids.
Coarseness buys layers 0 and 1 for free; the payment falls entirely
on the higher split families.
*** THE CASCADE'S CURRENCY IS HIGHER-LEVEL DEADNESS, NOT CELLS. ***
Consequences: the no-fixpoint theorem is NOT recoverable by any
cell-cost mechanism, and the barrier-coarse configuration is NOT a
counterexample lead (refuted for all weights at k=4 and k=5).

SHADOW SOUNDNESS RE-VERIFIED (s4_target.py) — THIS IS WHAT MAKES
EVERY SHADOW UNSAT A THEOREM.  Setting z[c][S] := [h_c(S) != 0]
satisfies (R0), (R2) always and (R1),(D) under the hypotheses; the
only non-trivial content is "a nonzero sum has a nonzero summand"
and "a sum with exactly one nonzero summand is nonzero", both valid
over ANY field.  Computationally re-verified: 2100/35520/86346
recurrence-clause evaluations at n=4/6/8 on exact random
configurations, all satisfied; deadness family checked on the K_4
dead configuration (18/18); mutation tripwire (strengthening "not
exactly one" to "none") correctly rejected by a cancelling
configuration.  So shadow UNSAT => theorem over all fields; shadow
SAT => nothing.  (This retroactively certifies the committed n=8
drop-(0,2,6) UNSAT and will certify the n=12 run if it lands.)

L14 (SPECTRUM LEMMA, PROVED — new): every extra PM P has
a_c = |P ∩ M_c| <= k-2 for every colour, because P xor M_c is a
union of alternating cycles of length >= 4, so |P \ M_c| >= 2.
Hence the profile spectrum is contained in {compositions of k with
max <= k-2}, and MEASURED EXACT at k=3,4,5,6 (all 32 triples at
k=3; all 1884 at k=4; samples of 3000 at k=5,6).
* COROLLARY — A COMPLETE WEIGHT-INDEPENDENT PROOF AT k=3 OVER ANY
  FIELD: the only composition of 3 with max <= 1 is (1,1,1), so
  every extra PM splits V into three 2-sets, one per colour; each
  part is a single nonzero cell, so the split is live for EVERY
  weighting.  With Theorem B this PROVES the target at k=3 —
  replacing a machine check with a real proof, and explaining why
  all 32 n=6 triples were "dead on arrival".
* COROLLARY — FORCED KILL COLOUR: a kill needs a_c >= 2, so at
  profile (1,1,k-2) the kill colour is forced (960 of 1884 triples
  at k=4 entirely).

TWO NATURAL ROUTES TO A GENERAL ARGUMENT ARE NOW BOTH CLOSED BY
EXPLICIT MEASURED COUNTEREXAMPLES:
(a) SINGLE-FAMILY COVERING IS DEAD — for several distinct profiles
    there exist triples exhibiting THAT PROFILE ALONE (k=4: 960 only
    (1,1,2), 492 only (0,2,2); k=5: 125 only (1,2,2), 59 only
    (0,2,3); k=6: 17 only (0,2,4), 2 only (1,2,3), 1 only (2,2,2)),
    so no single split family covers all anchor triples for any
    k >= 4.  The joint system is genuinely irreducible.
(b) THE k-UNIFORM UNIQUE-PM TEST IS NOT SUFFICIENT — false at k=4:
    the 624 barrier-coarse survivors have NO unique-PM live split
    and are refuted only by the full shadow.  It is a cheap
    sufficient condition; its strongest form IS the theorem at k=3
    (L14's first corollary).

STATUS: k=3 CLOSED cleanly over all fields; k >= 4 OPEN with both
natural general routes eliminated.

HYGIENE: discard stall3/n8_layered_minpivot_WEAK.log (first encoder
used only the minimum Laplace pivot; its SAT verdicts are
artifacts).  Pillar II is WEAKER than previously represented: its
divergence evidence is void; what survives is L8-L12 + the joint
infeasibility measurements.  Next: decide the barrier-coarse
question per k.

## 28. LITERATURE / PRIOR-ART CHECK (scratch litcheck/; first ever
      run in this program — three action items)

STATE OF THE ART.  The conjecture (Krenn-Gu; "monochromatic quantum
graph") is OPEN.  OUR TARGET IS THE FIELD'S NAMED NEXT CASE: N=8,
d=3 appears as `eqSystem8_no_solution_d3`, marked "research open"
with `sorry`, in DeepMind's formal-conjectures Lean repo.  Published
ladder: Bogdanov 2017 (real positive weights, N>=6 d>2); Cervera-
Lierta/Krenn/Aspuru-Guzik 2021 SAT (N=6 d>=3 and N=8 d>=4,
MONOCHROMATIC-EDGE model, Quantum 6, 836); Chandran-Gajjala
d > N-2 (EJC 2026) and d > N/sqrt(2) via LOCAL SPARSIFICATION
(Quantum 8, 1396); Chandran-Gajjala-Illickan MFCS 2024 (Delta<=3,
kappa<=2; minimal counterexample is 4-connected); DeepMind agent
2026 (d >= N over C, announcement only, arXiv:2605.22763).

3-CONNECTIVITY GUARD IMPORTED AND MEASURED (scratch conn3/;
MFCS Thm 1.9 "if kappa(G) <= 2 then mu(G) <= 2", verified verbatim
from their intro.tex, scope "when BOTH bi-coloured edges and
multi-edges are allowed", and NO |V|>4 hypothesis — it applies at
every even N >= 4; free sharpening mu(G) = mu(mcg(G)) lets it be
applied to the matching-covered core).  Two independent kappa
routines (exhaustive min vertex cut + Menger max-flow) that must
agree; discrimination probe distinguishes Q_3 / Wagner V_8 /
two-K_4-minus-e (identical degree sequence 3^8, kappa 3/3/2).

RESULTS: KILLS NOTHING CURRENTLY OPEN.  Corroborates the k2_step16
frontier point (degree-2 site, kappa=2 — a third independent kill).
ONE GENUINE UPGRADE: the committed OMEGA GUARD has kappa = 1 (cut
vertex p) and its matching-covered core is DISCONNECTED (pq and pr
lie in no PM; deleting p into {q,r} strands the odd set {a,c,d}) =>
NO WEIGHTING ON THAT SUPPORT CAN EVER BE EXACT, not merely the one
tested.  Fold that sentence into the committed artifact at the next
commit cycle.
STRUCTURAL REASON IT CANNOT HELP AT THE FRONTIER: connectivity is a
LOW-SUPPORT filter and our live N=8 objects carry 24 of 28 blocks.
Measured pruning: 94.6% of the full N=6 support space (only 1768 of
32768 supports can carry an exact source at all); 12.9/15.4/14.9% of
the committed census enumeration at k=4/5/6; at N=8, 100% up to
|E|=12 but 0% from |E|=24 — exactly where the live objects sit.
RECOMMENDATION: commit as a cheap necessary condition in the
census/support machinery (min-degree in the inner loop, full kappa
at the branch head), NOT as an attack.

*** CITATION ERRATA (citesweep agent, primary text; correct these
wherever they were repeated above) ***
(1) arXiv:2605.22763 is TSOUKALAS ET AL. (DeepMind), "Advancing
    Mathematics Research with AI-Driven Formal Proof Search" —
    KRENN IS NOT AN AUTHOR.  The Krenn/Firsching/... tensor-algebraic
    no-go is a SEPARATE in-preparation work.
(2) arXiv:2209.13063 is VARDI-ZHANG, "Quantum-Inspired Perfect
    Matching under Vertex-Color Constraints" (complexity of
    EXISTS-PMVC).  The "Tutte encoding, Dicke states to n=40"
    description matches NOTHING in it — that suggestion was based on
    a bad secondary description and must not be acted on as stated.
(3) THE 2021 SAT SCOPE IS NARROWER THAN RECORDED.  Primary text:
    "We obtained K = False for n up to 8 and d = n/2 colors" — i.e.
    exactly (n=6, d=3) and (n=8, d=4).  d >= n/2 is their
    CONJECTURE, not their result.  It is a Boolean edge-literal
    encoding with an escape clause for cancellation, NOT a weighted
    no-go.  => our diagonal theorem's excess over published
    monochromatic-edge coverage (N=8 d=3, N=10 d=3) STANDS.
(4) The Lean file's own bibliography is wrong in three places
    (Chandran/Gajjala initials; Soltesz as "U."; M. Krenn listed as
    a coauthor of arXiv:2407.00303) — recorded as errata in
    references/REFERENCES.md so nobody copies them.
(5) The EJC 2026 volume/page for Chandran-Gajjala is UNVERIFIED
    (arXiv v2 carries no journal-ref) — marked [unverified].
(6) OPEN CROSS-CHECK before any cubic novelty claim: MFCS Thm 1.11
    settles Krenn-Gu for max-degree-3 INPUT graphs in the general
    model, while our cubic results concern cubic OCCURRENCE UNIONS
    derived inside a realization.  Neither is known to subsume the
    other.
NOTHING PUBLISHED COVERS d=3 FOR N>=6 OVER C.  Prize: EUR 3,000.

ACTION 1 (DEFECT IN COMMITTED MATERIAL): THEOREM B IS NOT NEW.  It
is Bogdanov's observation (2017, MathOverflow 267013) = Theorem 1 of
Chandran-Gajjala EJC 2026 ("for G not isomorphic to K_4, mu(G) <= 2;
mu(K_4) = 3"), multigraph form = Thm 1.7 of MFCS 2024; classical
cousin: matching-covered graphs with exactly three PMs are
bi-subdivisions of Theta or K_4 (Lovasz 1983 ear decomposition).
notes/termwise-rank3-cubic-uniqueness.md (commit 364ccab) calls it
"new, standalone" — FALSE NOVELTY CLAIM, must be amended to "a
self-contained proof of Bogdanov's observation" with citations.

ACTION 2 RESOLVED BY PRIMARY SOURCE (coordinator, verified直from the
Lean file itself): the open problem's weight model is BICOLOURED.
Verbatim:
    structure EdgeN (N D : Nat) where
      u : V N ; v : V N ; i : Fin D ; j : Fin D
    abbrev WeightsN (N D : Nat) (a : Type) := EdgeN N D -> a
i.e. a weight per (edge, colour at u, colour at v) — exactly our
A_uv(i,j) with i != j ALLOWED.  EqSystemN quantifies over EVERY
iota : V N -> Fin D with pmSum = 1 if iota constant else 0 — exactly
our exactness condition.  => OUR MODEL IS THE OPEN MODEL; the
pencil form haf(sum xi Wi) = sum xi^k was only a SPECIALIZATION
(and the committed pencil guard already proves it is the wrong
target).  Confirmed open over C: N=8 D=3 AND N=6 D=3 (also D=4, 5;
D=6 is solved), plus the R, Z and {-1,0,1} analogues at N=6,8,10.

=> NEW QUESTION RAISED BY THIS (high priority): our committed
   SIX-SITE impossibility ("Theorem 1.1, no exact source at 6
   sites") — if it is stated in the BICOLOURED model over a field
   containing Q, it settles eqSystem6_no_solution_d3, which the Lean
   repo marks RESEARCH OPEN.  Either (a) our six-site theorem is
   model-restricted (= the 2021 SAT result, monochromatic edges), or
   (b) we hold an unpublished resolution of an open case.  MUST BE
   DETERMINED FROM THE COMMITTED ARTIFACT, not assumed.

   ANSWERED (coordinator, primary sources):
   * proofs/six-site-arbitrary-complex-obstruction.md Theorem 1.1
     reads: "There is no collection of complex matrices A_uv" with
     A_uv in V_u (x) V_v an ARBITRARY endpoint-ordered 3x3 complex
     matrix, such that H_6(A) = Delta_{6,3}; and sec. 2 states "no
     genericity, positivity, or restriction on the number of
     parallel sources is used" and "rules out every palette of size
     at least three".  Endpoint-ordered arbitrary A_uv = BICOLOURED
     (entries A_uv(i,j), i != j allowed).
   * The 2021 SAT result at n=6 is explicitly restricted: "for n = 6
     with allowed multi-edges but NOT bicolored edges, it was proved
     using SAT solvers that dimension is less than 3".  The
     bicoloured case is the hard one ("even for 4 vertices ...
     surprisingly challenging to prove analytically").
   => OUR COMMITTED THEOREM 1.1 IS EXACTLY THE OPEN CASE
      eqSystem6_no_solution_d3 OVER C.  If it holds, the program
      already contains an unpublished resolution of an open case,
      independent of the N=8 endgame — and much of our own work
      (e.g. the descent analysis, section 25e) is built on it.
      MAXIMUM-RIGOUR INDEPENDENT VERIFICATION DISPATCHED.

*** VERDICT: SOUND (2026-08-04, scratch sixsite/NOTES.md). ***
NO MATHEMATICAL GAP.  Every citation resolves, every claimed finite
check is performed by a committed checker, the whole chain
reproduces.  What the auditor did INDEPENDENTLY: re-derived the
19-type rank-defect census by hand (1,1,2,4,5,4,2 = 19, exactly
table (7); 11 types in sec. 5.1 + 8 in sec. 5.2); wrote its OWN
from-scratch encoder and got UNSAT for exactly {C_3+P_2+P_1,
C_3+P_3, C_5+P_1, C_3+C_3} and SAT for the other 15, matching every
support-UNSAT claim — and C_3+C_3 came out UNSAT WITHOUT the
basis-edge strengthening, i.e. that branch is STRONGER than claimed;
independently reproduced the five-edge lemmas (45/45 entries, 45/45
rectangles), the saturated C_6 forcings (54/54), and the four-edge
row certificates; matched the committed canonical CNF's 11163
variables exactly.  All 7 CNF sha256 digests reproduce
byte-identically; the C_3+3P_1 stratum has a RESOLUTION-LEVEL DRUP
refutation (variables=11163, cnf_clauses=85755, proof_additions=
57045) so no SAT solver is trusted there; zero bare asserts in all
10 load-bearing scripts; -O output byte-identical.
REDUCTION VERIFIED (sec. 2): projection to three colours keeps
matrices arbitrary; the rescaling mu_c^6 = lambda_c^{-1} exists over
C and D A_uv D preserves arbitrariness AND endpoint asymmetry.
FORCED-ANCHOR THEOREM verified (slice-cover.md), including the
Zariski-density step and the case-split exhaustiveness.
COUNTEREXAMPLE HUNT: exact settings + holomorphic Levenberg-
Marquardt over 135 complex unknowns / 729 equations; d=2 control
finds exact solutions (kernel dim 17); at d=3, 400 random starts,
best |r|^2 = 7.15e-15, with a REPRODUCIBLE near-miss attractor
(trials 6/33/41 converge to |r|^2 = 1.1808e-15, confirmed at 50
digits, not a precision artifact) and |r|^2 ~ ||A||^-23.5 as
||A|| grows.  READING: Delta_{6,3} is a BORDER POINT — approachable
but not attained — which is consistent with the theorem, explains
why only exact methods can settle it, and is a structural hint for
n=8.  NO COUNTEREXAMPLE FOUND.
TRUST BOUNDARY (stated honestly by the proof itself): the seven
|F|<=3 formulas and the |F|=4,5,6 UNSATs have no DRAT and rest on
solver correctness; mitigated by the DRUP stratum, four-engine
re-refutation of C_3+C_3 and C_4+P_2, cross-engine checks on four
g4-only results, and three independent re-derivations.  build_case
rebuilds the certified CNF only from semantically re-audited
records, so search bugs cannot reach the certified formula (tamper
tests raise).
THREE NON-MATHEMATICAL DEFECTS (fixes dispatched): (1) the proof
never cites notes/six-vertex-rank-graph.md though its defect budget
is load-bearing for |F|=6; (2) the entry-point list omits
search_f5_support_sat.py and verify_color_sensitive_support_
obstruction.py; (3) computations/verify_generalized_laurent_3p2.py
is BROKEN as committed (AssertionError at :124; under -O a
ValueError at :135 because the asserts are stripped) — not on the
Theorem 1.1 path, and the only file in the area using bare assert.

=> THE PROGRAM HOLDS A VERIFIED PROOF OF eqSystem6_no_solution_d3
   OVER C (the bicoloured/general model), WHICH THE DEEPMIND
   formal-conjectures REPO MARKS "RESEARCH OPEN".  Publishable
   independently of the N=8 endgame.  The conjecture itself remains
   OPEN at n >= 8.

ACTION 2 (ORIGINAL AMBIGUITY, now resolved above): Krenn's page credits the 2021
SAT result with (N=6, d>=3) in the MONOCHROMATIC-EDGE model, yet the
Lean file lists N=6 D=3 over C as OPEN — they reconcile only if the
SAT result is model-restricted.  Our hafnian pencil form
haf(x0W0+x1W1+x2W2) = sum xi^k is the monochromatic-edge model, but
our actual objects carry two-colour cells (D1's A_pq(b,c)).  WHICH
MODEL OUR THEOREMS LIVE IN MUST BE PINNED DOWN AND STATED — it
decides whether we are attacking the open case or a solved
restriction.

ACTION 3 (TECHNIQUE TO ADOPT): LOCAL SPARSIFICATION (arXiv:2304.06407)
— a vertex-count reduction that bought d > N/sqrt(2) without
exhaustive search; cf. MFCS Thm 1.10 (kappa <= 3 => delete two
vertices without lowering mu).  If any version survives at d=3 we
get induction on N >= 8.  NOTE: our own descent-from-N=10 came back
EMPTY (section 25e) — the literature's reduction goes the OTHER
direction (delete, not extend) and succeeded.  Also worth adopting:
Tutte-theorem-based Boolean encodings (Vardi-Zhang IJCAI 2023,
arXiv:2209.13063) — encodes the for-all-PM condition via Tutte
rather than enumerating matchings; verified Dicke states to n=40
where plain QBF dies at n=8; a drop-in scaling upgrade for our
recurrence-shadow SAT.

ACTION 1 DONE + THREE FURTHER FINDINGS (scratch attrib/):
* Attribution fixed in notes/termwise-rank3-cubic-uniqueness.md
  (new sec. 3.0) and the checker docstring; attribution now INSIDE
  the frozen digest (7503352a...) so silent removal changes the
  hash.  Verdict: our Theorem B is the CUBIC CASE COROLLARY of
  Bogdanov's observation, and STRICTLY WEAKER than CGI Thm 1.7
  (multigraphs excluded).  No priority claimed.
* INTERNAL REDUNDANCY (the bigger embarrassment): Theorem B was
  ALREADY PROVED IN THIS REPO in the stronger multigraph form
  BEFORE 364ccab — proofs/odd-near-perfect-gadget-obstruction.md:33
  ("the standard three-one-factors lemma") and
  notes/finite-obstruction.md:446-464 (same parity/interlacing
  argument), and it is invoked as "standard" in ~16 committed
  files.  The termwise note was the ONLY place claiming standalone
  status for something the repo elsewhere calls standard.
* REPO-WIDE CITATION GAP: grep for Bogdanov/Chandran/Gajjala returns
  ZERO hits outside this attack map; references/ contains only
  .gitkeep.  Every "standard three-one-factors lemma" invocation is
  uncited.  Also check notes/three-cut-cp-uniqueness-tight-boundary.md:146
  Lemma 4.1 against Lovasz 1983 (matching-covered classification).

MODEL PINNING, ESTABLISHED FROM REPO EVIDENCE (scratch
attrib/model-pinning-draft.md):
* Top-level statement (consolidated-proof-frontier.md:36-41,
  H_B(A) = Delta_{B,3} with endpoint-ordered A) = GENERAL/BICOLOURED
  over C = THE OPEN CASE.  Checkers verified genuinely general:
  matching_tensor ranges over all 3^N words, set_cell writes any
  (i,j) with the u>v transpose, and off-diagonal cells are actually
  populated in the guards.
* MONOCHROMATIC-EDGE (diagonal) artifacts:
  proofs/diagonal-hafnian-recurrence-obstruction.md (hypothesis is
  literally "diagonal aggregate edge matrices"), termwise Theorems
  A/C + the stall, and the pencil guard.  The pencil form is a
  monochromatic-edge object.
* BRIDGE: exact-source-live-split-forcing (general model; consumes
  the diagonal UNSAT to force nonzero off-diagonal cells).
* MACHINE-VERIFIED COLLAPSE: with diagonal blocks at N=6, all 729
  words satisfy H_B(A)(w) = prod_c haf(W_c[S_c]) exactly => the
  monochromatic-edge model IS the diagonal system.  No model error
  is being made; the diagonal results are necessary conditions on
  the diagonal shadow.
* CONCRETE RISK: in the MONOCHROMATIC-EDGE model our own diagonal
  theorem already closes N=8 d=3 (and N=10).  Any diagonal-family
  artifact omitting its model qualifier reads as attacking a
  restriction we have already solved.  Every such artifact needs the
  qualifier sentence (draft in scratch).

OVERLAP WARNING — RESOLVED, NO OVERLAP (sparsify/ agent read the
source .tex): CG's "crossing pair" is two edges crossing w.r.t. the
CYCLIC VERTEX ORDER OF A HAMILTONIAN CYCLE, and a "drum" is a
4-cycle from a nice crossing pair plus two cycle edges — a
planar/cyclic-order notion inside their d=2 Type-2 classification,
FORMALLY UNRELATED to our crossing pairs (pairs straddling a live
split of the vertex set).  "Crossing pairs"/"drums"/"legal-illegal
edges" appear in NEITHER 2304.06407 NOR 2407.00303.  One genuine
kinship to acknowledge, not a collision: their COLOUR-ISOLATED EDGE
(monochromatic, colour-degree 1 at both ends; Quantum Lemma 2.4) is
conceptually adjacent to our E1 pendant / star-injectivity facts,
though the statements differ.  (The Theorem-B attribution issue is
separate and real — see ACTION 1.)
Kevin M.'s N=4 Groebner certificate = our ideal-membership approach.

COMPETITION: Krenn, Firsching, Tsoukalas, Gajjala, Gu, Chaudhuri,
"A Tensor-Algebraic No-Go Theorem for High-Dimensional Photonic GHZ
States", in preparation 2026 (ref [38] of arXiv:2605.22763).  Scope
d >= N does NOT cover d=3.  Watch arXiv for [38] and watch the Lean
repo for eqSystem8_no_solution_d3 flipping to solved.

NOT FOUND (genuine gaps we may own): no tensor-rank/border-rank
no-go for this problem; no algebraic-geometry attack beyond the N=4
Groebner run; and the hafnian-pencil identity
haf(sum xi Wi) = sum xi^k is UNSTUDIED as such — our phrasing is a
novel presentation of a known object.

## PAUSE STATE (session limit hit ~evening 2026-08-03; RESOLVED —
   all five agents resumed after the 21:30 PT reset, both local
   computations survived the gap)

Terminated agents to resume (all have transcripts; resume via a
continue message): (1) D1/D2 attack = K1 first test (was reading
Theorem C checker + recurrence proof; nothing produced yet);
(2) census-pair packaging (was starting to write the checker);
(3) stall S2 attack (early); (4) K4 propagation (baseline chart-26
checker re-run PASSED; was reading translate-chart description);
(5) K3/K5 dual-structure tests (early).  STILL RUNNING locally,
unaffected: n=6 block Wiedemann (rank(A), started 17:38, log
scratchpad/n6trans/wiedemann.log) and the reconciliation agent's
exact-certificate solve (its agent will report when it lands).
Committed today: 1bbb4d9, 6c35854, 364ccab (commits 12-14).

## 25a. KEYSTONE SCOPING: FIVE CERTIFICATE-STYLE CANDIDATES K1-K5
      (scratch keyscope/; the strategic successor to the closures)

Component classification post-closure: I comparison-type DEAD
(125e231 + 6c35854), II certificate-type LIVE (= kill D1/D2), III
style-agnostic (comparison route dead; direct-construction route
survives as K5), IV mixed (off-diagonal clause dead; diagonal
membership clauses = K3/K4).  Support-1 duals independently
re-verified dead at n=6 (0/3375; 6 at n=4 = the GHZ sigma-bijections)
— the support-2 analogue of the GHZ indicator is an ALTERNATING-
EXCHANGE character (all three known small duals have this shape).
Chart-26's small certificate is a property of the FACE (pure-anchor
row + triangular exposure), not the ideal.

K1 D1/D2 exclusion — first test: confront D1's minor with the
   eq.-(3) minor certified nonzero at the synchronized
   representative (IN FLIGHT: the D1/D2 attack agent).
K2 E3-liveness contradiction (S3 integration) — exact + live split
   + E3-pure deleted-pair ⇒ h0h1h2 = 0 contradiction; with K1
   closes the label split at ALL even N.  First test: extend the
   720/729 stall guard to full exactness with E3-purity imposed.
   Half the keystone: largest certifiable structural gain, but the
   landing still needs K3/K4/K5.
K3 Exchange-dual classification + class-transversality law (n>=6)
   — first test: check the three known duals are exchange
   characters; then the untested class-transversal degree-2 shift
   at n=8 (Arm B).  Must land before the Wiedemann verdict is
   interpreted.
K4 Face-certificate localization/propagation (chart-26 made a
   theorem; REPLACES interface (25) rather than realizing it) —
   first test: transport the five-row bypass to a second Hamilton
   row + one translate-chart row.
K5 Normalized rootless annihilator (Component III direct) — first
   test: compute the stabilizer Reynolds average of the 726-dim
   family and evaluate the provenance criterion.  Lowest info
   density; failure = decisive demotion of Component III.

Ranking: K1 > K2 > K3 > K4 > K5.  Explicitly NOT resurrected: all
comparison-style material, dict5 primed blocks, t^k escalation,
pencil DIAG-infinity, frontier sec. 6.

K3 FIRST TEST: CONFIRMED on all three known duals (scratch k3k5/).
Precise definition AE0 (orbit-constant, two-valued +-kappa), AE1
(fixed-point-free sign-reversing involution on each column's signed
completions — generator-local annihilation), AE2 (paired matchings
differ by a SINGLE alternating cycle).  n=6 two-row dual: PASS (one
column orbit, C4 exchange; anatomy: 360-orbit has colour-0 = colour-1
doubled edges).  chart-25 four-row: PASS (56 labelled columns, all
single colour-preserving C4).  chart-26 cells: PASS (identity (7)
re-verified over Z).  Support-1 GHZ indicators = the degenerate
one-orbit case.  Refutation profile for an Arm-B dual: must violate
AE0, AE1, or AE2 — checker ready.  Next: n=6 support-3 successor
dual (first support-3 test), then Arm-B.

K5 FIRST TEST: CRITERION-INCOMPLETE — and the missing ingredient is
named: THE O3 CHAIN MAP (the same object section 2 identified as
"the missing chain map").  Family re-derived exactly (dim 726, chi*
= delta_X2 unique minimal-support element); chart stabilizer TRIVIAL
(|G|=1 over all 4320 of S6xS3) so Reynolds = identity and
normalization succeeds canonically — the stall is NOT normalization,
it is the missing comparison from the 729-word annihilator space to
A_4^*.  Proxy A vacuous (B,R realise to zero — that degeneracy IS
the problem); proxy B non-vacuous (provenance quotient dim 3).
Component III reachable only through Component II until O3 is built.
Next: construct O3 on the literal filtered blocks, re-evaluate
eq. (5) on the image of chi*.

K3 SUPPORT-3 TEST: CLASSIFICATION CONFIRMED (scratch k3k5/,
k3d_n6_support3_exchange.py).  The successor dual re-derived from
scratch (target-supported dual space EXACTLY 1-dim, support 3,
masses 24/48/24, pairing 48): AE0/AE1/AE2 all PASS — all 144
incident columns balanced, all exchanges single C4s.  The prepared
relaxations were never triggered.  Arm-B prior sharpened: support
growth alone does not force an AE violation.

K5 CLOSED: NOT-PROVENANT (k5b_o3_extension_provenance.py).  psi^odd
is type-disjoint from the needed comparison (243 deleted-site words
vs 729 residual-site words) — verdict built independently at the
segre-gap layer with literal committed objects.  MECHANISM (a
theorem of the committed objects under any filtration-respecting
comparison): M_q^col maps the whole cap family onto span{delta_X0,
delta_X1} TERMWISE (every B/R cap edge individually
tangent-invisible), so the induced mu-data of the ENTIRE 726-dim
family is zero, while the unit reading is realised only at cap
degree 3 ([X2]R^[3] = 1).  The obstruction is FILTRATION DEPTH, not
normalization.  Component III's direct certificate is dead at this
chart; Component III reachable only through Component II.  Any
resurrection must exhibit a committed source operation reaching the
anchor at cap degree < 3.

=> KEYSTONE FUNNEL NARROWED AGAIN: surviving candidates K1 (in
   flight), K2 (dispatching), K3 (confirmed twice, awaits Arm-B),
   K4 (sweep running).  K5 dead, Components I and III dead.

K4 FIRST TEST: BOTH TARGETS TRANSPORT — but the SHAPE does not
(scratch k4prop/; standalone frozen verifier verify_k4_transport.py
PASS, ledger 8f2e1b...).  (a) Second chart-26 row (colour-degrees
(4,3,0) vs (3,2,2) — S8xS3-inequivalence proved by invariant): face
is 94+9 (face shape is row-dependent), certificate is ONE row —
H_00000022 restricts to a literal monomial identity.  (b) Translate
chart-25 row: face 95+0, TWO-row certificate H_12102220 −
x12^21*H_12202220 = x14^22*x37^00, same anchor+bridge character —
THE FACE MACHINERY CROSSES CHARTS.  Both verified monomial-by-
monomial over Z with support-cleared literal identities.  FAILURES
OF THE LITERAL PATTERN: committed exposure vocabulary insufficient
(4 resp. 7 unexposed coordinates; admissibility survives under
widened ordered general two-row eliminations, each verified in the
cumulative quotient); the five-row packet is face-specific (the
committed face admits NO 1-row certificate, exhaustive).  K4
REFORMULATED: every terminal Hamilton row's face admits a small
(<= 5-row) certificate under the widened vocabulary — uniformity
must come from face classification, not packet transport.  Next:
the classification sweep over all S8xS3 classes of terminal
Hamilton rows.

## 26. n=4 CERTIFICATE ARCHAEOLOGY: THE COLOUR LAW IS MIS-STATED AND
      ITS MECHANISM IS n=4-ONLY (scratch certanat/; corrects lane M1)

Exact certificates EXTRACTED and verified monomial-by-monomial over Z
for both proved shifts (964/1104 orbits, 7422/8110 terms, ALL 78
mixed words used, denominators to 10^7; CRT at 3 primes + rational
reconstruction).  Anatomy: NO ANCHOR (all 10 target orbit-monomials
individually NOT in I_mix, two primes — the chart-26 move has no
n=4 analogue); rigid cores extracted (13/3/3 forced orbits, unit
propagation over Q); cancellation graph thousands-fold, no preferred
sector; NO shared skeleton with chart-26 (its factorisations exist
only because 96 coords were zeroed).

MECHANISM [PROVED]: a target monomial has a support-1 dual iff it
contains no mixed perfect matching; GHZ monomials M_sigma are exactly
the sigma-bijections; membership criterion at n=4 (19 shifts, zero
exceptions): s*H0H1H2 in I_mix <=> no sigma leaves every edge of s
sigma-native.  The shift's ONLY essential job: unlock the GHZ
monomial by supplying a mixed matching (lambda=1 rigid-core column).

=> COLOUR LAW CORRECTION: ">=2 colours" is FALSE in both directions
   at n=4.  One-colour shifts that WORK: x01^00*x02^00, x01^00*x13^00
   (two colour-0 edges on DIFFERENT classes).  Two-colour shifts that
   FAIL (explicit support-1 duals): x01^00*x02^11 and four more.
   Real invariant: CLASS-TRANSVERSALITY of the shift's off-nativity.
   DISCREPANCY with the earlier colour-law lane ("one-colour
   obstructs / >=2 clean, proved n=4") — reconciliation dispatched:
   different shift family, or a wrong computation.

=> n>=6: support-1 duals COUNT ZERO at n=6 (3375 targets) and n=8
   (1157625) [PROVED, exhaustive] — every cubic union on >=6 vertices
   has a colour-mixing PM; K_4 is the unique exception (the SAME
   K_4-uniqueness as Theorem B of the rank-3 pair, now on the dual
   side).  Predictions: (A) any n=6 obstruction has support >=2, a
   different animal; (B) if membership holds, expect an O(10^5)-orbit
   machine certificate, no anchor; (C) the n=4 positive mechanism is
   already gone at n=6.  The Wiedemann verdict's interpretation must
   be restated under the corrected law BEFORE it lands.
